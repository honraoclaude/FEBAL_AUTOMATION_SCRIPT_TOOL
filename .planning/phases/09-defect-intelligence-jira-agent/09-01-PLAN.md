---
phase: 09-defect-intelligence-jira-agent
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DEF-01, DEF-02]
files_modified:
  - apps/api/alembic/versions/0009_defects.py
  - apps/api/app/models/execution_history.py
  - apps/api/app/models/defects.py
  - apps/api/app/services/worker/job.py
  - apps/api/app/core/config.py
  - apps/api/app/services/defects/__init__.py
  - apps/api/app/services/defects/classifier.py
  - apps/api/app/services/defects/fingerprint.py
  - apps/api/app/services/defects/infra_health.py
  - apps/api/app/services/defects/evidence.py
  - apps/api/tests/unit/test_classifier.py
  - apps/api/tests/unit/test_classifier_evidence.py
  - apps/api/tests/unit/test_fingerprint.py
  - apps/api/tests/unit/test_no_llm_in_classifier.py
  - apps/api/tests/functional/test_migration_0009.py

must_haves:
  truths:
    - "A product-failure TestResult now persists the last-attempt error text (job.py no longer discards `output`)"
    - "The pure classifier maps evidence to one of {infrastructure, automation, product_defect} with a 0-100 confidence, citing the signals"
    - "The classifier module imports NOTHING from the LLM/gateway/langchain/langgraph path (grep gate green)"
    - "A stable fingerprint hashes (class + normalized error message + flow + step), stripping numbers/ids/timestamps/uuids"
    - "Migration 0009 upgrades + downgrades + upgrades reversibly (classifications + defects + test_results.error_text)"
  artifacts:
    - path: "apps/api/app/services/defects/classifier.py"
      provides: "Pure 3-way classify(evidence, weights) -> {classification, confidence, cited}"
      contains: "def classify"
    - path: "apps/api/app/services/defects/fingerprint.py"
      provides: "Pure normalize(msg) + fingerprint(cls, msg, flow_id, step)"
      contains: "def fingerprint"
    - path: "apps/api/app/models/defects.py"
      provides: "Classification + Defect ORM models"
      contains: "class Defect"
    - path: "apps/api/alembic/versions/0009_defects.py"
      provides: "classifications + defects tables + test_results.error_text"
      contains: "down_revision"
  key_links:
    - from: "apps/api/app/services/worker/job.py"
      to: "test_results.error_text"
      via: "TestResult(... error_text=last_output)"
      pattern: "error_text="
    - from: "apps/api/app/services/defects/evidence.py"
      to: "TestResult.error_text + HealAudit + TestArtifact"
      via: "read joins by run_id+flow_id"
      pattern: "error_text"
---

<objective>
Lay the evidence + decision foundation for defect intelligence: persist the error text the classifier needs (closing the Phase-7 persistence gap), add the defect/classification data model + migration 0009, and build the PURE deterministic 3-way classifier + 0-100 confidence + fingerprint + infra-health signal over the evidence joins. This is the first vertical slice: after it, a fixture evidence dict deterministically produces a cited classification + confidence + fingerprint, and a real product-failure run now stores its error text. No LLM is ever on the decision path.

Purpose: DEF-01 (3-way class + 0-100 confidence) and DEF-02 (classification cites error type / DOM diff / healing history / infra health, runs after the Phase-7 retry) require a pure, keyless, calibratable decision module AND the persisted error text it reads — neither exists today.
Output: migration 0009, the `error_text` column + job.py persist edit, the `defects.py` models, the Jira/defect settings block, and the pure `services/defects/` package (classifier, fingerprint, infra_health, evidence) with full unit coverage + the NO-LLM grep gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-defect-intelligence-jira-agent/09-CONTEXT.md
@.planning/phases/09-defect-intelligence-jira-agent/09-RESEARCH.md
@.planning/phases/09-defect-intelligence-jira-agent/09-PATTERNS.md
@.planning/phases/09-defect-intelligence-jira-agent/09-VALIDATION.md

<interfaces>
<!-- Key existing contracts the executor must read + clone. No codebase exploration needed. -->

From apps/api/app/services/kg/risk.py (THE shape to clone for classifier.py — pure frozen-weights clamped decision):
- module docstring asserts "PURE ... NEVER LLM JUDGMENT ... stdlib-only (dataclasses) ... weights are a STARTING POINT"
- @dataclass(frozen=True) class RiskWeights with int field defaults; DEFAULT_WEIGHTS = RiskWeights()
- def risk_score(signals: dict, w: RiskWeights = DEFAULT_WEIGHTS) -> int: uses signals.get(...) defaulting; returns max(0, min(100, raw))

From apps/api/app/models/execution_history.py (the TestResult to extend + the model style to clone):
- class TestResult: run_id String(64) index=True, flow_id String(255) index=True, verdict String(16), attempts Integer, exit_codes JSON, duration_ms Integer nullable, created_at server_default func.now()
- imports: from sqlalchemy import JSON, DateTime, Integer, String, func ; from sqlalchemy.orm import Mapped, mapped_column ; from app.db.base import Base
- NOTE: there is NO error_text column today — Task 1 adds it.

From apps/api/app/services/worker/job.py (the persistence gap — lines ~165 / ~199-208):
- result = await _run_spec_once(spec, base_url=base_url, extra_args=capture_args)  # result["output"] is the tail-capped combined stdout/stderr
- the loop appends exit codes; `result` holds the LAST attempt's dict after the loop
- db.add(TestResult(run_id=..., flow_id=..., verdict=verdict["verdict"], attempts=verdict["attempts"], exit_codes=exit_codes, duration_ms=duration_ms))  # error_text NOT persisted today
- the file is LLM-import-pure and the test_no_llm_in_worker gate scans it — the edit must add NO new imports.

From apps/api/app/services/worker/classifier.py (the retry/flaky classifier — the DEF-02 INPUT, do NOT replace):
- classify_retry(exit_codes) -> {verdict, attempts, passed, exit_codes}; reconcile_verdict(base, journal_events) -> verdict
- product-failure feed: reconcile_verdict maps the all-fail / fail_as_defect path to verdict == "product_failure"

From apps/api/app/models/heal_audit.py (evidence join + model style):
- HealAudit has run_id String(64) index, flow_id String(255) index, before_chain/after_chain JSON(none_as_null=True), confidence Float, outcome String(16), reviewed_outcome String(16) nullable, element_key, created_at

From apps/api/app/core/config.py (extend in place — the ci_token + heal_high_threshold precedent):
- optional never-log secret pattern: `anthropic_api_key: str | None = None  # env ANTHROPIC_API_KEY` ; `ci_token: str | None = None`
- config-tunable threshold with tuning note: `heal_high_threshold: float = 0.15  # env HEAL_HIGH_THRESHOLD — tuned by the QUAL-02 harness`
- module-level `settings = Settings()` singleton

From apps/api/tests/unit/test_no_llm_in_worker.py (the grep gate to clone, retarget at classifier.py):
- _FORBIDDEN = [re.compile(r"\binit_chat_model\b"), re.compile(r"\bllm_gateway\b"), re.compile(r"\bfrom\s+langchain\b"), re.compile(r"\bfrom\s+langgraph\b"), re.compile(r"\bfrom\s+app\.services\.explorer\b"), ...]
- _strip_comment_lines(text) drops full-line comments before scanning; offenders-walk asserts none

From apps/api/alembic/versions/0008_heal_audit.py (migration chain to clone):
- revision/down_revision string vars; down_revision = '0007' (0009 chains down_revision = '0008'); op.create_table + op.create_index in upgrade(); reverse-order drops in downgrade()
- migrations live in apps/api/alembic/versions/ (NOT app/alembic)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0009 + error_text persistence + Jira/defect settings</name>
  <files>apps/api/alembic/versions/0009_defects.py, apps/api/app/models/execution_history.py, apps/api/app/models/defects.py, apps/api/app/services/worker/job.py, apps/api/app/core/config.py, apps/api/tests/functional/test_migration_0009.py</files>
  <read_first>
    - apps/api/alembic/versions/0008_heal_audit.py (the revision/down_revision chain + create_table/create_index + reverse downgrade — clone)
    - apps/api/app/models/execution_history.py (TestResult — add error_text after exit_codes; the Mapped/mapped_column/String/JSON style)
    - apps/api/app/models/heal_audit.py (the Classification/Defect model style: String(16) vocab, JSON(none_as_null=True) evidence, run_id/flow_id index, server_default func.now())
    - apps/api/app/services/worker/job.py (lines ~160-213 — the retry loop holds the last `result`; persist result["output"] as error_text; ADD NO IMPORTS — the no-llm-in-worker gate scans this file)
    - apps/api/app/core/config.py (the ci_token + heal_high_threshold blocks — extend in place; the `settings = Settings()` singleton)
  </read_first>
  <behavior>
    - migration 0009 (down_revision '0008'): upgrade() creates `classifications` (id PK, run_id String(64) index, flow_id String(255) index, classification String(16), confidence Integer, evidence JSON, created_at) and `defects` (id PK, run_id String(64) index, flow_id String(255) index, classification String(16), confidence Integer, fingerprint String(64) index, jira_label String(64), jira_key String(32) nullable, status String(16) server_default 'draft', created_at, updated_at) and op.add_column('test_results', error_text Text nullable). downgrade() drops in reverse (indexes, then defects, then classifications, then the test_results.error_text column).
    - `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips clean.
    - TestResult gains `error_text: Mapped[str | None] = mapped_column(Text, nullable=True)`.
    - job.py persists the LAST attempt's `result["output"]` (captured already in hand) as `error_text=...` on the TestResult; aborted path leaves error_text None; NO new imports added (the no-llm-in-worker gate stays green).
    - config.py gains: jira_url / jira_email (str | None = None), jira_api_token (str | None = None, never logged), jira_project_key (str | None = None), jira_autonomous_enabled (bool = False — OFF by default), jira_confidence_threshold (int starting point, e.g. 70, env JIRA_CONFIDENCE_THRESHOLD, with the heal_high_threshold-style tuning note that the QUAL-03 harness calibrates it), jira_max_tickets_per_run (int, e.g. 25).
  </behavior>
  <action>Clone 0008_heal_audit.py to create apps/api/alembic/versions/0009_defects.py with revision '0009' and down_revision '0008'. In upgrade() create the `classifications` and `defects` tables and op.add_column the `test_results.error_text` Text column (RESEARCH Pitfall 1 / Open-Q1: error_text on test_results for the direct classifier read, evidence JSON on classifications for the full cited snapshot); add indexes on defects.run_id, defects.flow_id, defects.fingerprint. In downgrade() reverse every op (drop indexes, drop defects, drop classifications, drop the error_text column). Add `error_text: Mapped[str | None] = mapped_column(Text, nullable=True)` to TestResult in execution_history.py (import Text from sqlalchemy). Create app/models/defects.py with the Classification + Defect ORM classes mirroring heal_audit.py exactly (String(16) for classification/status vocab, Integer confidence 0-100, JSON evidence, String(64) fingerprint index, String(32) nullable jira_key, status server_default 'draft', run_id/flow_id FKs as plain indexed String columns like execution_history — the Defect row IS the test↔flow↔execution traceability link per JIRA-04, captured here for the Plan-04 pipeline). Edit job.py: after the retry loop, pass `error_text=result["output"]` (the last attempt's output already in hand at line ~165) into the TestResult(...) constructor — add NO imports. Extend config.py in place with the Jira/defect settings block per D-04/D-05 using the ci_token (never-log) + heal_high_threshold (tuning-note) precedents; jira_autonomous_enabled defaults False, jira_confidence_threshold carries the note that the QUAL-03 harness calibrates it. Write tests/functional/test_migration_0009.py asserting the up/down/up round-trip and that test_results.error_text exists after upgrade.</action>
  <verify>
    <automated>cd apps/api && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
    <automated>cd apps/api && uv run python -m pytest tests/unit/test_no_llm_in_worker.py -q</automated>
  </verify>
  <done>Migration 0009 round-trips; TestResult has error_text; job.py persists it with no new imports (no-llm-in-worker gate green); defects.py models exist; config.py carries the jira_* settings (autonomous OFF by default).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure classifier + fingerprint + infra-health signal + NO-LLM gate</name>
  <files>apps/api/app/services/defects/__init__.py, apps/api/app/services/defects/classifier.py, apps/api/app/services/defects/fingerprint.py, apps/api/app/services/defects/infra_health.py, apps/api/tests/unit/test_classifier.py, apps/api/tests/unit/test_fingerprint.py, apps/api/tests/unit/test_no_llm_in_classifier.py</files>
  <read_first>
    - apps/api/app/services/kg/risk.py (clone the docstring purity contract + @dataclass(frozen=True) weights + signals.get defaulting + max(0,min(100,raw)) clamp byte-faithfully)
    - apps/api/app/services/healing/confidence.py (the gate-FIRST precedence discipline — apply a class-rule precedence ordering the same way heal_outcome applies the uniqueness gate before any band)
    - apps/api/app/services/explorer/fingerprint.py (the hashlib + compiled-regex normalize discipline — _NUM_RE.sub + hashlib digest)
    - apps/api/tests/unit/test_no_llm_in_worker.py (clone _FORBIDDEN + _strip_comment_lines + the offenders walk, retargeted at app/services/defects/)
  </read_first>
  <behavior>
    - classify(evidence, w=DEFAULT_WEIGHTS) returns {classification, confidence, cited}. Class rules (precedence: infrastructure first, product_defect last/default — RESEARCH Pattern 1 taxonomy): browser-crash / Target closed / net::ERR_ / ERR_CONNECTION_REFUSED / DNS / timeout-never-loaded / infra_health=='down' -> infrastructure; locator-not-found after heal_outcome in {fail_as_defect, quarantine} / un-healed locator drift / test-data mismatch -> automation; assertion failure on a loaded page / functional/validation/API 4xx-5xx / SEED_BUG signature -> product_defect.
    - confidence is a clamped 0-100 weighted blend: strong_class_signal (e.g. 60) + corroborating_signal (e.g. 20) per additional same-class signal - weak_or_conflicting (e.g. 15) per cross-class signal; max(0, min(100, raw)). Weights are a frozen @dataclass STARTING POINT (the kg/risk + heal-band precedent — the QUAL-03 harness tunes them in Plan 02), never hardcoded literals in callers.
    - normalize(msg) strips uuids/ISO-timestamps/hex/digits to placeholders + collapses whitespace; fingerprint(cls, msg, flow_id, step) = hashlib.sha1(f"{cls}|{normalize(msg)}|{flow_id}|{step}".encode()).hexdigest()[:16] — deterministic (two messages differing only in numbers/ids/timestamps produce the SAME fingerprint).
    - infra_health signal is the PURE error-pattern source (RESEARCH Open-Q2 option b): connection-refused/DNS/timeout patterns over the error text -> 'down', otherwise 'up'/'unknown'; no live Docker probe (deferred to Phase 11).
    - the classifier/fingerprint/infra_health modules import NOTHING from init_chat_model/llm_gateway/langchain/langgraph/explorer/the DB session — stdlib + dataclasses only; the grep gate asserts it.
  </behavior>
  <action>Create app/services/defects/__init__.py and classifier.py cloning kg/risk.py byte-faithfully: the purity docstring, ClassifierWeights @dataclass(frozen=True) with the 60/20/-15 starting weights, DEFAULT_WEIGHTS, and a pure classify(evidence, w=DEFAULT_WEIGHTS) -> dict with a _classify_rules(evidence, cited) precedence body implementing the RESEARCH Pattern 1 taxonomy table and a clamped weighted confidence from _corroboration/_conflict counts. Create fingerprint.py with the RESEARCH Pattern 5 _UUID/_TS/_HEX/_NUM compiled regexes + normalize() + fingerprint() returning sha1[:16] (clone the explorer/fingerprint.py hashlib+re discipline; stdlib only, NO new package). Create infra_health.py as a pure error-pattern signal function. Write tests/unit/test_classifier.py (table-driven: each taxonomy row -> the expected class + a confidence band, on fixture evidence dicts, keyless), tests/unit/test_fingerprint.py (normalize idempotence + numbers/ids/timestamps collapse to one fingerprint + distinct flows/steps differ), and tests/unit/test_no_llm_in_classifier.py cloning test_no_llm_in_worker.py with _APP_ROOT/app/services/defects as the scan root + the same _FORBIDDEN list. Do NOT inline the QUAL-03 tuning — weights stay starting points; Plan 02 calibrates.</action>
  <verify>
    <automated>cd apps/api && uv run python -m pytest tests/unit/test_classifier.py tests/unit/test_fingerprint.py tests/unit/test_no_llm_in_classifier.py -q</automated>
  </verify>
  <done>classify() returns the right class + a 0-100 confidence + cited signals for every taxonomy fixture; fingerprint is stable under number/id/timestamp variation; the NO-LLM grep gate is green over the defects package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Evidence gather (read joins) + classifier-over-evidence wiring</name>
  <files>apps/api/app/services/defects/evidence.py, apps/api/tests/unit/test_classifier_evidence.py</files>
  <read_first>
    - apps/api/app/services/worker/job.py (lines ~178-213 — the FRESH SessionLocal read/commit ownership + the run_id+flow_id thread)
    - apps/api/app/models/execution_history.py (TestResult.error_text [new], TestArtifact.path; the run_id/flow_id index)
    - apps/api/app/models/heal_audit.py (HealAudit before_chain/after_chain/outcome — the DOM-diff + healing-history evidence)
    - apps/api/app/services/defects/classifier.py (the evidence dict keys classify() consumes: error_text, page_loaded, heal_outcome, infra_health, flow_id, step)
  </read_first>
  <behavior>
    - gather_evidence(db, run_id, flow_id) reads the TestResult.error_text + the HealAudit row(s) (DOM before/after + outcome = healing history) + the infra_health signal (from infra_health over the error text) + the TestArtifact paths, all threaded by run_id+flow_id, and returns the evidence dict classify() consumes plus a `cited` snapshot suitable for the classifications.evidence JSON.
    - DEF-02: classification runs AFTER the Phase-7 retry — the pipeline (Plan 04) calls gather_evidence only for verdict == 'product_failure'; this task provides the join + the classify-over-evidence helper, unit-tested against fixture/seeded rows (no real run needed).
    - the evidence module reads via the ORM (parameterized), uses a passed-in session, imports no LLM path.
  </behavior>
  <action>Create app/services/defects/evidence.py with an async gather_evidence(db, run_id, flow_id) that ORM-reads TestResult (incl. error_text), HealAudit (before_chain/after_chain/outcome), and TestArtifact rows by run_id+flow_id, derives the infra_health signal via infra_health.py, and assembles both the classify() input dict and the full cited-evidence snapshot for classifications.evidence. Add a thin classify_failure(db, run_id, flow_id) helper that gathers evidence then calls classifier.classify(...) and returns {classification, confidence, cited, evidence, fingerprint} (fingerprint built from the class + error_text + flow_id + failing step). Write tests/unit/test_classifier_evidence.py driving gather_evidence over seeded TestResult/HealAudit/TestArtifact rows (in-memory/SQLite or the functional DB fixture as the repo pattern dictates) and asserting the cited evidence + the resulting class/confidence for a seeded product-failure and an un-healed-locator case. evidence.py imports no LLM/gateway path.</action>
  <verify>
    <automated>cd apps/api && uv run python -m pytest tests/unit/test_classifier_evidence.py -q</automated>
    <automated>cd apps/api && uv run python -m pytest -m "not live_llm and not e2e and not graph and not functional" -q</automated>
  </verify>
  <done>gather_evidence assembles the cited evidence dict from the test_results/heal_audit/test_artifacts joins; classify_failure returns class + confidence + fingerprint + evidence over seeded rows; the deterministic suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker subprocess output -> error_text column | the captured stdout/stderr (untrusted target page text may appear in it) is persisted + later read by the classifier |
| stored error_text -> fingerprint/classifier | a malicious error string must not break the deterministic decision or the hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | classifier.py decision path | mitigate | pure stdlib, no eval/exec; evidence values are read as data only; the NO-LLM grep gate (test_no_llm_in_classifier) asserts no gateway import sneaks the decision onto an LLM |
| T-09-02 | Tampering | fingerprint over a hostile error_text | mitigate | normalize() + sha1 over a fixed template — a hostile string only changes the hex digest, never escapes into SQL/JQL (the label is a server-built `fp-<hash>` consumed in Plan 04) |
| T-09-03 | Information Disclosure | jira_api_token in config/logs | mitigate | jira_api_token is str|None=None (boot-safe) and never logged (the ci_token / anthropic_api_key SENSITIVE-regex precedent); no structlog event carries it |
| T-09-04 | Tampering | migration 0009 reversibility | mitigate | downgrade() reverses every op; the round-trip is an automated gate (test_migration_0009 + the alembic up/down/up command) |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | NO package install in this plan (stdlib + existing deps only); the gated atlassian-python-api install is isolated to Plan 03 behind its checkpoint |
</threat_model>

<verification>
- `cd apps/api && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` — 0009 reversible.
- `cd apps/api && uv run python -m pytest -m "not live_llm and not e2e and not graph and not functional" -q` — the pure classifier/fingerprint/evidence + grep gate green.
- `cd apps/api && uv run python -m pytest tests/unit/test_no_llm_in_worker.py tests/unit/test_no_llm_in_classifier.py -q` — both NO-LLM gates green.
</verification>

<success_criteria>
- DEF-01: classify(evidence) returns one of {infrastructure, automation, product_defect} + a 0-100 confidence citing signals, on every taxonomy fixture.
- DEF-02: gather_evidence joins error type (error_text) + DOM diff + healing history (heal_audit) + infra health; classification consumes it post-retry.
- The classifier/fingerprint/infra_health/evidence modules import no LLM/gateway path (grep gate green).
- Migration 0009 round-trips; job.py persists error_text with no new imports.
</success_criteria>

<output>
Create `.planning/phases/09-defect-intelligence-jira-agent/09-01-SUMMARY.md` when done.
</output>
