---
phase: 09-defect-intelligence-jira-agent
plan: 02
type: execute
wave: 2
depends_on: [09-01]
autonomous: true
requirements: [DEF-03, QUAL-03]
files_modified:
  - apps/api/tests/functional/test_classifier_accuracy.py
  - apps/api/app/core/config.py

must_haves:
  truths:
    - "A keyless labeled failure set spans all three classes: Product = SEED_BUG build, Automation = un-healed BREAK_REMOVE mutation, Infrastructure = a dead-port/forced-timeout fault"
    - "The classifier scores >=85% accuracy against the hand-labels"
    - "The calibrated jira_confidence_threshold is derived from the measured per-class confidences and asserted against the SHIPPED settings default (never a test-local literal)"
    - "The harness skips cleanly when the mutation/seed builds are down, and runs with neo4j OFF"
  artifacts:
    - path: "apps/api/tests/functional/test_classifier_accuracy.py"
      provides: "QUAL-03 keyless accuracy harness + threshold calibration"
      contains: "accuracy"
  key_links:
    - from: "apps/api/tests/functional/test_classifier_accuracy.py"
      to: "settings.jira_confidence_threshold"
      via: "assert against the shipped default, not a literal"
      pattern: "jira_confidence_threshold"
    - from: "apps/api/tests/functional/test_classifier_accuracy.py"
      to: "app.services.defects.classifier.classify"
      via: "the production pure classifier over real-run evidence"
      pattern: "classify"
---

<objective>
Prove the classifier with a keyless, deterministic accuracy harness and calibrate the autonomous-filing threshold from the measured confidences — the trust gate that must pass before any autonomous Jira filing is unlocked. Generate KNOWN-class failures keylessly (Product = the SEED_BUG build, Automation = an un-healed BREAK_REMOVE locator mutation, Infrastructure = a NEW dead-port/forced-timeout fault), run the production classifier over each, assert accuracy >= 85%, and derive + store the calibrated `jira_confidence_threshold` proving against the shipped settings default — exactly the way 08-04 tuned the heal band.

Purpose: DEF-03 (accuracy measured against a hand-labeled set before autonomous filing) and QUAL-03 (labeled set measures accuracy >85% AND calibrates the Jira confidence threshold).
Output: the `test_classifier_accuracy.py` harness (clone of the QUAL-02 mutation harness) + the calibrated `jira_confidence_threshold` settled into config and asserted against the shipped default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-defect-intelligence-jira-agent/09-RESEARCH.md
@.planning/phases/09-defect-intelligence-jira-agent/09-PATTERNS.md
@.planning/phases/09-defect-intelligence-jira-agent/09-VALIDATION.md
@.planning/phases/08-self-healing-engine/08-04-SUMMARY.md

<interfaces>
<!-- The harness to clone + the production classifier it measures. -->

From apps/api/tests/functional/test_healing_mutations.py (THE harness shape to clone — QUAL-02, keyless, deterministic):
- pytestmark = [pytest.mark.functional]
- from app.core.config import settings as _settings ; _MUTATION_HIGH = str(_settings.heal_high_threshold)  # prove the SHIPPED default, not a literal
- _port_open(url) -> bool (cheap TCP up-check via socket.create_connection timeout 2)
- _require_targets() -> pytest.skip("...not up...") when any build is down
- the inner planted-spec subprocess uses ["uv","run","python","-m","pytest", spec, "-q"] (Windows AppControl blocks the pytest.exe shim — os error 4551)
- the SauceDemo mutation builds: SEED_BUG (port 8081), BREAK_REMOVE (port 8086), BENIGN_* (8082-8085), BREAK_DUPLICATE (8087)

From apps/api/app/services/defects/classifier.py (Plan 01 — the production pure classifier; do NOT re-tune by editing the module, tune via the frozen weights / the settings threshold):
- classify(evidence, w=DEFAULT_WEIGHTS) -> {classification, confidence, cited}

From apps/api/app/services/defects/evidence.py (Plan 01 — the same evidence join the pipeline uses):
- async gather_evidence(db, run_id, flow_id) / classify_failure(db, run_id, flow_id)

From apps/api/app/core/config.py (Plan 01 added jira_confidence_threshold — the calibrated value the harness proves against, mirroring _MUTATION_HIGH = str(_settings.heal_high_threshold)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead-port infra-fault generator + the three-class labeled set</name>
  <files>apps/api/tests/functional/test_classifier_accuracy.py</files>
  <read_first>
    - apps/api/tests/functional/test_healing_mutations.py (clone pytestmark/functional, _port_open, _require_targets, the inner `uv run python -m pytest` runner, the SEED_BUG + BREAK_REMOVE build wiring)
    - apps/api/tests/functional/test_seeded_bug.py (the SEED_BUG run-against pattern + the skip-when-down discipline)
    - apps/api/app/services/defects/evidence.py + classifier.py (the production decision path the harness measures)
    - .planning/phases/08-self-healing-engine/08-04-SUMMARY.md (the measured-confidence-then-calibrate methodology + the Windows runner deviation)
  </read_first>
  <behavior>
    - The labeled set generates known-class failures keylessly: ("product_defect", run vs SEED_BUG URL 8081), ("automation", run vs BREAK_REMOVE URL 8086 un-healed), ("infrastructure", a NEW dead-port / forced-timeout fault — a non-listening port and a sub-second timeout, no Docker build needed: the _port_open inverse).
    - Multiple instances per class for a meaningful denominator (RESEARCH Pattern 3).
    - _require_targets() skips cleanly if the SEED_BUG/mutation builds are down; neo4j OFF in the run phase (3GB cap); no provider keys read.
    - The inner spec subprocess uses `uv run python -m pytest` (the 08-04 Windows AppControl deviation).
  </behavior>
  <action>Create apps/api/tests/functional/test_classifier_accuracy.py by cloning test_healing_mutations.py's harness scaffolding: pytestmark functional, _port_open, _require_targets, and the inner `["uv","run","python","-m","pytest", ...]` runner. Build the three-class labeled set: drive a real spec run against the SEED_BUG build (8081 -> product_defect), against the BREAK_REMOVE build (8086, un-healed -> automation), and add the NET-NEW dead-port/forced-timeout infra-fault generator (point a run at a non-listening port + a sub-second timeout -> infrastructure; the _port_open inverse, RESEARCH A6). For each case, gather the evidence via the Plan-01 gather_evidence over the resulting test_results/heal_audit rows (or assemble the evidence dict the same way the pipeline does) so the harness exercises the PRODUCTION classifier, not a stub. Use several instances per class. Skip cleanly when builds are down.</action>
  <verify>
    <automated>cd apps/api && uv run python -m pytest tests/functional/test_classifier_accuracy.py -m functional -q --collect-only</automated>
  </verify>
  <done>The harness collects, defines all three known-class generators (incl. the dead-port infra fault), exercises the production classifier over real-run evidence, and skips cleanly when builds are down.</done>
</task>

<task type="auto">
  <name>Task 2: Accuracy assertion >=85% + threshold calibration vs the shipped default</name>
  <files>apps/api/tests/functional/test_classifier_accuracy.py, apps/api/app/core/config.py</files>
  <read_first>
    - apps/api/tests/functional/test_healing_mutations.py (lines ~89, 303-311 — `_MUTATION_HIGH = str(_settings.heal_high_threshold)` + the rate assertions; the calibrate-against-shipped-default discipline)
    - .planning/phases/08-self-healing-engine/08-04-SUMMARY.md (the measured-confidence matrix -> separation-window -> retune-the-config-default flow; only the config default is tuned, never the pure module weights)
    - apps/api/app/core/config.py (jira_confidence_threshold added in Plan 01)
  </read_first>
  <behavior>
    - The harness computes accuracy = correct / total over the labeled set and asserts accuracy >= 0.85.
    - It collects per-class confidences and derives the separating threshold for autonomous filing (the product_defect distribution vs the misclassified/below tail), then asserts the calibrated value against `settings.jira_confidence_threshold` (the shipped default — never a test-local literal, the line-89 discipline).
    - If the measured separation requires it, the config default jira_confidence_threshold is retuned in config.py to sit in the empirical window (the 08-04 0.85->0.15 precedent) — only the config default + the harness proof move; classifier.py's frozen weights are not silently re-cut unless accuracy < 85% forces a documented taxonomy/weight fix.
  </behavior>
  <action>Extend test_classifier_accuracy.py to compute accuracy over the labeled set and assert >= 0.85, accumulate per-class confidences, derive the separating threshold, and assert it against settings.jira_confidence_threshold via `_THRESHOLD = settings.jira_confidence_threshold` (mirroring `_MUTATION_HIGH = str(_settings.heal_high_threshold)`) — proving the shipped config default, not a literal. Print/emit the per-class confidence distribution for auditability. If the measured window demands it, retune the jira_confidence_threshold DEFAULT in config.py into the empirical separation window (the 08-04 methodology) and re-run; document the measured matrix + the chosen value in the SUMMARY. Do NOT edit classifier.py's weights unless accuracy < 85% forces it — and if so, document the taxonomy/weight change explicitly (it is a decision, not a silent tune).</action>
  <verify>
    <automated>cd apps/api && uv run python -m pytest tests/functional/test_classifier_accuracy.py -m functional -q</automated>
  </verify>
  <done>accuracy >= 0.85 on the labeled set; the calibrated jira_confidence_threshold is asserted against the shipped config default; the measured per-class confidence matrix is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker mutation/seed builds -> the classifier under test | known-class failures cross into the production decision path; the harness must not leak keys or start neo4j |
| measured confidences -> the shipped config threshold | the calibration must prove the default the runtime uses, not a divergent test literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | the calibrated threshold drifting from the runtime value | mitigate | the harness asserts against `settings.jira_confidence_threshold` (the shipped default) — the `_MUTATION_HIGH = str(_settings...)` precedent; config can never silently diverge from the proof |
| T-09-06 | Denial of Service | OOM under the 3GB WSL cap during the run phase | accept (mitigated by sequencing) | neo4j OFF in the run phase (the classifier needs no graph); only the static nginx targets + a Chromium subprocess run — the Phase-7/8 sequencing. Exact `docker stats` fit is a Manual-Only observation |
| T-09-07 | Information Disclosure | provider keys / Jira token during the harness | mitigate | the harness is keyless (no provider keys read, no real Jira); the classifier is pure |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | NO package install in this plan; reuses the shipped SauceDemo builds + stdlib socket for the dead-port fault |
</threat_model>

<verification>
- `cd apps/api && uv run python -m pytest tests/functional/test_classifier_accuracy.py -m functional -q` (mutation/seed builds up, neo4j OFF) -> accuracy >= 0.85, threshold asserted against the shipped default.
- Skips cleanly (not fails) when builds are down: `_require_targets()` -> pytest.skip.
</verification>

<success_criteria>
- DEF-03 / QUAL-03: classification accuracy >= 85% measured on the keyless three-class labeled set; the jira_confidence_threshold calibrated from the measured confidences and asserted against the shipped settings default.
- Keyless + neo4j-OFF; the Infrastructure label generated by a NEW dead-port/forced-timeout fault.
</success_criteria>

<output>
Create `.planning/phases/09-defect-intelligence-jira-agent/09-02-SUMMARY.md` when done. Record the measured per-class confidence matrix + the chosen threshold (the 08-04 calibration precedent).
</output>
