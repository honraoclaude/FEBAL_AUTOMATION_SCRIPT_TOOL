---
phase: 09-defect-intelligence-jira-agent
plan: 05
type: execute
wave: 4
depends_on: [09-04]
autonomous: true
requirements: [JIRA-02, JIRA-04]
files_modified:
  - apps/web/lib/api/defects.ts
  - apps/web/app/(dashboard)/defects/page.tsx
  - apps/web/app/(dashboard)/defects/[id]/page.tsx
  - apps/web/components/defects/calibration-panel.tsx
  - apps/web/components/defects/class-badge.tsx
  - apps/web/components/defects/confidence-meter.tsx
  - apps/web/components/app-sidebar.tsx
  - apps/web/tests/e2e/defects.spec.ts

must_haves:
  truths:
    - "The Defects section ships EXACTLY to 09-UI-SPEC: a review-queue list (status+class filters, class badge word+icon+hue, 0-100 confidence meter, status, source refs, Jira key) + a detail (proposed Jira issue + cited evidence + auth-gated artifact links + apply create-vs-update + reject confirm) + a READ-ONLY calibration panel"
    - "ZERO new shadcn blocks and ZERO new frontend dependencies — `git diff apps/web/package.json` is clean; recharts is NOT used (the confidence/calibration meters are token-styled native progressbars)"
    - "Confidence bands and the calibration threshold render from the server payload (settings.jira_confidence_threshold), never a client literal; class/status/Jira-key/accuracy render strictly from the server (no fabrication)"
    - "Apply shows an HONEST pending→result state (never fake-instant success); the create-vs-update label comes from the server dedup result; reject is a destructive confirm"
    - "Every artifact link is an auth-gated URL built from run-relative basenames (never a raw filesystem path); no secret/token is rendered client-side; the route is auth-guarded"
    - "One 'Defects' sidebar item is appended after 'Executions'; the e2e suite runs against a MOCKED API (no real Jira, no provider keys, all states mockable)"
  artifacts:
    - path: "apps/web/lib/api/defects.ts"
      provides: "zod client mirroring schemas/defect.py (summary/detail/calibration + apply/reject)"
      contains: "defectDetailSchema"
    - path: "apps/web/app/(dashboard)/defects/page.tsx"
      provides: "review-queue list + filters + calibration panel"
      contains: "Defects"
    - path: "apps/web/app/(dashboard)/defects/[id]/page.tsx"
      provides: "defect detail/review (proposed issue + evidence + attachments + apply/reject)"
      contains: "Apply"
  key_links:
    - from: "apps/web/lib/api/defects.ts"
      to: "/api/defects?status=&class= + /{id} + /calibration + /{id}/apply + /{id}/reject"
      via: "the same-origin api client (httpOnly cookie)"
      pattern: "/api/defects"
    - from: "apps/web/components/defects/confidence-meter.tsx"
      to: "the server confidence_threshold"
      via: "band edge from the payload, never a client literal"
      pattern: "confidence_threshold"
    - from: "apps/web/components/app-sidebar.tsx"
      to: "NAV_ITEMS"
      via: "the Defects item appended after Executions"
      pattern: "Defects"
---

<objective>
Build the MINIMAL Defects review-queue UI EXACTLY to 09-UI-SPEC (D-06): the human-in-the-loop surface D-04's autonomy gate requires. A review-queue LIST (status + class filter segments, a drafts-first table with the class badge [word + icon + hue], the 0-100 confidence meter, the source test/flow/execution refs, the status, the optional Jira key) atop a READ-ONLY calibration panel (accuracy vs the ≥85% gate, draft precision vs the ≥90% gate, the calibrated threshold, the autonomy-flag state); and a DETAIL/review view (the proposed Jira issue + the cited evidence + auth-gated attachment links + Apply [honest pending→result, create-vs-update per the server dedup] + Reject [destructive confirm]). All over the Plan-04 `/api/defects` API, all honest (Jira-not-configured / empty / error-with-path-forward states), with ZERO new shadcn and ZERO new frontend deps.

Purpose: JIRA-02 (the review-queue surface is a named success-criterion behavior, and D-04's human gate needs this surface to review accuracy/precision before flipping the flag) and JIRA-04 (the detail surfaces the test↔flow↔execution traceability refs the Defect row carries). The backend (Plan 04) is the source of truth; this plan renders it faithfully and never fabricates.
Output: `lib/api/defects.ts` (the zod client), the `app/(dashboard)/defects/` pages, the `components/defects/` pieces (calibration panel, class badge, confidence meter), the sidebar append, and the mocked-API e2e suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-defect-intelligence-jira-agent/09-UI-SPEC.md
@.planning/phases/09-defect-intelligence-jira-agent/09-PATTERNS.md
@.planning/phases/09-defect-intelligence-jira-agent/09-VALIDATION.md
@.planning/phases/09-defect-intelligence-jira-agent/09-04-PLAN.md

<interfaces>
<!-- The Plan-04 API payloads + the shipped frontend analogs. Build to these — no codebase spelunking. -->

From apps/api/app/schemas/defect.py (Plan 04 — the payloads the zod client MUST mirror; field names are authoritative):
- DefectSummaryResponse: id, run_id, flow_id, classification (infrastructure|automation|product_defect), confidence (0-100 int), status (draft|applied|rejected), jira_key (str|None), updated_at  + the source refs (flow name / run_id / flow_id)
- DefectDetailResponse: the summary fields + the proposed issue (summary, description prose + an enriched flag, steps[], expected, actual, severity, priority) + the cited evidence (error_type, dom_diff, healing_history, infra_health) + attachment refs (run-relative basenames per kind) + fingerprint + confidence_threshold (the calibrated server value)
- CalibrationResponse: classification_accuracy (num|null), draft_precision (num|null), confidence_threshold (int), autonomous_enabled (bool)  — nullable for the not-measured state
- the apply response reports the create-vs-update dedup decision + the jira_key

From apps/api/app/routers/defects.py (Plan 04 — the endpoints, all get_current_user-gated):
- GET /api/defects?status=&class=  · GET /api/defects/{id}  · GET /api/defects/calibration  · POST /api/defects/{id}/apply  · POST /api/defects/{id}/reject
- an unauth request → 401 (the client wrapper redirects to /login)
- the artifact route reused for attachment links: /api/executions/{run_id}/artifacts/{flow_id}/{kind} (the Phase-7 run_id-derived, containment-guarded route — NEVER a raw path)

From apps/web/lib/api/scenarios.ts (THE zod-client analog to clone — zod mirrors Pydantic, api.get/api.post, field names aligned to the schema):
- export const scenarioSummarySchema = z.object({ id: z.number().int(), run_id: z.string(), flow_id: z.string(), status: z.string(), updated_at: z.string(), ... })
- export const scenarioDetailSchema = scenarioSummarySchema.extend({ ... })
- export async function listScenarios(status) { return schema.parse(await api.get(`/api/scenarios?status=${encodeURIComponent(status)}`)); }
- export async function approveScenario(id) { return schema.parse(await api.post(`/api/scenarios/${id}/approve`)); }

From apps/web/lib/api/client.ts (the same-origin /api/* wrapper — the httpOnly cookie rides automatically; 401 → refresh → /login; ApiError carries .status for the 404 branch). Reuse `api.get` / `api.post`; do NOT handle tokens.

From apps/web/app/(dashboard)/scenarios/page.tsx (the LIST analog — clone verbatim):
- the accent-underlined `<button>` filter segments (styled-native, NOT a tabs block) for `?status=` — replicate for BOTH ?status= AND ?class=
- useQuery({ retry: false }), the isEmpty derivation, the per-filter empty blocks + the inline error state (never a toast)

From apps/web/app/(dashboard)/scenarios/[id]/page.tsx (the DETAIL analog — clone verbatim):
- the breadcrumb + header-with-badges + cards + action-bar + confirm Dialog + mutations-with-invalidate
- invalidate() = Promise.all([invalidateQueries(["scenarios","detail",id]), invalidateQueries(["scenarios"])]); useMutation onSuccess → await invalidate() + toast.success(...)  (NO optimistic update — server-authoritative)
- the `ApiError && status === 404` branch + the skeleton loading blocks

From apps/web/components/app-sidebar.tsx (the one-line append — the file itself):
- type NavItem = { icon: LucideIcon; label: string; href: string }; const NAV_ITEMS: NavItem[] = [ ... { icon: PlayCircle, label: "Executions", href: "/executions" } ]
- append `{ icon: Bug, label: "Defects", href: "/defects" }` AFTER Executions; import Bug from lucide-react; active via the existing pathname.startsWith(item.href)

09-UI-SPEC.md is the EXACT contract: the Color section's class→token map (Infrastructure=neutral/ServerCog, Automation=quarantine-amber/Wrench, Product defect=fail-red/Bug — word + icon ALWAYS, never color alone), the confidence band (≥ threshold green, below amber — edge from the server confidence_threshold), the status map (Draft amber / Applied green / Rejected muted), the calibration gate map, every Copywriting string, and the full Screen & State Inventory (every state ships).

NEXT.JS NOTE (apps/web/AGENTS.md): this is a modified Next.js — read the relevant guide in node_modules/next/dist/docs/ before writing route/page code; honor deprecation notices. The dashboard route group `(dashboard)` + the scenarios pages are the shipped pattern to mirror.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: zod defects client + the class badge + the token-styled confidence meter</name>
  <files>apps/web/lib/api/defects.ts, apps/web/components/defects/class-badge.tsx, apps/web/components/defects/confidence-meter.tsx</files>
  <read_first>
    - apps/web/lib/api/scenarios.ts (the zod-mirrors-Pydantic + api.get/api.post + encodeURIComponent + field-names-aligned discipline)
    - apps/web/lib/api/client.ts (the api wrapper + ApiError.status)
    - 09-UI-SPEC.md Color (the class→token+icon map; the confidence band edge from the SERVER confidence_threshold; WCAG 1.4.1 word+icon-not-color-alone) + Accessibility (the confidence meter role="progressbar" with aria-valuenow/min/max + the value-and-threshold accessible name)
    - apps/api/app/schemas/defect.py (Plan 04 — the exact field names the zod schemas mirror)
  </read_first>
  <behavior>
    - defects.ts mirrors schemas/defect.py: defectSummarySchema (id/run_id/flow_id/classification/confidence/status/jira_key/updated_at + source refs), defectDetailSchema = summary.extend({ proposed issue summary/description/description_enriched flag/steps[]/expected/actual/severity/priority, evidence error_type/dom_diff/healing_history/infra_health, attachments per kind, fingerprint, confidence_threshold }), calibrationSchema (accuracy/precision nullable, confidence_threshold, autonomous_enabled). Fetchers: listDefects(status, klass), defectDetail(id), calibration(), applyDefect(id), rejectDefect(id) — through `api` (the cookie rides; no token handling). field names MUST match the Pydantic schema (the scenarios.ts rule).
    - class-badge.tsx: maps classification → { word, icon, token-class } per the UI-SPEC table (infrastructure → "Infrastructure" + ServerCog + --status-neutral; automation → "Automation" + Wrench + --status-quarantine; product_defect → "Product defect" + Bug + --status-fail). ALWAYS renders the WORD + an aria-hidden icon (never color alone — WCAG 1.4.1). A plain composition over the vendored `badge` + lucide icons; no new shadcn.
    - confidence-meter.tsx: a token-styled NATIVE meter (a bg-secondary track + a fill colored green when confidence >= the passed threshold else amber — the edge is the SERVER confidence_threshold, never a client literal) + the mono numeral always present as text. role="progressbar" with aria-valuenow={confidence} aria-valuemin=0 aria-valuemax=100 + an accessible name combining the value and whether it clears the threshold. NO recharts, NO new dep (the Phase-7 styled-native progress precedent).
  </behavior>
  <action>Create apps/web/lib/api/defects.ts cloning scenarios.ts: the zod summary/detail/calibration schemas mirroring schemas/defect.py field-for-field, and listDefects(status, klass)/defectDetail(id)/calibration()/applyDefect(id)/rejectDefect(id) over the `api` client (encodeURIComponent the query params; build the list URL with both ?status= and ?class= when klass is set). Create apps/web/components/defects/class-badge.tsx — a component over the vendored `badge` + lucide (ServerCog/Wrench/Bug) mapping the three classes to their UI-SPEC word+icon+--status-* token, the icon aria-hidden, the word always rendered. Create apps/web/components/defects/confidence-meter.tsx — a token-styled native `role="progressbar"` meter (bg-secondary track, green fill ≥ threshold / amber below, the edge taken from a `threshold` prop sourced from the server confidence_threshold) + the mono numeral, with aria-valuenow/min/max + the value+threshold accessible name. ZERO new shadcn add, ZERO new frontend dep (no recharts). Confirm `git diff apps/web/package.json` is clean.</action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
    <automated>cd apps/web && git diff --exit-code package.json package-lock.json</automated>
  </verify>
  <done>defects.ts mirrors the Plan-04 schema field-for-field over the cookie-riding api client; the class badge renders word+icon+hue per UI-SPEC (never color alone); the confidence meter is a token-styled accessible native progressbar banded off the SERVER threshold; tsc clean; package.json/lock unchanged (zero new deps).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Review-queue list page + the read-only calibration panel + the sidebar item</name>
  <files>apps/web/app/(dashboard)/defects/page.tsx, apps/web/components/defects/calibration-panel.tsx, apps/web/components/app-sidebar.tsx</files>
  <read_first>
    - apps/web/app/(dashboard)/scenarios/page.tsx (clone: the accent-underlined `<button>` filter segments, useQuery({retry:false}), the isEmpty derivation, the per-filter empty blocks + the INLINE error state never a toast)
    - 09-UI-SPEC.md §0 Calibration panel + §1 Defect review queue (the two filter groups, the table columns Defect/Class/Confidence/Source/Status/Updated, the drafts-first → confidence-desc → updated-desc sort, every empty/loading/error state, the calibration tiles + the read-only autonomy-flag display + its gate captions) + the Copywriting contract (every string verbatim)
    - apps/web/lib/api/defects.ts (Task 1 — listDefects + calibration + the class-badge + confidence-meter)
    - apps/web/components/app-sidebar.tsx (the NAV_ITEMS append-after-Executions, icon Bug, href /defects, the existing startsWith active logic)
    - node_modules/next/dist/docs/ (the relevant App Router page guide — this is a modified Next.js; honor deprecations)
  </read_first>
  <behavior>
    - The list page renders, in order: the page header ("Defects" Heading); the calibration panel (atop the queue, the shared component); the two filter segment groups (status: Drafts/Applied/Rejected/All default Drafts via ?status=; class: All classes/Infrastructure/Automation/Product defect default All via ?class= — both deep-linkable, accent-underlined styled-native buttons, NOT a tabs block); the queue table (Defect [accent drill-in link → /defects/[id] + mono defect-id caption] · Class [class-badge] · Confidence [confidence-meter banded off the row's threshold] · Source ["Flow {name}" + the mono "run {run_id} · {flow_id}" refs] · Status [status badge; an applied row shows the mono Jira-key link] · Updated [mono ts]); default sort drafts-first then confidence desc then updated desc.
    - calibration-panel.tsx: read-only tiles — Classification accuracy (mono "{n}%" + "Target ≥ 85%" + the met/unmet indicator), Draft precision (mono "{n}%" + "Target ≥ 90%" + indicator), Confidence threshold (mono "{n}" + "Calibrated from the labeled set."), Autonomous filing (the "On" green / "Off" muted word + the gate caption). The met/unmet map (green "Met" ≥ target / amber "Not met yet" below / muted "—" + the honest "Run the accuracy harness to measure." when not measured). The autonomy flag is DISPLAY-ONLY (no toggle that writes — D-04); if the vendored `switch` is used it is rendered disabled as an indicator only. Each tile value has the UI-SPEC aria-label; the flag is role="status" text.
    - Every state ships: loading (skeleton rows / skeleton tiles), empty-no-defects ("No defects yet" → "Go to executions"), empty-filter-no-match (per-filter → "View drafts"), populated (rejected rows muted), inline error + Retry (never a toast). No fabricated confidence/class/accuracy/Jira-key ever rendered.
    - The sidebar gains ONE "Defects" item (icon Bug) appended after Executions; active via the existing startsWith.
  </behavior>
  <action>Create apps/web/app/(dashboard)/defects/page.tsx cloning scenarios/page.tsx: the two accent-underlined filter segment groups (?status= + ?class=, the UI-SPEC defaults/labels), the calibration panel atop, and the queue table with the six UI-SPEC columns using the Task-1 class-badge + confidence-meter, the drafts-first→confidence-desc→updated-desc sort, useQuery({retry:false}) for both the list and the calibration query, and every empty/loading/inline-error state with the exact Copywriting strings (errors inline, never a toast). Create apps/web/components/defects/calibration-panel.tsx with the four read-only tiles + the met/unmet/not-measured indicator map + the read-only autonomy-flag display (no writing toggle; a disabled `switch` only if used) + the UI-SPEC aria-labels + the role="status" flag text, banded/captioned exactly per the UI-SPEC Color + Copywriting tables. Edit apps/web/components/app-sidebar.tsx: import Bug from lucide-react and append `{ icon: Bug, label: "Defects", href: "/defects" }` to NAV_ITEMS after the Executions item (one line; the active logic already handles it). Read the relevant node_modules/next/dist/docs/ page guide first (modified Next.js).</action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
    <automated>cd apps/web && npx eslint "app/(dashboard)/defects" components/defects components/app-sidebar.tsx lib/api/defects.ts</automated>
  </verify>
  <done>the /defects list renders the calibration panel + both filter segments + the six-column drafts-first queue with the class badge/confidence meter/source refs/status/Jira key and every empty/loading/inline-error state per UI-SPEC; the calibration panel is read-only with the met/unmet/not-measured + autonomy-flag display + the aria-labels; one "Defects" sidebar item is appended after Executions; tsc + eslint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Defect detail/review page (proposed issue + evidence + auth-gated attachments + apply/reject) + mocked-API e2e</name>
  <files>apps/web/app/(dashboard)/defects/[id]/page.tsx, apps/web/tests/e2e/defects.spec.ts</files>
  <read_first>
    - apps/web/app/(dashboard)/scenarios/[id]/page.tsx (clone: breadcrumb + header-with-badges + cards + action-bar + confirm Dialog + the invalidate()/useMutation/onSuccess-toast + NO-optimistic-update + the ApiError 404 branch + the skeleton blocks)
    - 09-UI-SPEC.md §2 Defect detail (the proposed-issue card fields, the evidence card + the cited-signals caption + the fingerprint caption, the attachments card built from run-relative basenames via the auth-gated route, the action bar Apply [create vs update {key}] + Reject confirm) + every detail state (loading / draft-configured / draft-not-configured / applying-pending / applied / apply-failed / rejected / update-on-duplicate / 404) + the Copywriting contract (every string)
    - apps/web/lib/api/defects.ts (Task 1 — defectDetail/applyDefect/rejectDefect + the attachment refs + confidence_threshold)
    - apps/web/app/(dashboard)/scenarios/scenarios.spec.ts or the existing scenarios/executions e2e (the mocked-API page.route pattern — mock all /api responses; no real backend, no keys, all states mockable)
    - node_modules/next/dist/docs/ (the dynamic-route page guide — modified Next.js)
  </read_first>
  <behavior>
    - The detail page renders: the breadcrumb ("Defects" → the summary) + the header (the proposed-issue summary Heading + the class badge + the status badge + the mono "Defect {id}" + "Updated {ts}"); the Proposed Jira issue card (Summary / Description [+ the honest "Description written without an LLM…" caption when description_enriched is false] / Steps to reproduce [ordered list] / Expected / Actual / Severity / Priority); the Evidence card (Error type / DOM diff [before/after] / Healing history [link to the run's heal record + outcome] / Infra health [up/down/unknown]) + the cited-signals caption + the mono "Fingerprint {fp-hash}" caption; the Attachments card (Screenshot/Trace/Video/Console log/Network log — each an auth-gated URL the CLIENT constructs from the run-relative basename via the Phase-7 route, e.g. /api/executions/{run_id}/artifacts/{flow_id}/{kind}, NEVER a raw path; an absent artifact → the honest caption, not a broken link); the action bar (the accent Apply [label "Apply — create Jira issue" or "Apply — update {key}" per the server dedup result] + the destructive Reject).
    - Apply is an HONEST pending→result transition: "Filing…" disabled while in flight, the status flips to "Applied" with the REAL server Jira key only on the response (NO fake-instant success); on success the toast ("Issue filed — {key}" / "Issue updated — {key}"); on failure the inline "Couldn't file the issue to Jira…" + Retry, the defect STAYS a draft; when Jira is not configured Apply is disabled with the "Set the Jira email, API token, and project key in config…" caption (Reject still available). Reject uses the shadcn Dialog confirm ("Reject this defect?" → "Reject defect"/"Keep in queue") → on confirm rejectDefect → invalidate + the "Defect rejected" toast. invalidate() covers the detail + list + calibration queries. 404 → "No defect found for this id." + "Back to defects".
    - The e2e (defects.spec.ts) runs against a MOCKED API (page.route over /api/defects*, /api/defects/{id}, /calibration, the apply/reject POSTs, and the artifact route) — no real Jira, no provider keys. It covers every UI-SPEC state: the list (populated/empty/filter/error), the calibration panel (not-measured / gates-not-met / gates-met), the detail (draft-configured apply→applied with key, draft-not-configured disabled, applying-pending, apply-failed-stays-draft, update-on-duplicate label+toast, reject-confirm→rejected, 404), and that NO fabricated state crosses the boundary (green/amber/Jira-key appear only when the mock reports them).
  </behavior>
  <action>Create apps/web/app/(dashboard)/defects/[id]/page.tsx cloning scenarios/[id]/page.tsx: the breadcrumb + header (summary + class-badge + status-badge + mono "Defect {id}"), the Proposed Jira issue card (the seven fields + the honest no-LLM description caption gated on description_enriched), the Evidence card (the four cited signals + the cited-signals caption + the mono Fingerprint caption; reuse a Phase-8 diff treatment for DOM diff if vendored else a plain mono before/after), the Attachments card (the five links built CLIENT-SIDE from the run-relative basenames via /api/executions/{run_id}/artifacts/{flow_id}/{kind} — never a raw path; honest absent-artifact captions), and the action bar with the Apply mutation (the create-vs-update label from the server result, the "Filing…" pending state, the success toast, the inline apply-failed + Retry keeping it draft, the disabled not-configured state with its caption) + the Reject Dialog confirm — all with invalidate() over detail+list+calibration, NO optimistic updates, and the ApiError 404 branch + skeleton loading blocks. Use the Task-1 confidence meter banded off the payload confidence_threshold. Create apps/web/tests/e2e/defects.spec.ts mocking ALL /api responses (the scenarios/executions mocked-e2e pattern) and asserting every UI-SPEC state for the list, the calibration panel, and the detail (incl. apply→applied-with-key, not-configured-disabled, applying-pending, apply-failed-stays-draft, update-on-duplicate, reject-confirm→rejected, 404) — no real Jira, no keys. Read the relevant node_modules/next/dist/docs/ dynamic-route guide first.</action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
    <automated>cd apps/web && npx eslint "app/(dashboard)/defects/[id]" tests/e2e/defects.spec.ts</automated>
    <automated>cd apps/web && npx playwright test tests/e2e/defects.spec.ts</automated>
  </verify>
  <done>the /defects/[id] detail renders the proposed issue + cited evidence + auth-gated artifact links (run-relative basenames, never raw paths) + the apply [honest pending→result, create-vs-update from the server] + the reject confirm, with every UI-SPEC state (not-configured/applying/applied/failed-stays-draft/update-on-duplicate/rejected/404); the mocked-API e2e is green and asserts no fabricated state crosses the boundary; tsc + eslint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/defects | the dashboard reads/mutates defects across the same-origin auth boundary (httpOnly cookie) |
| server payload → rendered state | class/confidence/status/Jira-key/accuracy must render only from the server (no client fabrication) |
| run-relative basename → artifact URL | the attachment link is constructed client-side; it must never become a raw filesystem path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-18 | Tampering | artifact path traversal via the attachment link | mitigate | the link is built from the run-relative basename + kind through the Phase-7 auth-gated /api/executions/{run_id}/artifacts/{flow_id}/{kind} route (server-side run_id-derived containment guard, Plan 04); the client never holds or sends a raw filesystem path |
| T-09-19 | Information Disclosure | a secret/token rendered client-side | mitigate | the Jira token + provider keys live server-side only and never appear on any /api/defects payload (Plan 04 never serializes them); the UI displays the autonomy FLAG state (on/off) + the calibration numbers only — no credential is fetched or rendered |
| T-09-20 | Elevation of Privilege | an unauthenticated route reach | mitigate | the /defects routes ride the dashboard auth shell; every /api/defects call is get_current_user-gated server-side (Plan 04) and the api client redirects a 401 to /login (the shipped client.ts behavior) |
| T-09-21 | Spoofing | a fabricated "Applied"/Jira-key/accuracy in the UI | mitigate | no optimistic updates — defect status, the Jira key, the create-vs-update decision, and the accuracy/precision numbers render strictly from the server response after each mutation (the Phase-6/7 honesty rule); Apply is a real pending→result transition |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new frontend dependencies (no recharts, no new shadcn add); `git diff apps/web/package.json/lock` is clean — the meters are token-styled native progressbars over existing tokens |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` — the whole web app typechecks against the new client + pages.
- `cd apps/web && npx eslint "app/(dashboard)/defects" components/defects components/app-sidebar.tsx lib/api/defects.ts tests/e2e/defects.spec.ts` — lint clean (the route path QUOTED — parens break POSIX sh).
- `cd apps/web && npx playwright test tests/e2e/defects.spec.ts` — every UI-SPEC state green against the MOCKED API (no real Jira, no keys).
- `cd apps/web && git diff --exit-code package.json package-lock.json` — ZERO new frontend deps (recharts not newly used; no new shadcn add).
- Live classify→draft→apply→dedup against a real Jira Cloud is Manual-Only (the UI is proven against a mocked API).
</verification>

<success_criteria>
- JIRA-02: the review-queue surface ships EXACTLY to 09-UI-SPEC — the list (status+class filters, class badge word+icon+hue, the 0-100 confidence meter, status, source refs, Jira key), the detail (proposed issue + evidence + auth-gated attachments + apply create-vs-update + reject confirm), and the READ-ONLY calibration panel (accuracy/precision/threshold/autonomy-flag) the human reviews before flipping the flag.
- JIRA-04: the detail surfaces the test↔flow↔execution traceability refs (run_id/flow_id + the Jira key) the Defect row carries.
- ZERO new shadcn / ZERO new frontend deps (clean package.json diff; recharts not used — native meters); honest states throughout (Jira-not-configured/empty/error-with-path-forward, no fabrication); confidence/threshold render off the server value; one "Defects" sidebar item; the e2e runs against a mocked API.
</success_criteria>

<output>
Create `.planning/phases/09-defect-intelligence-jira-agent/09-05-SUMMARY.md` when done.
</output>
