---
phase: 08-self-healing-engine
plan: 04
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - infra/targets/saucedemo/Dockerfile
  - infra/docker-compose.yml
  - apps/api/tests/functional/test_healing_mutations.py
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "A catalog of benign mutations (rename data-test, change text, change tag, reorder/wrap) each builds a mutated SauceDemo target"
    - "A catalog of breaking mutations (remove element, duplicate element, break flow, change semantics) each builds a mutated target"
    - "Benign mutations heal (>90% benign-heal rate) on planted specs, keyless"
    - "Breaking mutations still FAIL (~0 false-heal rate) — removed/duplicated elements never auto_heal"
  artifacts:
    - path: "infra/targets/saucedemo/Dockerfile"
      provides: "Benign + breaking mutation build-args extending SEED_BUG"
      contains: "ARG"
    - path: "apps/api/tests/functional/test_healing_mutations.py"
      provides: "The keyless >90% benign-heal / ~0 false-heal trust-gate proof"
      contains: "benign_heal_rate"
  key_links:
    - from: "apps/api/tests/functional/test_healing_mutations.py"
      to: "the mutation build targets + the in-spec heal accessor"
      via: "run a planted spec (with _healing.py wired) against each mutated build; assert journal outcomes"
      pattern: "auto_heal|fail_as_defect"
---

<objective>
Build the trust gate (QUAL-02): the benign-vs-breaking mutation harness that PROVES the deterministic engine heals real UI drift without masking real defects. Extend the SauceDemo SEED_BUG Dockerfile build-arg pattern into a catalog — BENIGN mutations (rename data-test, change visible text, change tag, reorder/wrap an element) that MUST heal (>90%), and BREAKING mutations (remove the element, duplicate it, break the flow, change semantics) that MUST still fail (~0 false-heal). Prove it DETERMINISTICALLY and KEYLESSLY on planted specs (the in-spec _healing.py from plan 02 carries the vendored scorer + chains; neo4j OFF in the run phase per the 3GB cap).

Purpose: QUAL-02 — the seeded-bug/benign-mutation harness measuring heal success (>90%) + a near-zero false-heal rate, proving generated tests fail on real bugs.
Output: the Dockerfile mutation catalog + compose mutation services + the keyless functional harness asserting the rates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-self-healing-engine/08-RESEARCH.md
@.planning/phases/08-self-healing-engine/08-PATTERNS.md
@.planning/phases/08-self-healing-engine/08-02-SUMMARY.md
@.planning/phases/06-bdd-playwright-generation/06-04-SUMMARY.md

<interfaces>
<!-- The seams this plan extends. Executor uses these directly. -->

From infra/targets/saucedemo/Dockerfile (the SEED_BUG build-arg pattern to extend, lines 42-46):
- `ARG SEED_BUG=0` then `RUN if [ "$SEED_BUG" = "1" ]; then grep -rl 'inventory_list' ... | xargs -r sed -i 's/inventory_list/inventory_list_BROKEN/g'; fi`
- default 0 keeps the standard build BYTE-IDENTICAL (T-06-21 — no drift)

From infra/docker-compose.yml (the existing saucedemo-bug profile service to mirror):
- the bugbuild-profile saucedemo-bug service with `build.args: SEED_BUG: "1"` on a distinct host port

From apps/api/tests/functional/test_seeded_bug.py (the accept/reject assertion shape to extend to a catalog):
- pytestmark = [pytest.mark.functional, pytest.mark.graph]; reuse `_plant` / `_WORKSPACES_ROOT` / SAUCEDEMO_HOST_URL from test_stability.py
- `run_seeded_bug(spec_path, base_url=...)` → {detected_breakage, exit_code, base_url}; the subprocess runner is stability._run_spec_once (argv list, no shell)

From apps/api/app/services/stability.py:
- `_run_spec_once(spec, base_url=..., extra_args=...)` → {exit_code, output} (the keyless subprocess primitive)

From plan 02 (the in-spec heal accessor):
- the planted spec includes the rendered _healing.py + a page object whose _resolve heals on a locator miss; the per-flow heal-journal.json records {outcome, live_match_count, ...}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The mutation catalog — Dockerfile build-args + compose mutation services (QUAL-02)</name>
  <read_first>
    - infra/targets/saucedemo/Dockerfile (the SEED_BUG build-arg lines 33-46 — the exact pattern to extend)
    - infra/docker-compose.yml (the saucedemo / saucedemo-bug services — mirror for the mutation services)
    - .planning/phases/08-self-healing-engine/08-RESEARCH.md (Pattern 6 catalog table lines 309-322 the exact sed rewrites + expected outcomes)
    - .planning/phases/08-self-healing-engine/08-PATTERNS.md (Dockerfile extend lines 270-280; compose line 31)
  </read_first>
  <action>
    EXTEND infra/targets/saucedemo/Dockerfile with one build-arg per catalog mutation, each defaulting to 0 (so the standard + existing builds stay byte-identical, T-06-21). BENIGN args (MUST heal — alt tiers still match): BENIGN_RENAME_DATATEST (sed rename a data-test attribute value, e.g. add-to-cart to add-to-cart-btn), BENIGN_CHANGE_TEXT (sed change a visible label), BENIGN_CHANGE_TAG (rewrite a button to an a role=button element), BENIGN_WRAP (wrap the target element in an extra div — ancestry/xpath shift, attrs/role/bbox unchanged). BREAKING args (MUST still fail — uniqueness gate / semantics): BREAK_REMOVE (delete the target element node), BREAK_DUPLICATE (clone the target element so a live match is ambiguous, count greater than 1), plus the EXISTING SEED_BUG (break the post-login flow — an assertion target, not a locator). Each is one deterministic sed/grep rewrite gated by its build-arg, mirroring the SEED_BUG block exactly. Add matching compose services to infra/docker-compose.yml under a mutation profile (mirror saucedemo-bug): one service per build-arg with build.args setting that arg to "1", each on a DISTINCT host port, in-cluster names like saucedemo-benign-rename / saucedemo-break-remove. Document the catalog-to-expected-outcome mapping in a Dockerfile comment block (benign means HEAL, breaking means FAIL) sourced from RESEARCH Pattern 6.
  </action>
  <verify>
    <automated>cd infra && docker compose --profile mutation config >/dev/null && echo ok && grep -cE 'ARG (BENIGN_|BREAK_)' targets/saucedemo/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `cd infra && docker compose --profile mutation config` validates (compose config is well-formed)
    - `grep -cE 'ARG (BENIGN_|BREAK_)' infra/targets/saucedemo/Dockerfile` returns >= 6 (4 benign + 2 breaking; SEED_BUG already exists)
    - Every mutation build-arg defaults to 0 (the standard build stays byte-identical): `grep -E 'ARG (BENIGN_|BREAK_)' infra/targets/saucedemo/Dockerfile` shows each `=0`
    - Each compose mutation service is on a distinct host port (no port collision) — verified in the compose config output
  </acceptance_criteria>
  <done>The Dockerfile carries the benign + breaking mutation build-arg catalog (each defaulting to 0), and compose exposes one mutation-profile service per mutation on distinct ports.</done>
</task>

<task type="auto">
  <name>Task 2: The keyless >90% benign-heal / ~0 false-heal mutation proof (QUAL-02)</name>
  <read_first>
    - apps/api/tests/functional/test_seeded_bug.py (the accept/reject assertion shape lines 49-105 to extend to a catalog)
    - apps/api/tests/functional/test_stability.py (the _plant / _WORKSPACES_ROOT / SAUCEDEMO_HOST_URL host fixtures)
    - apps/api/tests/functional/test_inspec_heal.py (plan 02 — the planted spec with _healing.py wired + the journal-read assertion pattern)
    - .planning/phases/08-self-healing-engine/08-RESEARCH.md (Pattern 6 measurement lines 324; Pitfall 6 sequencing lines 395-399 neo4j OFF in the run phase)
    - .planning/phases/08-self-healing-engine/08-VALIDATION.md (QUAL-02 rows; Manual-Only memory-fit note)
  </read_first>
  <action>
    Create apps/api/tests/functional/test_healing_mutations.py (pytestmark = [pytest.mark.functional]; keyless; neo4j OFF in the run phase — chains/history are vendored into _healing.py at plant time, per Pitfall 6). Plant the heal-wired spec (reuse the plan-02 _inspec_heal plant helper) and run it against EACH mutation build target by its host URL via stability._run_spec_once. For each BENIGN build: assert the spec PASSES and the per-flow heal-journal records an `auto_heal` entry with live_match_count == 1. For each BREAKING build: assert the spec FAILS and the journal records `fail_as_defect`/`quarantine` (NEVER `auto_heal`) — for BREAK_REMOVE live_match_count == 0, for BREAK_DUPLICATE live_match_count > 1, for SEED_BUG the failure is an assertion target (never a heal target). Aggregate two rates exactly as RESEARCH Pattern 6 measurement: `benign_heal_rate = healed_benign / total_benign` and assert >= 0.90; `false_heal_rate = auto_healed_breaking / total_breaking` and assert ~= 0 (== 0). Mark the test to skip gracefully (pytest.skip with a clear message) when the mutation-profile targets are not up, mirroring test_seeded_bug.py's REQUIRES note. Document at the top: the live end-to-end heal during a real LLM-generated suite + the docker-stats memory-fit observation are Manual-Only (08-VALIDATION).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_healing_mutations.py -m functional -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/api && uv run pytest tests/functional/test_healing_mutations.py -m functional -q` passes with the mutation-profile targets up and neo4j OFF (or skips cleanly with a clear message when targets are down)
    - benign_heal_rate >= 0.90 asserted; false_heal_rate == 0 asserted (no breaking build auto_heals)
    - BREAK_REMOVE journal shows live_match_count == 0 and outcome fail_as_defect (the uniqueness gate forbids heal); BREAK_DUPLICATE shows live_match_count > 1 and no auto_heal
    - The test reads NO provider keys (keyless) and starts NO neo4j in the run phase
  </acceptance_criteria>
  <done>The mutation harness proves >90% benign-heal AND ~0 false-heal on planted specs, keyless, with removed/duplicated elements provably never auto-healing — the QUAL-02 trust gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutated target DOM → in-spec heal | The harness runs the heal-wired spec against deliberately-mutated (untrusted) targets to prove the engine's structural guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Tampering | a false heal on a breaking change (the core security property) | mitigate | The harness IS the structural proof: BREAK_REMOVE (count 0) and BREAK_DUPLICATE (count >1) must never auto_heal — false_heal_rate == 0 is asserted. The uniqueness gate (plan 01/02) is verified end-to-end against real mutated pages. |
| T-08-16 | DoS | OOM during the mutation matrix under the 3GB WSL cap | mitigate | neo4j is OFF in the run phase (chains/history vendored into _healing.py); the matrix runs saucedemo variants + Chromium only — same sequencing as test_seeded_bug.py. Memory fit is a Manual-Only docker-stats observation. |
| T-08-17 | Tampering | mutation build-args drifting the standard build | mitigate | Every build-arg defaults to 0; the standard build stays byte-identical (T-06-21), asserted by the compose config + the default-0 grep. |
</threat_model>

<verification>
- The mutation harness passes keyless with the mutation-profile targets up + neo4j off.
- The standard build stays byte-identical (all mutation args default 0).
- Full deterministic suite unaffected: `cd apps/api && uv run pytest -m "not live_llm and not graph and not e2e" -q`.
</verification>

<success_criteria>
- QUAL-02: the benign-vs-breaking mutation catalog measures >90% benign-heal AND a near-zero false-heal rate on planted specs, keyless — generated tests still fail on real bugs.
- Zero new packages (the catalog is compose infra build-args, not packages).
</success_criteria>

<output>
Create `.planning/phases/08-self-healing-engine/08-04-SUMMARY.md` when done.
</output>
