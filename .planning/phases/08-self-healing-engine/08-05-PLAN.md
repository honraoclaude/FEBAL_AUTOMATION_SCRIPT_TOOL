---
phase: 08-self-healing-engine
plan: 05
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - apps/api/app/services/healing/stats.py
  - apps/api/app/schemas/heal.py
  - apps/api/app/routers/heals.py
  - apps/api/app/main.py
  - apps/api/tests/integration/test_heal_stats.py
  - apps/api/tests/integration/test_heals_router.py
autonomous: true
requirements: [HEAL-04, HEAL-03]
must_haves:
  truths:
    - "Per-element heal-success rate and false-heal rate are computed by aggregating heal_audit"
    - "An authenticated user can list quarantined heals with their before/after diff + confidence"
    - "An authenticated user can apply a quarantined heal (rewrite staged → applied) or reject it (marks false-heal)"
    - "Every quarantine API endpoint refuses an unauthenticated request (401)"
  artifacts:
    - path: "apps/api/app/services/healing/stats.py"
      provides: "per_element_heal_stats — success-rate + false-heal-rate aggregation over heal_audit"
      contains: "def per_element_heal_stats"
    - path: "apps/api/app/routers/heals.py"
      provides: "auth-gated GET /api/heals, POST /api/heals/{id}/apply, POST /api/heals/{id}/reject, GET /api/heals/stats"
      contains: "APIRouter"
    - path: "apps/api/app/schemas/heal.py"
      provides: "HealAuditResponse (before/after diff + confidence) + HealStatsResponse"
      contains: "HealAuditResponse"
  key_links:
    - from: "apps/api/app/routers/heals.py"
      to: "get_current_user"
      via: "router-level auth gate on every endpoint (state-changing apply/reject MUST be auth-gated)"
      pattern: "get_current_user"
    - from: "apps/api/app/routers/heals.py"
      to: "healing.stats.per_element_heal_stats + the HealAudit table"
      via: "GET /stats aggregates; apply/reject mutate the audit row"
      pattern: "per_element_heal_stats|reviewed_outcome"
---

<objective>
Expose healing for reporting + review (D-05 — NO heal UI, API only): per-element heal-success and false-heal aggregation over heal_audit (HEAL-04), and a minimal auth-gated quarantine API — list quarantined heals (with their before/after diff + confidence from the audit record), apply a quarantined heal (stage → applied, performing the deferred page-object rewrite), and reject it (marks the row a false-heal via reviewed_outcome). The rich quarantine review SCREEN + the heal-trend dashboards are DEFERRED to Phase 10; this plan persists + exposes only.

Purpose: HEAL-04 (per-element success/false-heal stats exposed) + HEAL-03's review surface (the before/after diff served via the API).
Output: the stats aggregation service + the heal schemas + the auth-gated heals router (registered in main.py) + integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-self-healing-engine/08-RESEARCH.md
@.planning/phases/08-self-healing-engine/08-PATTERNS.md
@.planning/phases/08-self-healing-engine/08-03-SUMMARY.md
@.planning/phases/07-execution-engine-workers/07-03-SUMMARY.md

<interfaces>
<!-- The seams this plan extends. Executor uses these directly. -->

From app/models/heal_audit.py (plan 03 — the table to aggregate + mutate):
- HealAudit: id PK, element_key, run_id, flow_id, before_chain JSON, after_chain JSON, confidence Float, outcome String(16) (auto_heal|quarantine|fail_as_defect|applied|rejected), live_match_count, reviewed_outcome String(16) nullable, created_at

From app/services/healing/ingest.py (plan 03 — the rewrite to reuse on apply):
- `rewrite_page_object_locator(source, *, element_key, new_selector) -> str` (ast-validated) for the deferred apply rewrite

From app/services/exec_history.py (the aggregation-query style to mirror — SQLAlchemy 2.0 select/scalars):
- pass_rate_trend / durations_by_flow / flaky_leaderboard / list_runs — select(...).group_by(...) patterns

From app/routers/executions.py (the auth-gated router pattern — REUSE get_current_user; require_role does NOT exist):
- `router = APIRouter(prefix="/api/executions", dependencies=[Depends(require_user_or_ci_token)])`
- `get_current_user` from app.core.security (the cookie auth gate); 401 on unauth
- handlers: GET returns response_model=list[...]; per-id GET 404s via HTTPException(status_code=404)
- the router is registered in app/main.py via the app factory (mirror the executions registration)

From app/schemas/execution.py (the Pydantic v2 response shape — from_attributes/model_validate):
- TestRunResponse with `model_config = ConfigDict(from_attributes=True)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-element heal-stats aggregation + the heal schemas (HEAL-04)</name>
  <read_first>
    - apps/api/app/services/exec_history.py (the SQLAlchemy 2.0 select/group_by aggregation style to mirror)
    - apps/api/app/models/heal_audit.py (the table to aggregate)
    - apps/api/app/schemas/execution.py (the Pydantic v2 from_attributes response shape)
    - .planning/phases/08-self-healing-engine/08-RESEARCH.md (Pattern 5 lines 303-307 the stat definitions)
    - .planning/phases/08-self-healing-engine/08-PATTERNS.md (stats lines 301-303; schema lines 230-232)
  </read_first>
  <behavior>
    - per_element_heal_stats over rows for element K with 8 auto_heal + 1 applied + 1 rejected → heal_success_rate = (auto_heal + applied) / total attempts; false_heal_rate = rejected-after-auto_heal / auto_heal count
    - an element with zero heal attempts is absent from the result (no divide-by-zero)
    - filtering by element returns only that element's stats
    - HealAuditResponse serializes before_chain/after_chain/confidence/outcome/live_match_count for the before/after diff
  </behavior>
  <action>
    Create apps/api/app/services/healing/stats.py with `async def per_element_heal_stats(db, *, element_key: str | None = None) -> list[dict]` — SQLAlchemy 2.0 select aggregations over HealAudit grouped by element_key (mirror exec_history.py): per element compute heal_success_rate = count(outcome IN (auto_heal, applied)) / count(all attempts), and false_heal_rate = count(reviewed_outcome == rejected on a row that was auto_heal/applied) / count(auto_heal) — the reject API flips reviewed_outcome (the false-heal signal, RESEARCH Pattern 5). Guard against divide-by-zero (skip elements with zero attempts). Optional element_key filter. Create apps/api/app/schemas/heal.py with Pydantic v2 `HealAuditResponse` (id, element_key, run_id, flow_id, before_chain, after_chain, confidence, outcome, live_match_count, reviewed_outcome, created_at; model_config from_attributes=True — the before/after diff renders from this) and `HealStatsResponse` (element_key, attempts, heal_success_rate, false_heal_rate). Write apps/api/tests/integration/test_heal_stats.py over seeded heal_audit rows asserting the two rates per element + the no-attempt skip.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/integration/test_heal_stats.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/api && uv run pytest tests/integration/test_heal_stats.py -q` passes
    - per_element_heal_stats computes both heal_success_rate and false_heal_rate per element and skips zero-attempt elements (no divide-by-zero)
    - HealAuditResponse.model_validate parses a HealAudit ORM row (from_attributes)
    - The stats queries are parameterized via the ORM (no string-built SQL): `grep -ciE 'select|func' apps/api/app/services/healing/stats.py` returns >= 1 and no f-string SQL is present
  </acceptance_criteria>
  <done>Per-element heal-success + false-heal rates aggregate over heal_audit, and the heal response/stats schemas serialize the before/after diff — HEAL-04 persisted + exposable.</done>
</task>

<task type="auto">
  <name>Task 2: The auth-gated quarantine API (list / apply / reject / stats) + main.py registration (D-05 / HEAL-04)</name>
  <read_first>
    - apps/api/app/routers/executions.py (the auth-gated APIRouter pattern lines 74-138; get_current_user; the 404 HTTPException shape)
    - apps/api/app/main.py (the app factory router registration — mirror the executions include_router)
    - apps/api/app/services/healing/ingest.py (rewrite_page_object_locator + append_element_history to reuse on apply)
    - .planning/phases/08-self-healing-engine/08-RESEARCH.md (Pattern 5; the API surface diagram lines 152-156; A6 no require_role lines 487)
    - .planning/phases/08-self-healing-engine/08-PATTERNS.md (router lines 213-226; auth-gated lines 333-335)
  </read_first>
  <action>
    Create apps/api/app/routers/heals.py: `router = APIRouter(prefix="/api/heals", tags=["heals"], dependencies=[Depends(get_current_user)])` (reuse get_current_user from app.core.security — require_role does NOT exist, A6; EVERY endpoint, especially the state-changing apply/reject, is auth-gated). Endpoints: `GET ""` with `?status=quarantined` (default) → `list[HealAuditResponse]` (the quarantine queue with before/after diff + confidence); `POST "/{heal_id}/apply"` → mark the row applied: load the HealAudit by int PK (404 on unknown), perform the DEFERRED page-object rewrite via rewrite_page_object_locator on the run's pages file (the rewrite was STAGED for quarantine in plan 03, applied here), append the KG history via kg/writer.append_element_history (best-effort), set outcome = "applied"; `POST "/{heal_id}/reject"` → set reviewed_outcome = "rejected" (the false-heal signal for HEAL-04 stats; revert any staged rewrite — for quarantine nothing was written so it is a no-op flag flip), 404 on unknown; `GET "/stats"` with `?element=<key>` → `list[HealStatsResponse]` via per_element_heal_stats. heal_id is an int PK + element is a string filter — parameterize via the ORM (V5). Register the router in app/main.py (mirror the executions include_router). Write apps/api/tests/integration/test_heals_router.py: unauth on every endpoint → 401; authed GET → the quarantined rows; authed apply → outcome applied + the page-object rewrite happened (ast-valid); authed reject → reviewed_outcome rejected; unknown heal_id → 404.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/integration/test_heals_router.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/api && uv run pytest tests/integration/test_heals_router.py -q` passes
    - Every endpoint refuses unauth → 401 (the router-level get_current_user gate): asserted across GET/apply/reject/stats
    - apply sets outcome = applied AND performs the ast-validated deferred rewrite; reject sets reviewed_outcome = rejected (the false-heal signal)
    - The router is registered: `grep -c 'heals' apps/api/app/main.py` returns >= 1
    - heal_id is an int PK + queries are ORM-parameterized (no string-built SQL); unknown id → 404
    - No heal UI is added (D-05 — API only): no apps/web changes in this plan
  </acceptance_criteria>
  <done>The auth-gated quarantine API lists/applies/rejects heals (with the before/after diff + confidence) and serves per-element stats; it is registered in main.py and every endpoint is auth-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/heals | Untrusted client requests cross into a state-changing review API (apply/reject mutate generated artifacts + DB rows). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Elevation of Privilege | unauthenticated apply/reject mutating state | mitigate | Router-level Depends(get_current_user) gates EVERY endpoint; unauth → 401. Asserted across all four routes (V4 — state-changing endpoints are never public). |
| T-08-19 | Tampering | SQL injection via the element filter / heal_id | mitigate | heal_id is an int PK; element is a string bound via the SQLAlchemy ORM — parameterized, no string-built SQL (V5). |
| T-08-20 | Tampering | code injection via the apply-time page-object rewrite | mitigate | apply reuses rewrite_page_object_locator (line-targeted by attr name, ast-validated) — a non-parsing rewrite raises, never persisted (carry T-08-10). |
| T-08-21 | Tampering | Cypher injection on apply's KG write-back | mitigate | apply routes through kg/writer.append_element_history (parameterized + single writer + read-back guard). |
</threat_model>

<verification>
- Full deterministic suite green: `cd apps/api && uv run pytest -m "not live_llm and not graph and not e2e" -q`
- Every /api/heals endpoint 401s when unauth.
- The single-write-path grep gate stays green (apply routes KG writes through kg/writer).
</verification>

<success_criteria>
- HEAL-04: per-element heal-success + false-heal rates aggregate over heal_audit and are exposed via GET /api/heals/stats.
- HEAL-03 review surface: the before/after diff + confidence are served via the auth-gated quarantine API (list/apply/reject).
- D-05 honored: API only, no heal UI; the screen + dashboards stay Phase 10.
- Zero new packages.
</success_criteria>

<output>
Create `.planning/phases/08-self-healing-engine/08-05-SUMMARY.md` when done.
</output>
