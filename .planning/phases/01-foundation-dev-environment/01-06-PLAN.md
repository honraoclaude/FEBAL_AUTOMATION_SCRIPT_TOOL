---
phase: 01-foundation-dev-environment
plan: 06
type: execute
wave: 5
depends_on: ["01-04", "01-05"]
files_modified:
  - apps/web/app/(dashboard)/targets/page.tsx
  - apps/web/components/targets/targets-table.tsx
  - apps/web/components/targets/target-dialog.tsx
  - apps/web/components/targets/deactivate-dialog.tsx
  - apps/web/lib/api/targets.ts
  - apps/api/tests/e2e/test_targets_ui.py
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "User registers a target through the dialog and sees it appear in the table"
    - "Editing a target never shows stored credentials — masked placeholders and helper copy only"
    - "Deactivating via the row menu marks the row inactive (muted, Inactive badge) after confirmation"
    - "Empty, loading, populated, and inactive table states render per the UI contract"
  artifacts:
    - path: "apps/web/components/targets/targets-table.tsx"
      provides: "Targets table per 01-UI-SPEC §3 (columns, badges, skeleton, empty state, row actions)"
    - path: "apps/web/components/targets/target-dialog.tsx"
      provides: "Register/edit dialog per 01-UI-SPEC §4 (sections, masked credentials, allowlist textarea, sandbox switch, budget overrides)"
    - path: "apps/api/tests/e2e/test_targets_ui.py"
      provides: "Playwright e2e for registry UI (VALIDATION row PLAT-01/e2e)"
  key_links:
    - from: "apps/web/components/targets/target-dialog.tsx"
      to: "/api/targets"
      via: "TanStack Query mutation + list invalidation"
      pattern: "api/targets"
    - from: "apps/web/components/targets/targets-table.tsx"
      to: "GET /api/targets?include_inactive=true"
      via: "useQuery"
      pattern: "include_inactive"
---

## Phase Goal

**As a** platform operator, **I want to** stand up the platform skeleton locally with one command, log in, and securely register a target application, **so that** the platform has a target to explore in later phases.

(This plan finishes the registration story in the browser — PLAT-01's UI half. After it, the entire phase goal is achievable end-to-end by a human with a mouse.)

<objective>
Build the target registry UI per the locked 01-UI-SPEC (§3 table, §4 dialog): list with all four states, register/edit dialog with write-only masked credentials and exploration rules, deactivate/reactivate flow — plus the Playwright e2e suite (D-02).

Purpose: PLAT-01 UI. Credentials masking in the UI is the frontend face of D-06.
Output: /targets fully functional; `uv run pytest tests/e2e/test_targets_ui.py -x` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-dev-environment/01-UI-SPEC.md
@.planning/phases/01-foundation-dev-environment/01-VALIDATION.md
@apps/web/app/(dashboard)/targets/page.tsx
@apps/web/lib/api/client.ts
@apps/api/app/schemas/target.py

<interfaces>
<!-- API contract from plan 01-05 (consume as-is): -->
```
POST   /api/targets        -> 201 TargetResponse        GET /api/targets?include_inactive=true -> [TargetResponse]
GET    /api/targets/{id}   -> 200 | 404                 PATCH /api/targets/{id} -> 200
DELETE /api/targets/{id}   -> 204 (soft delete)
TargetResponse = { id, name, base_url, has_credentials, origin_allowlist: string[],
                   sandbox: boolean, budget_overrides: object|null, is_active: boolean, created_at, updated_at }
TargetCreate requires credentials {username, password}; TargetUpdate: all optional, credentials replace-if-present, is_active toggles.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Targets table page — states, badges, row actions</name>
  <files>apps/web/app/(dashboard)/targets/page.tsx, apps/web/components/targets/targets-table.tsx, apps/web/lib/api/targets.ts</files>
  <read_first>
    - .planning/phases/01-foundation-dev-environment/01-UI-SPEC.md (§3 Target registry — columns, badge colors, states; Color section status tokens; Copywriting Contract — exact strings; Typography mono rule for URLs)
    - apps/web/app/(dashboard)/targets/page.tsx (stub from plan 01-04 — replace its body, keep header)
    - apps/web/lib/api/client.ts (fetch wrapper conventions from plan 01-04)
    - apps/api/app/schemas/target.py (field names/types — mirror in the zod schema)
  </read_first>
  <action>
    Create `lib/api/targets.ts`: zod schemas mirroring TargetResponse/TargetCreate/TargetUpdate from `<interfaces>` (parse API responses at the boundary), plus typed functions listTargets(includeInactive)/createTarget/updateTarget/deactivateTarget over the client.ts wrapper.

    Replace the /targets stub body with `targets-table.tsx` per UI-SPEC §3, fetching `useQuery(["targets"], () => listTargets(true))`:
    - Columns: Name · Base URL (Geist Mono at body size) · Sandbox (amber-outline "Sandbox" badge when true, blank when false — `--status-quarantine`) · Status (Active: green-dot badge `--status-pass` / Inactive: muted badge `--status-neutral`) · row-actions dropdown-menu with icon-only trigger carrying `aria-label="Actions for {name}"`, items Edit and Deactivate (Reactivate replaces Deactivate when is_active=false).
    - States: loading → skeleton rows; empty → exact copy "No target applications yet" heading + "Register a target application to give the platform something to explore." + "Register target" button (Copywriting Contract); populated; inactive rows → entire row text muted.
    - Page header from the 01-04 stub stays: Heading "Target Applications" left, accent "Register target" button right — now opening the dialog (Task 2 wires it; render the button enabled with a no-op until then is NOT acceptable — implement Tasks 1 and 2 in the same working session and wire on completion).
  </action>
  <verify>
    <automated>cd apps/web && npm run build && grep -c "include_inactive" lib/api/targets.ts && grep -c "Actions for" components/targets/targets-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - targets-table.tsx contains the exact empty-state strings from the Copywriting Contract
    - Base URL cells use a mono font class (grep `font-mono` in targets-table.tsx)
    - Row-actions trigger has `aria-label` containing "Actions for"
    - Status badges reference the status tokens (grep `status-pass` and `status-neutral` usage or their mapped classes)
  </acceptance_criteria>
  <done>Table renders all four states from live API data with contract-exact copy, badges, and accessibility labels.</done>
</task>

<task type="auto">
  <name>Task 2: Register/edit dialog, deactivate confirmation, mutations + toasts</name>
  <files>apps/web/components/targets/target-dialog.tsx, apps/web/components/targets/deactivate-dialog.tsx, apps/web/components/targets/targets-table.tsx, apps/web/app/(dashboard)/targets/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-dev-environment/01-UI-SPEC.md (§4 dialog — sections, field list, helper copy, states; Copywriting Contract — CTA labels, destructive confirmation, credential masking strings; Interaction defaults — toasts success-only, errors inline, mutations invalidate)
    - apps/web/components/targets/targets-table.tsx (Task 1 output — wire Edit/Deactivate actions)
  </read_first>
  <action>
    Build `target-dialog.tsx` (shadcn Dialog, md padding, single column) per UI-SPEC §4 with three Label-weight section headings separated by lg gaps:
    - "Target": name (required), base URL (required, zod URL validation — error copy "Enter a valid URL (including http:// or https://)").
    - "Credentials": username + password (password input type). Create mode: required. Edit mode: NEVER prefilled; both placeholders "••••••••"; helper caption "Stored encrypted and never shown. Enter new values to replace them."; submitted only if the user typed new values (omit `credentials` from PATCH otherwise — write-only contract D-06).
    - "Exploration rules": origin allowlist textarea one-origin-per-line, helper "Defaults to the base URL's origin" (send only if non-empty); sandbox Switch with caption "Allows destructive actions during exploration — only enable for restorable targets"; collapsible "Budget overrides" group of 4 optional numeric fields (max steps, max depth, wall-clock seconds, token budget).
    - CTAs: create → "Register target"; edit → "Save changes". States: submitting (disabled + spinner), field-level inline errors (12px destructive), request error → inline alert at top of dialog body with exact copy "Couldn't save the target. Try again — if it keeps failing, check that the API container is healthy (`docker compose ps`)." Cancel discards silently.

    Build `deactivate-dialog.tsx`: confirmation with exact copy "Deactivate {name}? The platform will stop running against this target. Its history is kept and you can reactivate it later." — destructive "Deactivate" / dismiss "Keep target". Reactivate needs no confirmation (PATCH is_active=true directly).

    Wire everything with TanStack mutations invalidating ["targets"] on success; sonner toasts (bottom-right) success-only: "Target registered" / "Target updated" / "Target deactivated". Errors never toast. No optimistic updates.
  </action>
  <verify>
    <automated>cd apps/web && npm run build && grep -c "Stored encrypted and never shown" components/targets/target-dialog.tsx && grep -c "Keep target" components/targets/deactivate-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Dialog source contains the exact helper/masking strings and both CTA labels from the Copywriting Contract
    - Edit mode source never reads credential values from TargetResponse (it has none) and omits `credentials` from PATCH when fields are untouched
    - Success paths call sonner toast with the three exact messages; no toast call on any error path
    - All mutations invalidate the ["targets"] query key
  </acceptance_criteria>
  <done>Full register/edit/deactivate/reactivate flow works in the browser against the live API with contract-exact copy and write-only credentials.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright e2e — registry UI flows</name>
  <files>apps/api/tests/e2e/test_targets_ui.py</files>
  <read_first>
    - apps/api/tests/e2e/test_login_ui.py (login helper pattern from plan 01-04 — reuse its login steps)
    - .planning/phases/01-foundation-dev-environment/01-VALIDATION.md (row PLAT-01/e2e — register via dialog, appears in table, credentials masked)
    - .planning/phases/01-foundation-dev-environment/01-UI-SPEC.md (§3/§4 states the tests must touch)
  </read_first>
  <action>
    Write `tests/e2e/test_targets_ui.py` (marker `e2e`, pytest-playwright on host against WEB_BASE, logging in first via the UI with ADMIN_EMAIL/ADMIN_PASSWORD; unique target names per test via uuid):
    1. `test_register_target_via_dialog` — open "Register target", fill name/base URL/credentials, submit → dialog closes, toast "Target registered" appears, new row visible with Active badge and mono base URL.
    2. `test_edit_dialog_masks_credentials` — open Edit on that row → username/password inputs are EMPTY with placeholder "••••••••" and helper text "Stored encrypted and never shown. Enter new values to replace them." visible; change name only → "Save changes" → row updates; password value typed at registration never appears anywhere in page content (`expect(page.locator("body")).not_to_contain_text(password)`).
    3. `test_deactivate_flow` — row menu → Deactivate → confirmation copy visible → confirm → row turns muted with Inactive badge; menu now offers Reactivate.
    4. `test_validation_errors_inline` — submit dialog empty → "Name is required" inline; invalid URL → "Enter a valid URL (including http:// or https://)" inline; no toast shown.
    Run against the full compose stack.
  </action>
  <verify>
    <automated>docker compose -f infra/docker-compose.yml --env-file .env up -d --wait && cd apps/api && uv run pytest tests/e2e/test_targets_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 4 e2e tests pass against http://localhost:3000 with the compose stack up
    - Test 2 asserts the registered password string is absent from the full page body during edit
    - Tests generate unique target names (uuid) — re-runnable without manual cleanup
  </acceptance_criteria>
  <done>ROADMAP criterion 3 fully proven: target registered through UI and API, listed, credentials masked end-to-end (VALIDATION row PLAT-01/e2e green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → dialog forms → API | untrusted names/URLs/credentials enter through the dialog |
| API responses → DOM | rendered data must carry no credential material |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-22 | Information Disclosure | credentials rendered in edit dialog | mitigate | TargetResponse has no credential fields (structural); dialog never prefils credential inputs; e2e asserts password absent from page body |
| T-01-23 | Tampering (V5) | malformed form input | mitigate | zod validation client-side (UX duplicate) + Pydantic server-side authority (RESEARCH Responsibility Map) |
| T-01-24 | Information Disclosure | XSS via target names/URLs echoed into table | mitigate | React default escaping only; no dangerouslySetInnerHTML anywhere (grep gate available) |
</threat_model>

<verification>
- `uv run pytest tests/e2e/test_targets_ui.py -x -q` green against `docker compose up` (VALIDATION row PLAT-01/e2e → task 01-06-T3)
- `npm run build` clean
</verification>

<success_criteria>
- Register/edit/deactivate/reactivate all work in the browser with UI-SPEC-exact copy and states
- Credentials handled write-only in the UI (masked, never prefilled, omitted when untouched)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-dev-environment/01-06-SUMMARY.md` when done.
</output>
