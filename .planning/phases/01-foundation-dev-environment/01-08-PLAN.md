---
phase: 01-foundation-dev-environment
plan: 08
type: execute
wave: 8
depends_on: ["01-05", "01-06", "01-07"]
# The live compose stack is an EXCLUSIVE execution resource — final verification runs alone in wave 8.
files_modified:
  - infra/scripts/verify_stack.py
  - docs/dev-setup.md
autonomous: false
requirements: [INFRA-01]

must_haves:
  truths:
    - "From a clean state (docker compose down -v), one command brings up the full stack and the entire test suite passes"
    - "verify_stack.py proves: all default services healthy, dormant services absent, every container has a non-zero memory limit"
    - "docs/dev-setup.md documents both run modes (compose and hybrid host) sharing one .env, plus .wslconfig install and reset-target usage"
    - "A human has confirmed Vmmem is bounded after wsl --shutdown and the UI works end-to-end"
    - "D-01: a green phase gate is the only ceremony between Phase 1 and Phase 2 — no extra approval rounds (continuous-build directive)"
  artifacts:
    - path: "infra/scripts/verify_stack.py"
      provides: "INFRA-01 evidence script (VALIDATION Wave 0 item)"
      contains: "HostConfig"
    - path: "docs/dev-setup.md"
      provides: "Complete dev workflow documentation (D-08/D-09/D-10)"
      contains: "hybrid"
  key_links:
    - from: "infra/scripts/verify_stack.py"
      to: "docker compose ps / docker inspect"
      via: "subprocess JSON parsing"
      pattern: "compose ps"
---

## Phase Goal

**As a** platform operator, **I want to** stand up the platform skeleton locally with one command, log in, and securely register a target application, **so that** the platform has a target to explore in later phases.

(This plan is the phase gate: prove the whole goal holds from a cold start, produce the INFRA-01 evidence script, finish the docs, and get human sign-off on the host-level checks no script can perform.)

<objective>
Ship the verify_stack.py evidence script, complete docs/dev-setup.md, run the clean-state phase gate (down -v → up → full suite → verify), and close with the human verification of `.wslconfig`/Vmmem and a visual UI walkthrough.

Purpose: INFRA-01 success-criterion evidence; closes every remaining 01-VALIDATION.md item. Per D-01 (continuous-build directive), a green phase gate flows directly into Phase 2 without extra ceremony.
Output: green clean-state run; completed docs; human-approved phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-dev-environment/01-VALIDATION.md
@.planning/phases/01-foundation-dev-environment/01-RESEARCH.md
@infra/docker-compose.yml
@docs/dev-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify_stack.py evidence script + complete dev-setup docs</name>
  <files>infra/scripts/verify_stack.py, docs/dev-setup.md</files>
  <read_first>
    - .planning/phases/01-foundation-dev-environment/01-VALIDATION.md (row INFRA-01 — exact assertions the script must make; Manual-Only Verifications table — what docs must explain)
    - .planning/phases/01-foundation-dev-environment/01-RESEARCH.md (Pitfall 3 — docker inspect Memory check; Pattern 1 service inventory; D-09 hybrid mode; Pitfall 1 — hybrid is primary DX)
    - docs/dev-setup.md (initial version from plan 01-01 — complete its placeholder sections)
    - infra/docker-compose.yml (final service inventory — script asserts against it)
  </read_first>
  <action>
    Write `infra/scripts/verify_stack.py` (stdlib only, like reset_target.py): runs `docker compose -f <resolved path> ps --format json`, asserts (1) the default-profile service set is exactly {postgres, redis, api, web, saucedemo} and every one reports Health=healthy; (2) dormant services neo4j/rabbitmq/elasticsearch are ABSENT from ps output (profiles verified by absence — RESEARCH anti-pattern note); (3) for each running container, `docker inspect -f '{{.HostConfig.Memory}}'` is non-zero (Pitfall 3); (4) GET /health on :8000 returns 200 with postgres+redis true, GET :3000 returns 200, GET :8080 returns 200. Print a PASS/FAIL table per check; exit 0 only if all pass.

    Complete `docs/dev-setup.md`: (1) Quick start — the canonical one-command invocation `docker compose -f infra/docker-compose.yml --env-file .env up -d --build --wait`, first-run .env creation from .env.example with the three key-generation one-liners; (2) Hybrid mode (D-09, primary iteration workflow per Pitfall 1) — infra in Docker, `uv run uvicorn app.main:app --reload` and `npm run dev` on the host, same .env, API_URL switch explained; (3) Test workflow — quick (`uv run pytest tests/functional -x -q`), full (compose up --wait + `uv run pytest tests -q`), e2e prerequisites (`playwright install chromium`); (4) reset-target usage and the localStorage honesty note; (5) .wslconfig install steps and Vmmem expectations; (6) verify_stack.py usage as the INFRA-01 evidence command.
  </action>
  <verify>
    <automated>python infra/scripts/verify_stack.py</automated>
  </verify>
  <acceptance_criteria>
    - `python infra/scripts/verify_stack.py` exits 0 against the running stack and prints one PASS line per check group
    - Manually stopping one service (`docker compose stop redis`) then running the script exits non-zero naming redis; restart restores exit 0
    - docs/dev-setup.md contains the canonical compose invocation, a "Hybrid" section, the reset-target command, and the .wslconfig steps (grep: "hybrid", "reset_target", ".wslconfig")
    - verify_stack.py imports only stdlib
  </acceptance_criteria>
  <done>INFRA-01 evidence script live and self-demonstrating; dev workflow fully documented for both run modes.</done>
</task>

<task type="auto">
  <name>Task 2: Clean-state phase gate run</name>
  <files>(no file changes — gate execution; fixes belong to the owning plan's files if anything fails)</files>
  <read_first>
    - .planning/phases/01-foundation-dev-environment/01-VALIDATION.md (Sampling Rate — "Before /gsd:verify-work" clean-state procedure)
  </read_first>
  <action>
    Execute the phase gate exactly as VALIDATION specifies, from repo root:
    1. `docker compose -f infra/docker-compose.yml --env-file .env down -v` (destroys pgdata — proves migrations + seed are fully self-contained per D-08).
    2. `docker compose -f infra/docker-compose.yml --env-file .env up -d --build --wait` — must reach healthy without any manual step.
    3. `cd apps/api && uv run pytest tests -q` — full suite: functional (auth, targets, credential security, reset target) + e2e (login, targets UI) all green against the fresh stack.
    4. `python infra/scripts/verify_stack.py` — exit 0.
    If anything fails, fix in the owning module (the failure belongs to whichever plan created it), re-run the gate from step 1, and record the fix in the SUMMARY.
  </action>
  <verify>
    <automated>docker compose -f infra/docker-compose.yml --env-file .env down -v && docker compose -f infra/docker-compose.yml --env-file .env up -d --build --wait && cd apps/api && uv run pytest tests -q && cd ../.. && python infra/scripts/verify_stack.py</automated>
  </verify>
  <acceptance_criteria>
    - The four-step gate passes end-to-end starting from `down -v` (zero manual interventions between steps)
    - Full pytest suite exit 0 — every VALIDATION Per-Task row green in one run
    - verify_stack.py exit 0 immediately after
  </acceptance_criteria>
  <done>ROADMAP success criterion 1 evidenced from a cold start; phase is mechanically complete pending human sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Host-level verification — WSL memory cap + UI walkthrough</name>
  <what-built>The complete Phase 1 stack: one-command compose (5 services, healthchecks, memory limits), JWT login UI, encrypted target registry (API + UI), self-hosted resettable SauceDemo, full functional/e2e suite green from a clean state. Two checks remain that no script can perform (host-level, per 01-VALIDATION.md Manual-Only table).</what-built>
  <how-to-verify>
    1. WSL memory cap (INFRA-01 / Pitfall 9): close work, run `wsl --shutdown`, restart Docker Desktop, run `docker compose -f infra/docker-compose.yml --env-file .env up -d --wait`. Open Task Manager → Details → Vmmem (or VmmemWSL): its memory should stay bounded near the `memory=` cap in %USERPROFILE%\.wslconfig, not grow toward total RAM. Then `python infra/scripts/verify_stack.py` → exit 0 (stack healthy after WSL restart — "Looks Done But Isn't" check).
    2. UI walkthrough: visit http://localhost:3000 → redirected to /login. Log in with ADMIN_EMAIL/ADMIN_PASSWORD from .env → /targets. Register a target (name: saucedemo, base URL: http://localhost:8080, any credentials, toggle Sandbox on) → row appears with Sandbox and Active badges. Edit it → credential fields masked with helper text. Deactivate → confirm → row goes muted/Inactive. Reactivate it. Log out → back to /login.
    3. Demo target: http://localhost:8080 shows the SauceDemo login page; `python infra/scripts/reset_target.py saucedemo` exits 0.
  </how-to-verify>
  <action>Present the three verification procedures above with exact commands and URLs, then PAUSE for the human to perform them (they are host-level and disruptive — wsl --shutdown restarts Docker). Do not mark the phase complete until approval.</action>
  <done>Human typed "approved" after all three checks pass; failures route back to the owning plan for fixes.</done>
  <resume-signal>Type "approved" if all three pass, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify scripts → docker daemon | evidence scripts run docker subprocesses read-only |
| docs → future operators | documented commands must not embed secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-28 | Information Disclosure | secrets in docs/dev-setup.md examples | mitigate | docs reference variable NAMES and generation one-liners only, never values; acceptance grep on doc content vs .env values |
| T-01-29 | Denial of Service | unbounded WSL2/Vmmem memory on host | mitigate | .wslconfig cap (plan 01-01) + human verification of Vmmem after wsl --shutdown (Task 3) |
| T-01-30 | Repudiation | "works on my machine" phase claims | mitigate | clean-state gate (down -v) + verify_stack.py PASS table recorded in SUMMARY as INFRA-01 evidence |
</threat_model>

<verification>
- Clean-state gate: down -v → up --wait → full pytest suite → verify_stack.py, all green in sequence (VALIDATION rows INFRA-01 → tasks 01-08-T1/T2)
- Human approval of the two Manual-Only verifications + UI walkthrough
</verification>

<success_criteria>
- All 5 ROADMAP Phase 1 success criteria demonstrably TRUE from a cold start
- 01-VALIDATION.md: every Per-Task row green, both manual rows human-confirmed
- docs make the stack reproducible by a future operator without tribal knowledge
</success_criteria>

<output>
Create `.planning/phases/01-foundation-dev-environment/01-08-SUMMARY.md` when done.
</output>
