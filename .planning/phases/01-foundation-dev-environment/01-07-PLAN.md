---
phase: 01-foundation-dev-environment
plan: 07
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - infra/targets/saucedemo/Dockerfile
  - infra/targets/saucedemo/nginx.conf
  - infra/docker-compose.yml
  - infra/scripts/reset_target.py
  - apps/api/tests/functional/test_reset_target.py
autonomous: true
requirements: [QUAL-04]

must_haves:
  truths:
    - "A self-hosted SauceDemo serves its login page at http://localhost:8080 from docker compose up"
    - "python infra/scripts/reset_target.py saucedemo exits 0 and the target is healthy afterwards"
    - "reset_target.py is a generic name→strategy contract a stateful target can plug into at Phase 4 without changing callers"
  artifacts:
    - path: "infra/targets/saucedemo/Dockerfile"
      provides: "Own multi-stage build (upstream Dockerfile unusable): node build → nginx:alpine static serve, pinned upstream SHA"
      contains: "nginx"
    - path: "infra/scripts/reset_target.py"
      provides: "reset-target <name> contract: strategy registry, health-wait, exit codes"
      contains: "STRATEGIES"
    - path: "apps/api/tests/functional/test_reset_target.py"
      provides: "QUAL-04 smoke test (VALIDATION row QUAL-04)"
  key_links:
    - from: "infra/scripts/reset_target.py"
      to: "docker compose restart saucedemo"
      via: "compose-restart strategy via subprocess"
      pattern: "compose"
    - from: "infra/docker-compose.yml"
      to: "infra/targets/saucedemo/Dockerfile"
      via: "saucedemo service build context"
      pattern: "targets/saucedemo"
---

## Phase Goal

**As a** platform operator, **I want to** stand up the platform skeleton locally with one command, log in, and securely register a target application, **so that** the platform has a target to explore in later phases.

(This plan supplies the thing to explore: a self-hosted, resettable SauceDemo demo target — QUAL-04 and D-10.)

<objective>
Self-host SauceDemo in Docker with our own multi-stage build (pinned upstream SHA), add it to the compose default profile, and ship the generic `reset-target <name>` snapshot/restore contract with its functional test.

Purpose: QUAL-04; the reset contract is consumed by Phase 4 (stateful targets) and Phase 7 (reproducibility checks) per CONTEXT integration points.
Output: saucedemo healthy on :8080; reset script + test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-dev-environment/01-RESEARCH.md
@.planning/phases/01-foundation-dev-environment/01-VALIDATION.md
@infra/docker-compose.yml
@apps/api/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SauceDemo image + compose service</name>
  <files>infra/targets/saucedemo/Dockerfile, infra/targets/saucedemo/nginx.conf, infra/docker-compose.yml</files>
  <read_first>
    - .planning/phases/01-foundation-dev-environment/01-RESEARCH.md (Pattern 5 — Dockerfile shape, node:16-bullseye rationale, A1 OpenSSL fallback; Open Question 2 — SHA pinning procedure; Pitfall 9)
    - infra/docker-compose.yml (current state after plan 01-04 — APPEND saucedemo service only)
  </read_first>
  <action>
    Resolve the current master SHA of github.com/saucelabs/sample-app-web (`git ls-remote https://github.com/saucelabs/sample-app-web.git HEAD`) and pin it as the `SAUCEDEMO_SHA` ARG default (Open Question 2 procedure).

    Write `infra/targets/saucedemo/Dockerfile` per RESEARCH Pattern 5: build stage `node:16-bullseye` (upstream webpack predates OpenSSL 3 — assumption A1), git clone + checkout ${SAUCEDEMO_SHA}, `npm ci && npm run build`; serve stage `nginx:alpine` copying `build/` to /usr/share/nginx/html with our nginx.conf. If the build fails with ERR_OSSL_EVP_UNSUPPORTED, add `ENV NODE_OPTIONS=--openssl-legacy-provider` to the build stage (documented A1 fallback).

    Write `infra/targets/saucedemo/nginx.conf`: default server on 80, root /usr/share/nginx/html, `try_files $uri /index.html` SPA fallback (A5 — client routing must survive direct URLs for Phase 4 exploration).

    Append `saucedemo` service to compose: `build: ./targets/saucedemo`, `mem_limit: 128m`, wget-spider healthcheck on localhost:80, ports 8080:80, default profile (no `profiles:` key — it is part of `docker compose up`).

    Build and verify: compose up → healthy; curl localhost:8080 returns the SPA shell; a deep route (e.g., /inventory.html or the SPA path) returns 200 via the fallback, not 404. Honesty note for the SUMMARY (from RESEARCH Pattern 6): SauceDemo state lives in browser localStorage, so restart resets nothing tests see — Playwright's fresh contexts provide real isolation; the contract ships for Phase 4/7 consumers.
  </action>
  <verify>
    <automated>docker compose -f infra/docker-compose.yml --env-file .env up -d --build --wait saucedemo && curl -sf http://localhost:8080/ | grep -qi "<title" && curl -sf -o /dev/null -w "%{http_code}" http://localhost:8080/some/deep/route | grep -q 200</automated>
  </verify>
  <acceptance_criteria>
    - saucedemo container healthy under the canonical compose invocation; `docker inspect -f '{{.HostConfig.Memory}}'` returns 134217728
    - Dockerfile pins a real 40-char commit SHA in the SAUCEDEMO_SHA ARG default (not "master")
    - http://localhost:8080/ serves HTML containing a title tag; an arbitrary deep path returns 200 (SPA fallback works)
    - nginx.conf contains `try_files $uri /index.html`
  </acceptance_criteria>
  <done>Reproducible self-hosted demo target running under the one-command stack at a pinned upstream revision.</done>
</task>

<task type="auto">
  <name>Task 2: Generic reset-target contract + functional test</name>
  <files>infra/scripts/reset_target.py, apps/api/tests/functional/test_reset_target.py</files>
  <read_first>
    - .planning/phases/01-foundation-dev-environment/01-RESEARCH.md (Pattern 6 — contract: perform strategy, wait for health, exit 0/non-zero; STRATEGIES registry shape)
    - apps/api/tests/conftest.py (env-based base URLs pattern)
    - infra/docker-compose.yml (service name and compose-file path the script must target)
  </read_first>
  <action>
    Write `infra/scripts/reset_target.py` (Python, stdlib only — no third-party deps so it runs without uv env; Python entrypoint also dodges CRLF Pitfall 5). Implement RESEARCH Pattern 6:
    - `STRATEGIES = {"saucedemo": {"strategy": "compose-restart", "service": "saucedemo", "health_url": "http://localhost:8080"}}` — registry keyed by target name; Phase 4 adds a `db-snapshot` entry for OrangeHRM without changing the CLI contract (D-10).
    - CLI: `python infra/scripts/reset_target.py <name>`; unknown name → exit 2 with message listing known targets.
    - compose-restart strategy: `subprocess.run(["docker","compose","-f","<repo-relative infra/docker-compose.yml>","restart",service])` (resolve the compose path relative to the script's own location so cwd doesn't matter), then poll health_url with urllib until HTTP 200 or 60s timeout; exit 0 on success, 1 with a diagnostic on timeout/compose failure. Idempotent — safe between every run.

    Write `apps/api/tests/functional/test_reset_target.py` (marker `functional`) per the VALIDATION QUAL-04 row:
    1. `test_saucedemo_serves_200` — GET http://localhost:8080 → 200.
    2. `test_reset_target_exits_zero_and_target_healthy` — run the script via subprocess (`sys.executable`, repo-root-relative path) → returncode 0; immediately GET the health_url → 200.
    3. `test_reset_target_unknown_name_fails` — `reset_target.py nonexistent` → returncode 2 and stderr/stdout names the known targets.
  </action>
  <verify>
    <automated>python infra/scripts/reset_target.py saucedemo && cd apps/api && uv run pytest tests/functional/test_reset_target.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python infra/scripts/reset_target.py saucedemo` exits 0 from repo root AND from any other cwd; target returns 200 right after
    - `python infra/scripts/reset_target.py nonexistent` exits 2 with the known-target list in output
    - reset_target.py imports only stdlib modules (grep its imports — no third-party names)
    - All 3 functional tests pass against the running stack
  </acceptance_criteria>
  <done>QUAL-04 complete: snapshot/restore contract live with its strategy registry, exercised by functional tests, ready for stateful targets in Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → upstream SauceDemo repo → image build | third-party code is built and hosted locally |
| reset script → docker daemon | script executes docker compose subprocesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-25 | Tampering | upstream sample-app-web drift/compromise | mitigate | build pinned to an exact commit SHA (not master); image only serves static files via nginx, no server-side code executes |
| T-01-26 | Elevation of Privilege | reset script command injection via target name | mitigate | name is a dict KEY lookup into STRATEGIES — never interpolated into the subprocess argv; argv built from registry constants as a list (no shell=True) |
| T-01-27 | Denial of Service | saucedemo memory creep | mitigate | mem_limit 128m; nginx static serving only |
</threat_model>

<verification>
- `uv run pytest tests/functional/test_reset_target.py -x -q` green (VALIDATION row QUAL-04 → task 01-07-T2)
- saucedemo healthy inside the default compose profile
</verification>

<success_criteria>
- ROADMAP criterion 5 demonstrable: SauceDemo runs in Docker and resets to clean on demand via the generic contract
- Contract extensible (registry) without touching callers — Phase 4/7 integration points honored
</success_criteria>

<output>
Create `.planning/phases/01-foundation-dev-environment/01-07-SUMMARY.md` when done.
</output>
