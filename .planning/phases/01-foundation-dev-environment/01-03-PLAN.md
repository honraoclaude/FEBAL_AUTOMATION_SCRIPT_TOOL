---
phase: 01-foundation-dev-environment
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/api/app/models/user.py
  - apps/api/app/schemas/auth.py
  - apps/api/app/core/security.py
  - apps/api/app/routers/auth.py
  - apps/api/app/main.py
  - apps/api/alembic/versions/0001_users.py
  - apps/api/alembic/env.py
  - apps/api/tests/conftest.py
  - apps/api/tests/functional/test_auth.py
autonomous: true
requirements: [PLAT-03]

must_haves:
  truths:
    - "User can log in with ADMIN_EMAIL/ADMIN_PASSWORD and receives httpOnly access+refresh cookies"
    - "Wrong password and unknown email both return an identical 401 body (no user enumeration)"
    - "An authenticated request to GET /api/auth/me succeeds using only cookies; without cookies it returns 401"
    - "POST /api/auth/refresh issues a new access cookie; POST /api/auth/logout clears both cookies"
  artifacts:
    - path: "apps/api/app/core/security.py"
      provides: "argon2 hash/verify, JWT mint/decode, cookie set/clear helpers, get_current_user dependency"
      exports: ["create_token", "get_current_user", "hash_password", "verify_password"]
    - path: "apps/api/app/routers/auth.py"
      provides: "POST /api/auth/login, /refresh, /logout; GET /api/auth/me"
    - path: "apps/api/alembic/versions/0001_users.py"
      provides: "users table migration"
    - path: "apps/api/tests/functional/test_auth.py"
      provides: "D-02 functional tests against the live stack (VALIDATION row PLAT-03/functional)"
  key_links:
    - from: "apps/api/app/main.py"
      to: "admin seed"
      via: "lifespan idempotent seed from ADMIN_EMAIL/ADMIN_PASSWORD"
      pattern: "admin"
    - from: "apps/api/app/routers/auth.py"
      to: "users table"
      via: "SQLAlchemy select by email"
      pattern: "select"
---

## Phase Goal

**As a** platform operator, **I want to** stand up the platform skeleton locally with one command, log in, and securely register a target application, **so that** the platform has a target to explore in later phases.

(This plan delivers the "log in" capability at the API layer — after it, a user can authenticate via curl/httpx and hold a session across requests. The walking skeleton's happy path becomes real on the backend.)

<objective>
Implement email/password login with JWT httpOnly-cookie sessions (D-04), env-seeded admin (D-03), and the full D-02 functional test file — tests written FIRST and failing, then made green.

Purpose: PLAT-03. Also extends tests/conftest.py with the authed-client and DB-cleanup fixtures every later plan reuses.
Output: auth endpoints live in the compose stack; `uv run pytest tests/functional/test_auth.py -x` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-dev-environment/01-RESEARCH.md
@.planning/phases/01-foundation-dev-environment/01-VALIDATION.md
@apps/api/app/core/config.py
@apps/api/app/db/session.py
@apps/api/tests/conftest.py

<interfaces>
<!-- Contracts created by plan 01-02 (use as-is) and contracts THIS plan creates for plans 01-04/01-05. -->

Existing (plan 01-02):
```python
from app.core.config import settings           # settings.jwt_secret, .admin_email, .admin_password, .cookie_secure
from app.db.session import get_db, SessionLocal
from app.db.base import Base
```

Created here (consumed by 01-04 e2e and 01-05 targets router):
```python
# app/core/security.py
def hash_password(plain: str) -> str: ...
def verify_password(hashed: str, plain: str) -> bool: ...   # constant-time; False on mismatch
def create_token(sub: str, token_type: str, expires: timedelta) -> str: ...
async def get_current_user(request: Request, db: AsyncSession = Depends(get_db)) -> User: ...

# Endpoints (all JSON):
# POST /api/auth/login    {email, password}  -> 200 {"ok": true} + Set-Cookie access_token, refresh_token | 401 {"detail": "Invalid email or password"}
# POST /api/auth/refresh  (refresh cookie)   -> 200 {"ok": true} + new access cookie | 401
# POST /api/auth/logout                      -> 200 {"ok": true} + both cookies cleared
# GET  /api/auth/me       (access cookie)    -> 200 {"id", "email"} | 401

# tests/conftest.py additions (reused by 01-05/01-06):
# ADMIN_EMAIL/ADMIN_PASSWORD read from env; `authed_client` fixture (logs in, yields cookie-bearing AsyncClient);
# `clean_targets` autouse-able fixture truncating targets table via asyncpg (no-op until table exists)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing functional auth tests (RED)</name>
  <files>apps/api/tests/functional/test_auth.py, apps/api/tests/conftest.py</files>
  <read_first>
    - apps/api/tests/conftest.py (current state from plan 01-02 — EXTEND, do not rewrite; keep API_BASE/client fixture)
    - .planning/phases/01-foundation-dev-environment/01-VALIDATION.md (Per-Task Verification row PLAT-03/functional — the behaviors below are its literal content)
    - .planning/phases/01-foundation-dev-environment/01-RESEARCH.md (Code Example "Functional test shape" — live-stack httpx pattern, no ASGITransport)
  </read_first>
  <action>
    Extend `tests/conftest.py`: read `ADMIN_EMAIL`/`ADMIN_PASSWORD` from env (the same .env values the stack was seeded with — load via os.environ with dotenv fallback to repo-root .env); add `authed_client` fixture that POSTs `/api/auth/login` with admin credentials and yields the cookie-holding `httpx.AsyncClient` (per RESEARCH functional test shape).

    Write `tests/functional/test_auth.py` (marker `functional`) covering exactly the VALIDATION row behaviors, asserting against the LIVE stack at API_BASE:
    1. `test_login_sets_httponly_cookies` — POST /api/auth/login with admin creds → 200; Set-Cookie headers for `access_token` and `refresh_token` both contain `HttpOnly` and `SameSite=lax` (case-insensitive header inspection); response body never contains the password value.
    2. `test_login_wrong_password_401` and `test_login_unknown_email_401` — both → 401 and their JSON bodies are byte-identical (uniform error, no enumeration).
    3. `test_me_requires_auth` — GET /api/auth/me with no cookies → 401; with authed_client → 200 and `email == ADMIN_EMAIL`.
    4. `test_refresh_rotates_access` — authed client POST /api/auth/refresh → 200 and a NEW `access_token` Set-Cookie is issued (value differs from the login one).
    5. `test_logout_clears_cookies` — POST /api/auth/logout → Set-Cookie headers expiring both cookies (Max-Age=0 or expires in the past); subsequent GET /api/auth/me with that client → 401.
    6. `test_refresh_token_rejected_as_access` — craft a request sending the refresh token value in the `access_token` cookie → GET /api/auth/me returns 401 (the `type` claim gate works).

    Run the suite against the running stack and confirm ALL tests FAIL with 404 (endpoints don't exist yet). Commit the red state: `test(01-03): add failing functional tests for auth slice`.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_auth.py -q; test $? -ne 0 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - test_auth.py contains the 6 named test functions and zero mocks/ASGITransport imports (grep `ASGITransport` returns nothing)
    - `uv run pytest tests/functional/test_auth.py` exits non-zero with failures caused by 404 responses (not import/fixture errors)
    - conftest.py still exposes the original `client` fixture plus new `authed_client`
  </acceptance_criteria>
  <done>Red suite committed; behaviors pinned before implementation (D-02 + MVP failing-e2e-first rule).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auth — user model, migration, seed, security core, router (GREEN)</name>
  <files>apps/api/app/models/user.py, apps/api/app/schemas/auth.py, apps/api/app/core/security.py, apps/api/app/routers/auth.py, apps/api/app/main.py, apps/api/alembic/versions/0001_users.py, apps/api/alembic/env.py</files>
  <read_first>
    - apps/api/tests/functional/test_auth.py (from Task 1 — the spec to satisfy)
    - .planning/phases/01-foundation-dev-environment/01-RESEARCH.md (Pattern 2 — login/get_current_user code, cookie flags, CSRF posture; Pitfall 6 cookie flags; Pitfall 7 idempotent seed; Security Domain V2/V3 + timing-enumeration row)
    - apps/api/app/main.py (lifespan extension point comment from plan 01-02)
    - apps/api/alembic/env.py (add user model import for autogenerate)
  </read_first>
  <behavior>
    - All 6 Task-1 tests pass against the live stack
    - Access token: HS256, claims {sub: user.id, type: "access", iat, exp=+30min}; refresh: type "refresh", exp=+7d, cookie path "/api/auth"
    - Unknown-user login path still performs one argon2 verify against a module-level dummy hash (timing-enumeration mitigation)
  </behavior>
  <action>
    Implement per RESEARCH Pattern 2, using the exact names in the `<interfaces>` block:
    - `app/models/user.py`: `User(Base)` — `id` (int PK), `email` (str, unique, indexed), `password_hash` (str), `created_at` (server default now). Import it in `alembic/env.py`, generate migration `uv run alembic revision --autogenerate -m "users"` → ensure the version file creates the users table + unique index (rename/curate file to `0001_users.py` style if desired, keeping alembic revision IDs intact).
    - `app/core/security.py`: argon2 `PasswordHasher()` (default argon2id params — OWASP posture); `hash_password`/`verify_password` (catch `VerifyMismatchError` → False); module-level `DUMMY_HASH = ph.hash("dummy-timing-pad")`; `create_token(sub, token_type, expires)` per RESEARCH (PyJWT HS256, claims sub/type/iat/exp, key=settings.jwt_secret); `decode_token(token) -> dict` raising HTTPException(401) on any PyJWTError; cookie helpers `set_auth_cookies(response, access, refresh)` / `clear_auth_cookies(response)` with httponly=True, samesite="lax", secure=settings.cookie_secure, access max_age=1800 path="/", refresh max_age=604800 path="/api/auth" (Pitfall 6: COOKIE_SECURE=false on plain-http localhost); `get_current_user` dependency reading the `access_token` cookie, enforcing `payload["type"] == "access"`, loading the User or 401.
    - `app/schemas/auth.py`: `LoginRequest{email: EmailStr, password: str}`, `MeResponse{id: int, email: str}`.
    - `app/routers/auth.py` (prefix `/api/auth`): `POST /login` — fetch user by email; if absent, run `verify_password(DUMMY_HASH, body.password)` then raise the SAME HTTPException(401, "Invalid email or password") used for wrong-password (uniform body); on success mint both tokens and set cookies, return {"ok": true}. `POST /refresh` — read refresh cookie, require type=="refresh", mint+set new access cookie. `POST /logout` — clear both cookies (client-side invalidation only per D-04; no denylist). `GET /me` — Depends(get_current_user) → MeResponse.
    - `app/main.py`: include auth router; implement the lifespan seed extension point: idempotent admin seed — inside a transaction, select user by settings.admin_email; create with hash_password(settings.admin_password) only if absent (Pitfall 7; single-worker dev so no race).

    Apply migration to the running stack: restart the api container (its entrypoint runs `alembic upgrade head` and lifespan seeds the admin) via `docker compose -f infra/docker-compose.yml --env-file .env up -d --build api` — or in hybrid mode run alembic + uvicorn on the host. Then run the suite green. Commit: `feat(01-03): implement JWT cookie auth with seeded admin`.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_auth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 6 functional tests pass against the live compose stack (exit 0)
    - `docker compose logs api` after a restart shows exactly one admin user ever created (restart twice; second run logs no insert / no crash — idempotent seed)
    - Login response body and headers contain no password hash and no JWT secret (assert in test 1 already; spot-check logs show `[REDACTED]` for any password field)
    - `python -c` decode of an issued access token shows claims sub/type/iat/exp with type=="access" and ~30-min expiry
  </acceptance_criteria>
  <done>PLAT-03 API-side complete: login/refresh/logout/me live, admin seeded from env, all VALIDATION PLAT-03 functional behaviors green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client → /api/auth/* | untrusted credentials and cookies cross here |
| JWT secret (api env) → tokens | only the API tier ever holds the signing key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing (V2) | login endpoint | mitigate | argon2id via argon2-cffi; uniform 401 body for unknown-user vs wrong-password (tests assert byte-identical) |
| T-01-08 | Information Disclosure (V2) | timing-based user enumeration | mitigate | dummy-hash argon2 verify on unknown-user path (RESEARCH Security Domain row) |
| T-01-09 | Elevation of Privilege (V3) | refresh token used as access token | mitigate | `type` claim enforced in get_current_user; test_refresh_token_rejected_as_access |
| T-01-10 | Information Disclosure (V3) | XSS stealing tokens | mitigate | httpOnly cookies (JS-unreadable); React default escaping in web tier |
| T-01-11 | Tampering (V3) | CSRF on state-changing auth routes | mitigate | SameSite=Lax + JSON-only bodies + same-origin rewrites (RESEARCH A3 — accepted localhost posture; revisit before network exposure) |
| T-01-12 | Repudiation | no server-side revocation after logout | accept | D-04 locks client-side logout for Phase 1; Redis denylist arrives with RBAC (Phase 10) |
</threat_model>

<verification>
- `uv run pytest tests/functional/test_auth.py -x -q` green against `docker compose up` stack (VALIDATION row PLAT-03/functional → tasks 01-03-T1/T2)
- Restart api container twice → still exactly one admin row
</verification>

<success_criteria>
- All 5 ROADMAP criterion-2 behaviors (login, persistence via JWT cookies) demonstrable with curl/httpx
- Uniform-401 and refresh-type-gate security behaviors test-pinned
- conftest authed_client + cleanup fixtures available for plans 01-05/01-06
</success_criteria>

<output>
Create `.planning/phases/01-foundation-dev-environment/01-03-SUMMARY.md` when done.
</output>
