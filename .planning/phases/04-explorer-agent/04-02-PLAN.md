---
phase: 04-explorer-agent
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/api/app/services/explorer/fingerprint.py
  - apps/api/app/services/explorer/auth.py
  - apps/api/app/services/explorer/nodes.py
  - apps/api/app/services/explorer/state.py
  - apps/api/tests/unit/test_fingerprint.py
  - apps/api/tests/unit/test_convergence.py
  - apps/api/tests/unit/test_auth_detect.py
  - apps/api/tests/unit/conftest.py
  - apps/api/tests/fixtures/aria/__init__.py
autonomous: true
requirements: [EXPL-06, EXPL-05, EXPL-02]

must_haves:
  truths:
    - "A pure, tunable structural-skeleton fingerprint collapses revisits of the same logical screen and distinguishes template (product list) from instance data (which products), replacing the temporary URL key"
    - "Two consecutive runs over fixed fixture snapshots (mocked gateway) converge to an identical fingerprint set and stop with stop_reason=saturation — proven deterministically, zero spend"
    - "The explorer auto-detects a login form heuristically (password input + nearby text input + submit), injects credentials via the single decrypt surface, captures storageState for reuse, and re-logs-in if logged out mid-run"
  artifacts:
    - path: "apps/api/app/services/explorer/fingerprint.py"
      provides: "structural_fingerprint(tree, cfg) -> str + FingerprintConfig (tunable) behind a swappable interface"
      contains: "structural_fingerprint"
    - path: "apps/api/app/services/explorer/auth.py"
      provides: "login-form detection + credential injection + storageState capture/reuse + relogin recovery"
      contains: "get_decrypted_credentials"
    - path: "apps/api/tests/unit/test_convergence.py"
      provides: "two-run determinism: identical fingerprint set + stop_reason=saturation"
      contains: "saturation"
  key_links:
    - from: "apps/api/app/services/explorer/nodes.py"
      to: "apps/api/app/services/explorer/fingerprint.py"
      via: "converge node fingerprints the current state (replaces the Slice-1 URL key)"
      pattern: "structural_fingerprint"
    - from: "apps/api/app/services/explorer/auth.py"
      to: "target_service.get_decrypted_credentials"
      via: "single decrypt surface — creds never logged, never on a node"
      pattern: "get_decrypted_credentials"
---

<objective>
Replace Slice 1's temporary URL dedup key with the experimental structural-skeleton DOM fingerprint (EXPL-06) — pure, tunable, unit-tested for template-vs-instance separation — and wire it into the converge node so saturation-based convergence works. Prove convergence DETERMINISTICALLY: two consecutive runs over fixed fixture snapshots with a mocked gateway yield an identical fingerprint set and stop_reason=saturation (the EXPL-05 convergence half). Add real auth handling (EXPL-02): heuristic login-form detection, credential injection via the single decrypt surface, Playwright storageState capture/reuse, and logout→relogin recovery mid-run.

Purpose: Make exploration converge (not just halt on budget) and log in to any target without hardcoded SauceDemo selectors — the two capabilities that distinguish a real Explorer from the tracer.
Output: `fingerprint.py` (the isolated experimental module behind a swappable interface), `auth.py`, the converge/navigate node wiring, and three unit test files including the two-run convergence harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-explorer-agent/04-CONTEXT.md
@.planning/phases/04-explorer-agent/04-RESEARCH.md
@.planning/phases/04-explorer-agent/04-PATTERNS.md
@.planning/phases/04-explorer-agent/04-01-SUMMARY.md

<interfaces>
From apps/api/app/services/explorer/nodes.py (Slice 1) — the converge node currently dedups on the temporary URL key (`# TEMP: replaced by structural_fingerprint in Slice 2`). Slice 2 swaps in the fingerprint.
From apps/api/app/services/explorer/budget.py (Slice 1) — ExploreBudget.saturation_window + is_loop already exist; this slice drives saturation off the fingerprint set.
From apps/api/app/services/explorer.py — the Phase-3 login block to generalize:
```
user, password = await target_service.get_decrypted_credentials(db, target_id)   # single decrypt surface
await page.fill("#user-name", user); await page.fill("#password", password); await page.click("#login-button")
```
From tests/unit/conftest.py — fake_gateway (added in Slice 1) returns scripted action indices; reuse for the deterministic two-run convergence test.
SauceDemo note: uses `data-test` (not `data-testid`); login form has input#user-name, input#password (type=password), input#login-button (submit).
</interfaces>

<fingerprint_reference>
RESEARCH "Fingerprint Normalization" (lines 271-295): Candidate A structural-skeleton SHA-256 over a normalized DOM/aria skeleton — keep tag/role + structural ARIA attrs + landmark/heading structure; STRIP text, id/dynamic attr values, numbers, href query strings, known dynamic id patterns. Sibling-subtree folding ON by default (this is what separates template from instance). Tunables in FingerprintConfig: max_depth, kept_attrs, fold_siblings, strip_text. Keep Candidate B (SimHash) as a documented upgrade path behind the same `fingerprint(state) -> str` interface.
</fingerprint_reference>

<auth_reference>
RESEARCH "Auth Handling" (lines 316-321): login heuristic = input[type=password] + nearby text/email input + a submit control. Credential injection ONLY via get_decrypted_credentials. storageState: `state = await context.storage_state()` → persist to workspaces/<run_id>/storage_state.json → reuse via browser.new_context(storage_state=path). Logout detection: a navigation lands back on the login form (password input reappears) mid-run → re-run the login flow. Keep SauceDemo selectors as a fast path but generalize to the heuristic.
</auth_reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Structural fingerprint module (the experimental unknown) + swap into the converge node</name>
  <read_first>
    - .planning/phases/04-explorer-agent/04-RESEARCH.md "Fingerprint Normalization" (lines 271-295) + Assumptions A3 (line 464)
    - .planning/phases/04-explorer-agent/04-PATTERNS.md "fingerprint.py" (lines 169-181) — closest analog is explorer._page_key (the URL key it replaces)
    - apps/api/app/services/explorer/nodes.py — the converge node + the TEMP URL-key marker from Slice 1
    - apps/api/app/services/explorer/budget.py — saturation_window field to drive
  </read_first>
  <behavior>
    - Test 1 (template equality): a "product list with 6 items" tree and a "product list with 4 items" tree (same skeleton, different instance count) hash IDENTICALLY when fold_siblings=True.
    - Test 2 (instance collapse): two renders of the same screen with different text/ids hash identically (text + dynamic ids stripped).
    - Test 3 (layout difference): a structurally different page (different landmark/heading skeleton) hashes DIFFERENTLY.
    - Test 4 (tunable): with fold_siblings=False the 6-item vs 4-item trees hash differently (config actually changes behavior).
  </behavior>
  <action>
    Create explorer/fingerprint.py: a `FingerprintConfig` dataclass (max_depth: int=12, kept_attrs: frozenset (e.g. role + structural aria-*), fold_siblings: bool=True, strip_text: bool=True) and a PURE `structural_fingerprint(tree, cfg: FingerprintConfig) -> str` that walks the tree depth-first, keeps `role-or-tag` + sorted kept_attrs, strips text/id/dynamic-attr values/numbers/href query strings, folds repeated sibling subtrees when fold_siblings (collapse N identical siblings to one + a count-agnostic marker — this is the template-vs-instance separator), serializes depth:role:attrs lines, and SHA-256s the join. Expose the seam as a module-level `fingerprint(tree, cfg=DEFAULT_CONFIG) -> str` so Candidate B (SimHash) can drop in behind the same signature later (document the upgrade path in a docstring).
    Provide a small adapter that turns a Playwright page (or the aria_snapshot tree / a DOM walk) into the normalized node tree the function consumes — keep parsing separate from hashing so the hash is unit-testable on plain fixture trees (no browser).
    Swap the converge node in nodes.py: replace the `# TEMP` URL key with `fingerprint(...)`; maintain `seen_keys[fp] = {first_step, visits}`, reset `steps_since_new` to 0 on a new fp, increment otherwise, and set stop_reason="saturation" when steps_since_new reaches budget.saturation_window.
    Add tests/unit/test_fingerprint.py covering the four behaviors on hand-built fixture trees (pure — no browser, no LLM, no spend). Add a tests/fixtures/aria/ package holding the fixture trees (and aria_snapshot YAML samples) reused by the convergence test in Task 2.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest -m "not live_llm and not graph" tests/unit/test_fingerprint.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def structural_fingerprint\|def fingerprint\|class FingerprintConfig" apps/api/app/services/explorer/fingerprint.py` all present; the function is pure (no playwright/llm/db import in the hashing path).
    - `grep -n "fingerprint(" apps/api/app/services/explorer/nodes.py` present in the converge node; the Slice-1 `# TEMP` URL-key marker is gone.
    - `grep -n "saturation" apps/api/app/services/explorer/nodes.py` sets stop_reason at saturation_window.
    - test_fingerprint.py: template-equality + instance-collapse + layout-difference + config-tunability all pass.
  </acceptance_criteria>
  <done>The experimental fingerprint is an isolated, pure, tunable, unit-tested module separating template from instance; the converge node dedups by fingerprint and saturates — EXPL-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-run convergence harness (deterministic, mocked gateway, zero spend)</name>
  <read_first>
    - .planning/phases/04-explorer-agent/04-RESEARCH.md "Convergence + Budgets" (lines 340-355, esp. "Proving convergence in a test") + Test Map EXPL-05 (line 514)
    - apps/api/tests/unit/conftest.py — fake_gateway (scripted indices) from Slice 1
    - apps/api/app/services/explorer/graph.py — build_explorer_graph to invoke against fixtures with the mocked gateway
    - tests/fixtures/aria/ — fixture trees from Task 1
  </read_first>
  <behavior>
    - Test 1 (two-run convergence): drive the explorer loop twice over the SAME fixed fixture snapshots with the fake_gateway returning deterministic indices; assert run 2 adds ~0 new fingerprints (the fingerprint set is identical across the two runs) AND both runs end with stop_reason=="saturation".
    - Test 2 (budget backstop): with a non-saturating fixture set, the loop still halts with a cap stop_reason (never infinite).
  </behavior>
  <action>
    Add tests/unit/test_convergence.py: a deterministic harness that feeds the graph fixed fixture snapshots (no live browser) via a perception/enumeration seam patched to return the fixtures, and the fake_gateway returning scripted action indices. Run the loop twice; collect the fingerprint set from each run; assert set equality and stop_reason=="saturation" on both. Add the non-saturating backstop case asserting a cap stop_reason. Zero spend (gateway mocked), no graph_mode, no browser. This is the EXPL-05 deterministic convergence proof referenced in the VALIDATION map.
    If the graph cannot be driven in-process without a browser, factor a thin `run_over_fixtures(snapshots, gateway, budget) -> ExplorerState` helper out of the node logic so the convergence proof exercises the SAME fingerprint/saturation/budget code paths the live loop uses (do not duplicate the logic).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest -m "not live_llm and not graph" tests/unit/test_convergence.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_convergence.py runs with NO provider key and NO graph_mode (pure unit) and passes.
    - `grep -n "saturation" apps/api/tests/unit/test_convergence.py` asserts stop_reason; the two-run fingerprint-set equality assertion is present.
    - The harness exercises the real fingerprint + saturation + budget code (not a reimplementation) — verified by importing from explorer.fingerprint / explorer.budget.
  </acceptance_criteria>
  <done>Convergence is proven deterministically: two runs collapse to the same fingerprinted graph and stop on saturation, with zero spend — the EXPL-05 convergence guarantee.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Auth handling — login detection, storageState reuse, relogin recovery</name>
  <read_first>
    - .planning/phases/04-explorer-agent/04-RESEARCH.md "Auth Handling" (lines 316-321) + Assumptions A7 (line 468)
    - .planning/phases/04-explorer-agent/04-PATTERNS.md "auth.py" (lines 201-209) — REUSE the decrypt surface, NET-NEW storageState/relogin
    - apps/api/app/services/explorer.py — the Phase-3 SauceDemo login block to generalize
    - apps/api/app/services/explorer/nodes.py — the navigate/perceive nodes where the relogin guard hooks in
  </read_first>
  <behavior>
    - Test 1 (detection, unit): given a DOM/aria fixture with input[type=password] + a nearby text input + a submit control, detect_login_form returns the field locators; given a fixture with no password input, it returns None (not a login page).
    - Test 2 (logout detection, unit): given a fixture where the password input reappears mid-run, needs_relogin returns True.
  </behavior>
  <action>
    Create explorer/auth.py: `detect_login_form(snapshot_or_handles) -> LoginForm | None` (heuristic: input[type=password] + nearest preceding text/email input + a submit button/[type=submit]; keep SauceDemo's #user-name/#password/#login-button as a fast path but fall through to the heuristic). `async def perform_login(page, user, password, form) -> None` filling the detected fields and submitting. `async def capture_storage_state(context, run_id) -> str` → `await context.storage_state(path=workspaces/<run_id>/storage_state.json)` returning the path. `async def load_storage_state_path(run_id) -> str | None` for reuse on new contexts. `needs_relogin(snapshot_or_handles) -> bool` (password input reappeared → logged out).
    Wire auth into explorer.py / nodes.py: at run start, if a login form is detected and not yet authenticated, get creds via `target_service.get_decrypted_credentials(db, target_id)` (THE single decrypt surface — never log, never write to a node), perform_login, then capture_storage_state for reuse; in the navigate/perceive path, if needs_relogin() is True mid-run, re-run perform_login with the same creds and continue (a node-level guard).
    Add tests/unit/test_auth_detect.py covering detection present/absent + relogin detection on fixtures (pure; no browser, no creds, no spend).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest -m "not live_llm and not graph" tests/unit/test_auth_detect.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "get_decrypted_credentials" apps/api/app/services/explorer/auth.py` is the ONLY decrypt path; `grep -rn "decrypt\|Fernet" apps/api/app/services/explorer/` shows no second decrypt surface.
    - `grep -n "def detect_login_form\|def needs_relogin\|storage_state" apps/api/app/services/explorer/auth.py` all present.
    - No credential value is ever passed to a logger or a Neo4j write (review nodes.py persist params — creds absent).
    - test_auth_detect.py passes: detection present/absent + relogin trigger.
  </acceptance_criteria>
  <done>The explorer logs in heuristically (no hardcoded selectors required), reuses storageState, and recovers from mid-run logout — EXPL-02, with creds confined to the single decrypt surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target creds → auth.py | decrypted credentials used to fill the login form |
| storageState file → workspaces/<run_id>/ | a session token blob persisted to the gitignored workspaces tree |
| page content → fingerprint | page-derived structure normalized into a hash (text stripped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Info Disclosure | target creds in auth.py | mitigate | Creds ONLY via get_decrypted_credentials (single decrypt surface); never logged, never written to a node or fingerprint; grep-gated |
| T-04-08 | Info Disclosure | storage_state.json (session token) | mitigate | Written under the gitignored workspaces/<run_id>/ tree (per-run, not platform session); path is run_id-derived, never client input |
| T-04-09 | Tampering | fingerprint over page-derived strings | mitigate | Fingerprint strips text/ids and hashes only structure; it is never used as a Cypher value without parameterization |
| T-04-10 | DoS | non-converging app | mitigate | Saturation + budget caps from Slice 1 guarantee halt even when the fingerprint never saturates |
</threat_model>

<verification>
- Full unit suite green, zero spend: `cd apps/api && uv run pytest -m "not live_llm and not graph" -q` (fingerprint, convergence, auth-detect all pure).
- Convergence proof runs with no provider key and no graph_mode and passes (two-run fingerprint-set equality + saturation).
- Optional graph_mode re-run of test_explore_discovery.py confirms fingerprint dedup did not break live discovery.
</verification>

<success_criteria>
- The structural fingerprint collapses revisits and separates template from instance, is tunable, and is the converge node's dedup key.
- Two consecutive runs converge to the same fingerprint set and stop on saturation — proven deterministically with zero spend.
- Auto-login works heuristically with storageState reuse + relogin recovery; creds stay on the single decrypt surface.
</success_criteria>

<output>
Create `.planning/phases/04-explorer-agent/04-02-SUMMARY.md` when done
</output>
