- **[02-03, environmental]** Host memory pressure: `docker compose exec api uv run python` (functional ledger driver) intermittently fails with `OSError: [Errno 12] Cannot allocate memory` when the `web` container holds ~1.3GiB on the 3GB-capped WSL host. Workaround: stop `web` for in-container driver tests, or raise the WSL memory cap. Not a code defect — the gateway import fails before any logic runs. Out of scope for this plan.
