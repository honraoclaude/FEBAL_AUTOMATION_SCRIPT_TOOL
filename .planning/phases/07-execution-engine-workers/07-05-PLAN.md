---
phase: 07-execution-engine-workers
plan: 05
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .github/workflows/run-suite.yml
  - apps/api/app/core/config.py
  - apps/api/tests/unit/test_ci_workflow_contract.py
  - apps/api/tests/functional/test_determinism.py
autonomous: true
requirements: [EXEC-02]
user_setup:
  - service: github-actions
    why: "CI trigger reaches the platform API to start a tier run and poll status (EXEC-02 / D-08)"
    env_vars:
      - name: CI_TOKEN
        source: "A scoped JWT/service-account token minted by the platform that can ONLY start runs + read status (store as a GitHub Actions secret)"
      - name: PLATFORM_API_URL
        source: "The reachable URL of the platform API (a self-hosted runner on the dev box, or a tunnel — host port 8001 is local)"
    dashboard_config:
      - task: "Register a self-hosted GitHub Actions runner on the dev box OR configure a tunnel so the workflow can reach the local API"
        location: "GitHub repo → Settings → Actions → Runners"

must_haves:
  truths:
    - "A GitHub Actions workflow starts a tier run via the platform API and polls run status back, mapping passed→exit 0 and failed→exit 1 (the same engine code path, never pytest directly)"
    - "Two consecutive runs of the same planted spec against a reset SauceDemo target produce identical results (status + per-test verdicts), proven keyless"
    - "The CI token is scoped to start+poll only and is never echoed in workflow logs"
  artifacts:
    - path: ".github/workflows/run-suite.yml"
      provides: "workflow_dispatch CI trigger calling the API + polling status"
      contains: "workflow_dispatch"
    - path: "apps/api/tests/functional/test_determinism.py"
      provides: "two-runs-identical proof against a reset target (planted spec, keyless)"
      contains: "reset"
  key_links:
    - from: ".github/workflows/run-suite.yml"
      to: "POST /api/executions + GET /api/executions/{run_id}"
      via: "curl with the scoped CI_TOKEN bearer"
      pattern: "api/executions"
    - from: "apps/api/tests/functional/test_determinism.py"
      to: "infra/scripts/reset_target.py + the planted spec"
      via: "reset between runs, compare on status + verdict not timing"
      pattern: "reset_target|reset"
---

<objective>
Deliver local/Docker/CI execution parity (EXEC-02): a GitHub Actions workflow that triggers a tier run through the SAME platform API engine and reports status back, plus the determinism harness proving two consecutive runs against a reset target produce identical results. Both proven without a second code path and (for determinism) without provider keys via the Phase-6 planted-spec trick.

Purpose: SC2 (same suite local/Docker/CI) and SC5 (two runs identical). CI never runs pytest directly — it calls the engine — so local/Docker/CI cannot diverge (D-08).
Output: .github/workflows/run-suite.yml; a scoped CI token contract (config + frontmatter user_setup); the determinism functional test reusing reset_target.py; a keyless workflow-contract unit test (yaml parse + exit→conclusion mapping). (NOTE: the actual ci_token bearer is WIRED into the start/poll routes in Plan 03 (B1/I1, executions.py); this plan only DEFINES the config setting + documents the scope.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-execution-engine-workers/07-CONTEXT.md
@.planning/phases/07-execution-engine-workers/07-RESEARCH.md
@.planning/phases/07-execution-engine-workers/07-PATTERNS.md
@.planning/phases/07-execution-engine-workers/07-01-SUMMARY.md
@.planning/phases/06-bdd-playwright-generation/06-04-SUMMARY.md

<interfaces>
From apps/api/app/services/stability.py:
- `_run_spec_once(spec_path, *, base_url=None)` returns `{passed, exit_code, output}` (TARGET_BASE_URL override) — the determinism harness reuses this to run the planted spec twice
- `run_stability(spec_path, *, runs=None)` — the N-run isolated-subprocess shape
From infra/scripts/reset_target.py:
- the generic `name → strategy` reset contract with an exit-code contract (e.g. `reset_target.py saucedemo`) — Phase 1; reset between the two determinism runs
From 06-04-SUMMARY.md:
- the planted spec = the retained test_login.py.j2 rendered with fixed SauceDemo slots, TARGET_BASE_URL-overridable; host-published ports (8080 std)
From apps/api/app/core/config.py:
- ADD `ci_token: str | None` (env CI_TOKEN) — the scoped start+poll credential (the route-level bearer check is in Plan 03 executions.py, I1)
From .planning/phases/07-execution-engine-workers/07-RESEARCH.md:
- the GitHub Actions trigger yaml shape (workflow_dispatch → curl POST /api/executions → poll GET /api/executions/{run_id} → exit 0/1); reachability assumption A5 (self-hosted runner / tunnel)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions trigger workflow + scoped CI token contract + contract unit test</name>
  <read_first>
    - .planning/phases/07-execution-engine-workers/07-RESEARCH.md ("GitHub Actions trigger" code block + Pitfall 7 token discipline + A5 reachability + Security Domain V2 scoped token)
    - .planning/phases/07-execution-engine-workers/07-PATTERNS.md (".github/workflows/run-suite.yml" NET-NEW entry — no .github exists)
    - apps/api/app/core/config.py (the Settings class + how secrets are declared, e.g. anthropic_api_key None default)
    - apps/api/app/routers/auth.py (how the platform mints/validates JWTs — to define the scoped CI token shape; the route-level enforcement lives in Plan 03 executions.py)
  </read_first>
  <action>
    Create `.github/workflows/run-suite.yml` (the directory does not exist — net-new CI surface) per RESEARCH: `on: workflow_dispatch` with a `tier` input (default smoke); a `run` job that (1) POSTs `{"tier": <input>}` to `$PLATFORM_API_URL/api/executions` with `Authorization: Bearer $CI_TOKEN`, captures `run_id` via jq into `$GITHUB_OUTPUT`; (2) polls `GET $PLATFORM_API_URL/api/executions/{run_id}` until status is terminal — `passed` → `exit 0`, `failed`/`killed` → `exit 1`, timeout → `exit 1`. `PLATFORM_API_URL` and `CI_TOKEN` come from `secrets`, NEVER echoed (Pitfall 7). Add a header comment documenting the A5 reachability assumption (self-hosted runner on the dev box OR a tunnel; host port 8001 is local) and the scoped-token requirement (start+poll only; the route-level bearer check is in Plan 03 executions.py). Add `ci_token: str | None = None` (env CI_TOKEN) to `app/core/config.py` Settings as the scoped start+poll credential (default None so the api boots without it). Create `tests/unit/test_ci_workflow_contract.py` (keyless): parse the workflow yaml (PyYAML/`yaml.safe_load`), assert it has `workflow_dispatch` with the `tier` input, calls `/api/executions` (POST) and the status poll (GET /api/executions/{run_id}), references `CI_TOKEN`/`PLATFORM_API_URL` via `secrets`, never inlines a literal token, and that the documented mapping is `passed→0` / `failed→1`.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_ci_workflow_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/run-suite.yml` exists, parses as valid yaml, has workflow_dispatch + tier input.
    - The workflow POSTs to /api/executions and polls GET /api/executions/{run_id}; uses `secrets.CI_TOKEN` + `secrets.PLATFORM_API_URL`; no literal token string anywhere.
    - `grep -n "echo .*CI_TOKEN\|echo .*token" .github/workflows/run-suite.yml` finds nothing (token never echoed).
    - test_ci_workflow_contract.py passes (parses the yaml + asserts the start/poll/exit-mapping contract).
    - `ci_token` exists in Settings (env CI_TOKEN), default None.
  </acceptance_criteria>
  <done>The CI workflow triggers a tier run via the API and polls status back with a scoped, never-echoed token; the contract is asserted keyless by a unit test. (The route-level bearer enforcement is delivered in Plan 03.)</done>
</task>

<task type="auto">
  <name>Task 2: Determinism harness — two runs vs a reset target identical (planted spec, keyless)</name>
  <read_first>
    - apps/api/app/services/stability.py (`_run_spec_once`, `run_stability` — the isolated-subprocess shape + TARGET_BASE_URL override)
    - apps/api/tests/functional/test_stability.py + test_seeded_bug.py (the planted-spec host-driver pattern, host-published ports 8080)
    - infra/scripts/reset_target.py (the name→strategy reset contract + exit-code contract)
    - .planning/phases/07-execution-engine-workers/07-RESEARCH.md (Pitfall 6 — compare on STATUS + flaky verdict NOT timing/timestamps; reset between runs; planted-spec keyless trick)
    - .planning/phases/06-bdd-playwright-generation/06-04-SUMMARY.md (the planted spec rendered with fixed slots, TARGET_BASE_URL-overridable)
  </read_first>
  <action>
    Create `tests/functional/test_determinism.py` (marker functional; keyless) proving SC5: render the SAME planted spec used by the Phase-6 harness (the retained test_login.py.j2 with fixed SauceDemo slots, TARGET_BASE_URL-overridable, host-driver pattern pointing at host port 8080), run it TWICE via `_run_spec_once` (or run_stability with runs=1 twice), calling `reset_target.py saucedemo` between the two runs (subprocess, honoring its exit-code contract) so target state is identical. Assert the two runs are IDENTICAL on the comparable surface — exit_code/passed and the derived verdict — NOT on timing/timestamps/durations (Pitfall 6: timestamps and durations are expected to differ; never compare them). Document in the test docstring the D-03b/Phase-6 sequencing (neo4j OFF during the run phase; the planted spec needs no graph and no keys) and the SauceDemo localStorage isolation note (fresh Playwright contexts already isolate runs; reset_target.py is the explicit reset). Keep the subprocess discipline (argv list, no shell) — reuse stability's helpers verbatim, do not re-implement the runner.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_determinism.py -m functional -q</automated>
  </verify>
  <acceptance_criteria>
    - test_determinism.py runs the planted spec twice with a reset_target.py call between, and asserts identical status/verdict across the two runs.
    - The comparison explicitly EXCLUDES timing/timestamps/durations (grep the test: it compares exit_code/verdict, not duration).
    - The test is keyless (no provider key) and uses the planted spec — `grep -n "reset_target\|reset" tests/functional/test_determinism.py` finds the reset call.
    - `grep -n "create_subprocess_exec\|shell=True" tests/functional/test_determinism.py` shows it reuses stability's helpers (no new shell-based runner).
  </acceptance_criteria>
  <done>Two consecutive runs of the planted spec against a reset SauceDemo produce identical results (status + verdict, not timing), proven keyless.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → platform API | an external CI runner authenticates to start runs + read status |
| CI secret store → workflow env | the scoped token crosses into the runner environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Info Disclosure | CI_TOKEN leaking in workflow logs | mitigate | token passed via GitHub secret, NEVER echoed (Pitfall 7); contract unit test asserts no echo/literal token in the yaml |
| T-07-08 | Spoofing/Elevation | over-scoped CI credential | mitigate | the CI token is scoped to start-run + read-status only (documented in config + workflow header; route-level bearer check in Plan 03); mirror PLAT-07 credential discipline; full RBAC is Phase 10 |
| T-07-09 | Spoofing | unauthenticated CI call to the engine | mitigate | the start/poll routes are Depends(get_current_user)-gated on executions.py (Plan 03); the CI token is a valid bearer for exactly those two routes (wired in Plan 03) |
| T-07-06 | Tampering/DoS | non-determinism breaking the two-runs proof | mitigate | reset_target.py between runs; compare on status/verdict not timing; planted spec is deterministic + keyless (Pitfall 6) |
</threat_model>

<verification>
- `cd apps/api && uv run pytest tests/unit/test_ci_workflow_contract.py -q` green.
- `cd apps/api && uv run pytest tests/functional/test_determinism.py -m functional -q` green (queue profile not required; saucedemo on 8080 up, neo4j off).
- `cd apps/api && uv run pytest -m "not live_llm and not graph and not e2e" -q` no regressions.
- Manual-Only: the live GH Actions trigger reaching the local API needs a self-hosted runner / tunnel (documented in user_setup).

## SC3 parallel claim (W6 — named, not silently unproven)
SC3 ("two jobs run concurrently") is proven in TWO named parts, never left implicit:
  1. AUTOMATED (prefetch bound): Plan 01's `tests/functional/test_worker_consume.py` asserts `prefetch_count==2` is set on the real consumer channel — the configured concurrency bound. This is the positively-tested half of the parallel claim.
  2. Manual-Only (memory-fit concurrency): two browser jobs actually running at once under the 768m worker mem_limit + the 3GB cap is exercised by the Plan-04 Manual-Only live end-to-end run (watch two flows progress concurrently under prefetch=2). This half is explicitly a Manual check — NOT silently assumed.
The parallel claim is therefore scoped to (1) the prefetch-bound config (automated) + (2) the memory-fit concurrency Manual check — named here so it is never treated as unproven.
</verification>

<success_criteria>
- A GitHub Actions workflow starts a tier run via the API and polls status, mapping passed/failed to the check conclusion, using a scoped never-echoed token.
- Two consecutive planted-spec runs against a reset target are identical on status/verdict (not timing), proven keyless.
- SC3 parallel concurrency is named in two parts: prefetch-bound config (automated, Plan 01) + memory-fit concurrency (Manual-Only, Plan 04).
</success_criteria>

<output>
Create `.planning/phases/07-execution-engine-workers/07-05-SUMMARY.md` when done.
</output>
</content>
