---
phase: 07-execution-engine-workers
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - apps/api/app/services/worker/classifier.py
  - apps/api/app/services/worker/job.py
  - apps/api/app/services/exec_history.py
  - apps/api/app/services/exec_service.py
  - apps/api/app/routers/executions.py
  - apps/api/app/main.py
  - apps/api/tests/unit/test_flaky_classifier.py
  - apps/api/tests/functional/test_artifact_capture.py
  - apps/api/tests/functional/test_exec_history.py
  - apps/api/tests/functional/test_execute_tier.py
autonomous: true
requirements: [EXEC-04, EXEC-05]
user_setup: []

must_haves:
  truths:
    - "A planted-spec run captures per-step screenshots + Playwright trace always, and video only on failure, on the filesystem under workspaces/<run_id>/<flow_id>/ with run-relative paths recorded in test_artifacts (no binaries in Postgres)"
    - "A failed test is retried up to 2x; passes-on-retry → flaky (infra), all-attempts-fail → product_failure (the pure classifier)"
    - "Execution history exposes pass/fail trends, durations, and a flaky leaderboard via queries over test_runs/test_results"
    - "POST /api/executions {tier} (owned by executions.py) resolves the tier (incl. risk-based), creates a test_run row, enqueues per-flow jobs, and returns 202 + run_id; the SAME router's GET / and GET /{run_id} return history + status — with EXACTLY ONE handler per (method, path)"
  artifacts:
    - path: "apps/api/app/services/worker/classifier.py"
      provides: "pure flaky-vs-product retry classifier (no I/O, no LLM)"
      contains: "classify_retry"
    - path: "apps/api/app/services/exec_history.py"
      provides: "trend / duration / flaky-leaderboard queries"
      contains: "flaky"
    - path: "apps/api/app/routers/executions.py"
      provides: "POST /api/executions + GET history/status (single owner of /api/executions, auth-gated)"
      contains: "executions"
  key_links:
    - from: "apps/api/app/services/worker/job.py"
      to: "pytest-playwright artifact flags + test_artifacts rows (run-relative paths under <run_id>/<flow_id>/)"
      via: "argv --screenshot=on --tracing=on --video=retain-on-failure --output <run_dir>/<flow_id>"
      pattern: "retain-on-failure|--tracing"
    - from: "apps/api/app/services/worker/job.py"
      to: "classify_retry"
      via: "retry loop over per-attempt exit codes"
      pattern: "classify_retry"
    - from: "apps/api/app/routers/executions.py"
      to: "exec_service.resolve_tier + enqueue_jobs"
      via: "POST /api/executions resolves tier and enqueues per-flow jobs"
      pattern: "resolve_tier|enqueue_jobs"
---

<objective>
Make every run fully EVIDENCED and HISTORIED: extend the worker job with per-step artifact capture (screenshots + trace always, video on failure) and the 2x retry loop feeding the pure flaky-vs-product classifier; persist results + run-relative artifact paths; expose execution history (trends, durations, flaky leaderboard); and make `routers/executions.py` the SINGLE owner of `/api/executions` — wiring the auth-gated POST {tier} → resolve tier → create run → enqueue per-flow jobs round-trip and reconciling the Phase-3 GET ""/GET /{run_id} handlers to the test_runs shape so there is exactly one handler per (method, path).

Purpose: EXEC-04 (artifacts on filesystem, paths in Postgres) and EXEC-05 (history + flaky detection that distinguishes infra flake from product failure, D-05). This is the slice where a tier run becomes a real, retrievable, evidenced execution.
Output: worker/classifier.py (pure); extended worker/job.py (capture flags + retry loop + concrete on-disk layout); exec_history.py queries; the POST/GET execution routes consolidated INTO executions.py; keyless classifier table tests; functional proofs for artifact capture, history queries, and the API tier round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-execution-engine-workers/07-CONTEXT.md
@.planning/phases/07-execution-engine-workers/07-RESEARCH.md
@.planning/phases/07-execution-engine-workers/07-PATTERNS.md
@.planning/phases/07-execution-engine-workers/07-01-SUMMARY.md
@.planning/phases/07-execution-engine-workers/07-02-SUMMARY.md

<interfaces>
From apps/api/app/services/worker/job.py (Plan 01, to extend):
- `run_flow_job(job: dict) -> None` — currently single-attempt; this plan adds the capture flags + retry loop + classifier + artifact-path recording
From apps/api/app/services/stability.py:
- `_run_spec_once(spec_path, *, base_url=None)` returns `{passed, exit_code, output}` — the per-attempt primitive; this plan APPENDS the pytest-playwright artifact flags to its argv (no shell)
From apps/api/app/services/kg/risk.py:
- the pure, table-testable, stdlib-only module shape classifier.py mirrors
From apps/api/app/services/exec_service.py (Plans 01/02):
- `create_test_run(db, tier, selector)`, `enqueue_jobs(run_id, jobs)`, `resolve_tier(tier)`, `rank_risk_flows(db)`
From apps/api/app/models/execution_history.py (Plan 01):
- `TestRun`, `TestResult(run_id, flow_id, verdict, attempts, exit_codes JSON, duration_ms)`, `TestArtifact(run_id, flow_id, kind, path)` — kind enum is screenshot|trace|video ONLY (console/network live INSIDE the trace per W4 decision (a) — see 07-01)
From apps/api/app/routers/executions.py (EXISTING — the SINGLE owner of prefix="/api/executions"):
- `router = APIRouter(prefix="/api/executions", tags=["executions"], dependencies=[Depends(get_current_user)])` (the auth gate is ALREADY here — T-03-07)
- EXISTING `@router.get("", response_model=dict)` `list_executions(db)` returning `{"runs": [...], "executions": [...]}` (Phase-3 RunResponse/ExecutionResponse shape)
- EXISTING `@router.get("/{run_id}", response_model=RunStatus)` `get_execution_status(run_id, db)` returning Phase-3 `RunStatus` via `run_service.get_status_by_run_id`
- These two MUST be reconciled (not double-registered) — see B1 action below.
From apps/api/app/routers/explore.py (the router shape to mirror for the 202 POST):
- the 202 start shape (POST returning `{run_id, status}`); router-level Depends(get_current_user)
From apps/api/app/core/workspaces.py:
- `run_dir(run_id, create=True) -> Path` — the per-run workspace root; artifacts land under here
From apps/api/app/schemas/execution.py (Plan 01):
- `ExecuteTierRequest`, `TestRunResponse(from_attributes=True)`
From .planning/phases/07-execution-engine-workers/07-RESEARCH.md:
- Pattern 4 capture flags; the classify_retry reference body; the history SQL (pass-rate trend, durations, flaky leaderboard); D-05 retry rule
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure flaky classifier + worker retry loop + per-step artifact capture with a concrete on-disk layout</name>
  <read_first>
    - apps/api/app/services/worker/job.py (Plan 01 single-attempt version to extend)
    - apps/api/app/services/stability.py `_run_spec_once` (the argv shape to extend with capture flags)
    - apps/api/app/services/kg/risk.py (the pure-module discipline classifier mirrors)
    - apps/api/app/core/workspaces.py `run_dir` (the per-run workspace root the --output dir lives under)
    - apps/api/app/templates/conftest.py.j2 (the generated conftest — confirm NO page.on console/network hooks exist; W4 decision (a) keeps it that way: console/network are captured INSIDE the trace via --tracing=on, NOT as separate files)
    - .planning/phases/07-execution-engine-workers/07-RESEARCH.md (Pattern 4 capture flags; "The flaky classifier" + "The worker job" code; D-05 rule; A3 trace=console+network)
    - .planning/phases/07-execution-engine-workers/07-PATTERNS.md (worker/job.py + worker/classifier.py DIRECT-REUSE entries)
  </read_first>
  <behavior>
    - classify_retry([0]) → passed (1 attempt); classify_retry([1,0]) → flaky; classify_retry([1,1,1]) → product_failure; classify_retry([1,1,0]) → flaky
    - the retry loop runs at most 3 attempts (original + 2 retries) and stops early on exit 0
    - a run captures screenshot + trace always and video only on failure UNDER workspaces/<run_id>/<flow_id>/ (a per-flow subdir below the run workspace); pytest-playwright's --output writes per-test SUBDIRECTORIES under that dir (e.g. <test-slug>/test-failed-1.png, <test-slug>/trace.zip)
    - TestArtifact.path records a RUN-RELATIVE path (relative to run_dir(run_id)) that PRESERVES the subdir segment(s) — e.g. "<flow_id>/<test-slug>/trace.zip" — never a bare basename and never an absolute path; no binaries in DB
    - the worker records ONE TestResult with the verdict, attempts count, and exit_codes JSON list, in a fresh SessionLocal
  </behavior>
  <action>
    Create `app/services/worker/classifier.py` as a PURE, stdlib-only, no-I/O, no-LLM module mirroring `kg/risk.py`: `classify_retry(attempt_exit_codes: list[int]) -> dict` per the RESEARCH reference body — `passed = any(code==0)`, `retried = len>1`; if passed → verdict `"flaky"` if retried else `"passed"`; else `"product_failure"`; return `{verdict, attempts, passed, exit_codes}`. Verdict vocabulary: passed|flaky|product_failure|aborted (aborted is set by the kill path in Plan 04, not here).

    DEFINE THE CONCRETE ON-DISK LAYOUT (B2): the per-flow output dir is `out_dir = run_dir(run_id, create=True) / flow_id` (flow_id from the trusted job message, run_id-derived — T-07). Pass `--output <out_dir>` so pytest-playwright writes its per-test subdirectories UNDER `<run_id>/<flow_id>/`. After the run, discover captured files by walking `out_dir.rglob("*")` for files; for each file compute its path RELATIVE to `run_dir(run_id)` (i.e. `file.relative_to(run_dir(run_id))`, which yields `<flow_id>/<subdir...>/<name>` with POSIX separators) and store THAT run-relative string in TestArtifact.path — this is the exact string the Plan-04 artifact route will resolve. Infer `kind` from the filename suffix/stem: `*.png`/screenshot → screenshot, `trace*.zip` → trace, `*.webm`/video → video. (Per W4 decision (a) there are NO console_log/network_log files — console+network are captured inside the trace via --tracing=on; do NOT emit those kinds.)

    Extend `app/services/worker/job.py` `run_flow_job`: build the per-attempt argv by APPENDING the pytest-playwright capture flags to the stability `_run_spec_once` shape (no shell) — `--screenshot=on` (always, D-04), `--tracing=on` (always — trace carries console+network, A3/W4), `--video=retain-on-failure` (video on failure only, D-04), `--output <out_dir>` (the per-flow dir above); loop up to MAX_ATTEMPTS=3, breaking on exit 0, collecting per-attempt exit codes; call `classify_retry(exit_codes)`; in a FRESH `async with SessionLocal() as db:` write ONE TestResult (verdict, attempts=len(codes), exit_codes JSON, duration_ms) and one TestArtifact row per captured file discovered under out_dir (run-relative path preserving subdirs, kind inferred — no binary in DB). Keep the kill-flag check stub from Plan 01 as a no-op hook (Plan 04 implements the real drain); do NOT add a placeholder kill behavior.

    Create `tests/unit/test_flaky_classifier.py` — the full table (passed/flaky on [1,0] and [1,1,0]/product on all-fail), keyless, pure. Create `tests/functional/test_artifact_capture.py` (marker functional): drive run_flow_job over a planted spec (one that passes → screenshot+trace present, no video; one rigged to fail → video present), assert the files exist UNDER workspaces/<run_id>/<flow_id>/ in per-test SUBDIRECTORIES AND that test_artifacts rows record their RUN-RELATIVE paths (containing the `<flow_id>/` segment, NOT a bare basename) with the correct kinds, and that NO binary bytes are stored in Postgres (only String paths).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_flaky_classifier.py -q && uv run pytest tests/functional/test_artifact_capture.py -m functional -q && uv run pytest tests/unit/test_no_llm_in_worker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The classifier table tests pass for passed/flaky/product_failure including [1,0] and [1,1,0]→flaky and [1,1,1]→product_failure.
    - `grep -n "retain-on-failure\|--tracing=on\|--screenshot=on\|--output" app/services/worker/job.py` finds all four flags; `grep -n "shell=True" app/services/worker/` finds nothing in code.
    - test_artifact_capture.py: a passing run yields screenshot+trace artifact rows and NO video row; a failing run yields a video row. EACH recorded TestArtifact.path is RUN-RELATIVE and CONTAINS the `<flow_id>/` subdir segment (a multi-segment path, not a bare basename); the DB stores only String paths.
    - NO TestArtifact row has kind console_log or network_log (W4 decision (a): those live inside the trace).
    - test_no_llm_in_worker.py still green (classifier + job import no LLM symbol).
  </acceptance_criteria>
  <done>The pure flaky classifier, the 2x retry loop, and per-step artifact capture (screenshots/trace always, video on failure) are implemented with a concrete <run_id>/<flow_id>/ on-disk layout; results + run-relative artifact paths persist; proven keyless.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execution-history queries + consolidate /api/executions into executions.py (POST + reconciled GET history/status)</name>
  <read_first>
    - apps/api/app/routers/executions.py (THE existing single owner of prefix="/api/executions" — its current GET ""/GET /{run_id} handlers + the router-level auth gate; you EXTEND this file, you do NOT add executions routes to execute.py)
    - apps/api/app/routers/execute.py (the Phase-3 POST /api/execute router under prefix="/api" — LEAVE IT; the legacy single-spec /execute path stays here, the NEW tier routes go in executions.py)
    - apps/api/app/routers/explore.py lines 43-53 (the 202 start shape to mirror for POST)
    - apps/api/app/services/run_service.py `list_runs`/`get_status_by_run_id` (the SQLAlchemy 2.0 select/scalars shape; the EXISTING GET handlers call get_status_by_run_id)
    - apps/api/app/services/exec_service.py (resolve_tier, rank_risk_flows, create_test_run, enqueue_jobs from Plans 01/02)
    - apps/api/app/schemas/execution.py (ExecuteTierRequest, TestRunResponse from Plan 01) + apps/api/app/schemas/run.py (the Phase-3 RunStatus/RunResponse/ExecutionResponse the old handlers return)
    - apps/api/app/main.py (router registration block — executions.py is already registered; do NOT register it twice)
    - apps/api/app/routers/auth.py (how the platform validates JWTs — to wire the scoped ci_token bearer note, I1)
    - .planning/phases/07-execution-engine-workers/07-RESEARCH.md ("Trend/duration/flaky queries"; "POST start endpoint" PATTERNS note)
    - .planning/phases/07-execution-engine-workers/07-PATTERNS.md (exec_history.py entry; "per-flow enqueue for ALL tiers")
  </read_first>
  <behavior>
    - GET pass-rate trend returns per-day sum(passed)/sum(total) over test_runs; durations returns avg/max duration_ms per flow; flaky leaderboard returns flow_ids ordered by flaky count
    - POST /api/executions {tier} resolves the tier (incl. risk-based via rank_risk_flows), creates a test_run row, enqueues ONE job per chosen flow, returns 202 + run_id
    - GET /api/executions returns the run history (the test_runs shape, TestRunResponse[]); GET /api/executions/{run_id} returns the run status + summary; 404 for an unknown run_id
    - EXACTLY ONE handler exists per (method, path) under /api/executions — the legacy Phase-3 GET ""/GET /{run_id} are reconciled to the test_runs shape, NOT left double-registered alongside new ones
    - all routes stay under the router-level Depends(get_current_user) gate; an unauthenticated request → 401
  </behavior>
  <action>
    Create `app/services/exec_history.py` with the EXEC-05 queries per RESEARCH (SQLAlchemy 2.0 select/scalars like run_service.list_runs): `pass_rate_trend(db)` (per-day sum(passed)/sum(total) from test_runs), `durations_by_flow(db)` (avg/max duration_ms from test_results), `flaky_leaderboard(db)` (flow_id count where verdict='flaky' desc), `list_runs(db)` (test_runs newest-first), `get_run_status(db, run_id)` (the test_run row + its test_results summary; None → 404).

    CONSOLIDATE /api/executions OWNERSHIP IN executions.py (B1). executions.py ALREADY owns `APIRouter(prefix="/api/executions", dependencies=[Depends(get_current_user)])` with two Phase-3 handlers (GET "" returning the {runs, executions} dict and GET "/{run_id}" returning RunStatus). Do ALL of the following IN executions.py (NOT execute.py):
      1. ADD `POST ""` (i.e. POST /api/executions, status_code=202): take `ExecuteTierRequest` → `resolve_tier(tier)` (422 on the ValueError); for tag tiers enqueue one job per approved flow carrying the tag, for full one per approved flow, for risk-based call `rank_risk_flows(db)` (BEFORE the run phase, neo4j up — document D-03b) and enqueue the top-N flows' spec paths; create the test_run row via `create_test_run`; `enqueue_jobs(run_id, jobs)`; return 202 `{run_id}` (mirror explore.py's 202 shape).
      2. RECONCILE the existing GET handlers so there is EXACTLY ONE handler per (method, path). Reshape `GET ""` to return the test_runs history as `TestRunResponse[]` via `exec_history.list_runs(db)` (the Phase-7 history surface supersedes the Phase-3 {runs, executions} dict). Reshape `GET "/{run_id}"` to return the test_run status+summary via `exec_history.get_run_status(db, run_id)` (404 on None). If the Phase-3 RunStatus poll surface (the legacy explore/execute single-spec path) must remain reachable, NAMESPACE it under a distinct path (e.g. `GET /{run_id}/legacy-status`) — do NOT leave a second handler on the same (GET, "/{run_id}"). The decision (extend vs namespace) MUST result in zero duplicate (method,path) registrations.
      3. Do NOT add ANY /api/executions route to execute.py (that file keeps only POST /api/execute under prefix="/api"); fix any stale CONTEXT/PATTERNS line that says "extend execute.py" for these routes.
      4. (I1) Add a one-line note + the wiring stub so the POST start + GET poll routes ALSO accept the scoped `settings.ci_token` as a bearer credential (in addition to the cookie Depends(get_current_user)) — the CI workflow (Plan 05) authenticates with it. The actual bearer check reads `Authorization: Bearer <token>` and accepts it when it equals settings.ci_token (start+poll scope only); exercising it live is Manual-Only, but the wiring must exist here.
    Register-check main.py (executions.py is already registered — ensure it is NOT registered twice).

    Create `tests/functional/test_exec_history.py` (marker functional): seed test_runs/test_results rows, assert the trend/duration/flaky queries return the expected aggregates. Create `tests/functional/test_execute_tier.py` (marker functional): POST a `smoke` tier run to /api/executions, assert 202 + run_id, assert a test_run row was created and per-flow jobs were enqueued (assert against the queue or a captured enqueue call), assert GET /api/executions/{run_id} returns the status (and 404 for a bogus id), assert an unauthenticated POST returns 401, AND assert the route table has EXACTLY ONE handler per (method, path) for paths under /api/executions (introspect `app.routes`, group by (methods, path), assert no duplicates).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_exec_history.py tests/functional/test_execute_tier.py -m functional -q</automated>
  </verify>
  <acceptance_criteria>
    - The three history queries return the expected aggregates over seeded rows.
    - POST /api/executions {tier:"smoke"} returns 202 + run_id, creates a test_run row, and enqueues one job per tagged flow.
    - GET /api/executions lists runs as TestRunResponse[]; GET /api/executions/{run_id} returns status; bogus id → 404; unauthenticated POST → 401.
    - EXACTLY ONE handler per (method, path) under /api/executions — the test introspects app.routes and asserts no duplicate (method,path) registration; no /api/executions route exists in execute.py.
    - `grep -n "resolve_tier\|enqueue_jobs\|rank_risk_flows" app/routers/executions.py` shows the tier round-trip wiring lives in executions.py; `grep -rn "/executions" app/routers/execute.py` finds nothing.
    - `grep -n "Depends(get_current_user)" app/routers/executions.py` confirms the auth gate; the ci_token bearer wiring is present for the start/poll routes.
  </acceptance_criteria>
  <done>Execution history queries are implemented and /api/executions is solely owned by executions.py with the auth-gated POST tier round-trip + reconciled GET history/status (one handler per method,path), proven functionally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API execution routes | tier + run_id inputs cross the auth gate into tier resolution + queries |
| worker → filesystem | captured artifacts written under workspaces/<run_id>/<flow_id>/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Info Disclosure | unauth access to execution history/status | mitigate | router-level Depends(get_current_user) on executions.py (already present); unauth → 401 (asserted in test) |
| T-07-05 | Tampering | tier input → pytest selector | mitigate | resolve_tier allow-list (Plan 02) → 422 on unknown; selector tokens are constants |
| T-07-11 | Tampering | artifact output path | mitigate | --output is run_dir(run_id)/flow_id (run_id-derived, create=True); flow_id from the trusted job message, not raw client input; TestArtifact.path is run-relative; serving guard added in Plan 04 |
| T-07-12 | Disclosure/Integrity | storing artifact binaries in Postgres | mitigate | test_artifacts stores String(1024) paths only; functional test asserts no binary column populated (carry-forward rule) |
| T-07-18 | Tampering/Integrity | duplicate (method,path) on /api/executions silently shadowing a handler with a different schema | mitigate | executions.py is the SINGLE owner; legacy GET handlers reconciled/namespaced; test asserts one handler per (method,path) |
| T-07-04 | Governance | LLM in worker (SC3) | mitigate | classifier + job extension import no LLM; test_no_llm_in_worker.py re-run green |
</threat_model>

<verification>
- `cd apps/api && uv run pytest tests/unit/test_flaky_classifier.py -q` green.
- `cd apps/api && uv run pytest -m functional -q` (queue profile up, neo4j off) — artifact capture + history + tier round-trip green.
- `cd apps/api && uv run pytest tests/unit/test_no_llm_in_worker.py -q` green.
- `cd apps/api && uv run pytest -m "not live_llm and not graph and not e2e" -q` no regressions.
</verification>

<success_criteria>
- Per-step screenshots + trace always, video on failure, run-relative paths (under <run_id>/<flow_id>/) in test_artifacts (no binaries in Postgres).
- 2x retry loop + pure flaky-vs-product classifier (passes-on-retry → flaky).
- Execution history trends/durations/flaky-leaderboard queries.
- /api/executions solely owned by executions.py: auth-gated POST tier round-trip (incl. risk-based) + reconciled GET history/status with one handler per (method,path).
</success_criteria>

<output>
Create `.planning/phases/07-execution-engine-workers/07-03-SUMMARY.md` when done.
</output>
</content>
