---
phase: 07-execution-engine-workers
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - apps/api/app/services/worker/classifier.py
  - apps/api/app/services/worker/job.py
  - apps/api/app/services/exec_history.py
  - apps/api/app/services/exec_service.py
  - apps/api/app/routers/execute.py
  - apps/api/app/main.py
  - apps/api/tests/unit/test_flaky_classifier.py
  - apps/api/tests/functional/test_artifact_capture.py
  - apps/api/tests/functional/test_exec_history.py
  - apps/api/tests/functional/test_execute_tier.py
autonomous: true
requirements: [EXEC-04, EXEC-05]
user_setup: []

must_haves:
  truths:
    - "A planted-spec run captures per-step screenshots + Playwright trace always, and video only on failure, on the filesystem under workspaces/<run_id>/ with paths recorded in test_artifacts (no binaries in Postgres)"
    - "A failed test is retried up to 2x; passes-on-retry → flaky (infra), all-attempts-fail → product_failure (the pure classifier)"
    - "Execution history exposes pass/fail trends, durations, and a flaky leaderboard via queries over test_runs/test_results"
    - "POST /api/executions {tier} resolves the tier (incl. risk-based), creates a test_run row, enqueues per-flow jobs, and returns 202 + run_id; GET /api/executions and GET /api/executions/{run_id} return history + status"
  artifacts:
    - path: "apps/api/app/services/worker/classifier.py"
      provides: "pure flaky-vs-product retry classifier (no I/O, no LLM)"
      contains: "classify_retry"
    - path: "apps/api/app/services/exec_history.py"
      provides: "trend / duration / flaky-leaderboard queries"
      contains: "flaky"
    - path: "apps/api/app/routers/execute.py"
      provides: "POST /api/executions + GET history/status (auth-gated)"
      contains: "executions"
  key_links:
    - from: "apps/api/app/services/worker/job.py"
      to: "pytest-playwright artifact flags + test_artifacts rows"
      via: "argv --screenshot=on --tracing=on --video=retain-on-failure --output"
      pattern: "retain-on-failure|--tracing"
    - from: "apps/api/app/services/worker/job.py"
      to: "classify_retry"
      via: "retry loop over per-attempt exit codes"
      pattern: "classify_retry"
    - from: "apps/api/app/routers/execute.py"
      to: "exec_service.resolve_tier + enqueue_jobs"
      via: "POST /api/executions resolves tier and enqueues per-flow jobs"
      pattern: "resolve_tier|enqueue_jobs"
---

<objective>
Make every run fully EVIDENCED and HISTORIED: extend the worker job with per-step artifact capture (screenshots + trace always, video on failure) and the 2x retry loop feeding the pure flaky-vs-product classifier; persist results + artifact paths; expose execution history (trends, durations, flaky leaderboard); and wire the auth-gated POST /api/executions {tier} → resolve tier → create run → enqueue per-flow jobs round-trip plus GET history/status.

Purpose: EXEC-04 (artifacts on filesystem, paths in Postgres) and EXEC-05 (history + flaky detection that distinguishes infra flake from product failure, D-05). This is the slice where a tier run becomes a real, retrievable, evidenced execution.
Output: worker/classifier.py (pure); extended worker/job.py (capture flags + retry loop); exec_history.py queries; the POST/GET execution routes; keyless classifier table tests; functional proofs for artifact capture, history queries, and the API tier round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-execution-engine-workers/07-CONTEXT.md
@.planning/phases/07-execution-engine-workers/07-RESEARCH.md
@.planning/phases/07-execution-engine-workers/07-PATTERNS.md
@.planning/phases/07-execution-engine-workers/07-01-SUMMARY.md
@.planning/phases/07-execution-engine-workers/07-02-SUMMARY.md

<interfaces>
From apps/api/app/services/worker/job.py (Plan 01, to extend):
- `run_flow_job(job: dict) -> None` — currently single-attempt; this plan adds the capture flags + retry loop + classifier + artifact-path recording
From apps/api/app/services/stability.py:
- `_run_spec_once(spec_path, *, base_url=None)` returns `{passed, exit_code, output}` — the per-attempt primitive; this plan APPENDS the pytest-playwright artifact flags to its argv (no shell)
From apps/api/app/services/kg/risk.py:
- the pure, table-testable, stdlib-only module shape classifier.py mirrors
From apps/api/app/services/exec_service.py (Plans 01/02):
- `create_test_run(db, tier, selector)`, `enqueue_jobs(run_id, jobs)`, `resolve_tier(tier)`, `rank_risk_flows(db)`
From apps/api/app/models/execution_history.py (Plan 01):
- `TestRun`, `TestResult(run_id, flow_id, verdict, attempts, exit_codes JSON, duration_ms)`, `TestArtifact(run_id, flow_id, kind, path)`
From apps/api/app/routers/explore.py (the router template):
- `APIRouter(prefix="/api", dependencies=[Depends(get_current_user)])`; the 202 start shape (explore.py line 43); GET handlers
From apps/api/app/routers/executions.py (existing GET /executions for Phase 3):
- the existing list/status surface to extend or sit alongside (executions_v2 if needed — but prefer extending execute.py per PATTERNS)
From apps/api/app/core/workspaces.py:
- `run_dir(run_id, create=True)` — artifacts land under here
From .planning/phases/07-execution-engine-workers/07-RESEARCH.md:
- Pattern 4 capture flags; the classify_retry reference body; the history SQL (pass-rate trend, durations, flaky leaderboard); D-05 retry rule
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure flaky classifier + worker retry loop + per-step artifact capture</name>
  <read_first>
    - apps/api/app/services/worker/job.py (Plan 01 single-attempt version to extend)
    - apps/api/app/services/stability.py `_run_spec_once` (the argv shape to extend with capture flags)
    - apps/api/app/services/kg/risk.py (the pure-module discipline classifier mirrors)
    - apps/api/app/core/workspaces.py `run_dir` (artifact output dir)
    - .planning/phases/07-execution-engine-workers/07-RESEARCH.md (Pattern 4 capture flags; "The flaky classifier" + "The worker job" code; D-05 rule; A3 trace=console+network)
    - .planning/phases/07-execution-engine-workers/07-PATTERNS.md (worker/job.py + worker/classifier.py DIRECT-REUSE entries)
  </read_first>
  <behavior>
    - classify_retry([0]) → passed (1 attempt); classify_retry([1,0]) → flaky; classify_retry([1,1,1]) → product_failure; classify_retry([1,1,0]) → flaky
    - the retry loop runs at most 3 attempts (original + 2 retries) and stops early on exit 0
    - a run captures screenshot + trace always and video only on failure under workspaces/<run_id>/; TestArtifact rows record the relative paths (no binaries in DB)
    - the worker records ONE TestResult with the verdict, attempts count, and exit_codes JSON list, in a fresh SessionLocal
  </behavior>
  <action>
    Create `app/services/worker/classifier.py` as a PURE, stdlib-only, no-I/O, no-LLM module mirroring `kg/risk.py`: `classify_retry(attempt_exit_codes: list[int]) -> dict` per the RESEARCH reference body — `passed = any(code==0)`, `retried = len>1`; if passed → verdict `"flaky"` if retried else `"passed"`; else `"product_failure"`; return `{verdict, attempts, passed, exit_codes}`. Verdict vocabulary: passed|flaky|product_failure|aborted (aborted is set by the kill path in Plan 04, not here). Extend `app/services/worker/job.py` `run_flow_job`: build the per-attempt argv by APPENDING the pytest-playwright capture flags to the stability `_run_spec_once` shape (no shell) — `--screenshot=on` (always, D-04), `--tracing=on` (always — trace carries console+network, A3), `--video=retain-on-failure` (video on failure only, D-04), `--output <run_dir>` where run_dir = `run_dir(run_id, create=True)` (run_id-derived, T-07); loop up to MAX_ATTEMPTS=3, breaking on exit 0, collecting per-attempt exit codes; call `classify_retry(exit_codes)`; in a FRESH `async with SessionLocal() as db:` write ONE TestResult (verdict, attempts=len(codes), exit_codes JSON, duration_ms) and one TestArtifact row per captured file discovered under run_dir (kind inferred from the filename: screenshot|trace|video; relative path only — no binary in DB). Keep the kill-flag check stub from Plan 01 as a no-op hook (Plan 04 implements the real drain); do NOT add a placeholder kill behavior. Create `tests/unit/test_flaky_classifier.py` — the full table (passed/flaky on [1,0] and [1,1,0]/product on all-fail), keyless, pure. Create `tests/functional/test_artifact_capture.py` (marker functional): drive run_flow_job over a planted spec (one that passes → screenshot+trace present, no video; one rigged to fail → video present), assert the files exist under workspaces/<run_id>/ AND that test_artifacts rows record their relative paths and the correct kinds, and that NO binary bytes are stored in Postgres (only String paths).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_flaky_classifier.py -q && uv run pytest tests/functional/test_artifact_capture.py -m functional -q && uv run pytest tests/unit/test_no_llm_in_worker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The classifier table tests pass for passed/flaky/product_failure including [1,0] and [1,1,0]→flaky and [1,1,1]→product_failure.
    - `grep -n "retain-on-failure\|--tracing=on\|--screenshot=on" app/services/worker/job.py` finds all three capture flags; `grep -n "shell=True" app/services/worker/` finds nothing in code.
    - test_artifact_capture.py: a passing run yields screenshot+trace artifact rows and NO video row; a failing run yields a video row; all paths are relative under workspaces/<run_id>/ and the DB stores only paths.
    - test_no_llm_in_worker.py still green (classifier + job import no LLM symbol).
  </acceptance_criteria>
  <done>The pure flaky classifier, the 2x retry loop, and per-step artifact capture (screenshots/trace always, video on failure) are implemented; results + artifact paths persist; proven keyless.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execution-history queries + auth-gated POST /api/executions + GET history/status</name>
  <read_first>
    - apps/api/app/services/run_service.py `list_runs`/`get_status_by_run_id` (the SQLAlchemy 2.0 select/scalars shape)
    - apps/api/app/routers/explore.py lines 35-55 (router auth gate + 202 start shape)
    - apps/api/app/routers/executions.py (the existing GET /executions surface for Phase 3 — extend or sit alongside)
    - apps/api/app/services/exec_service.py (resolve_tier, rank_risk_flows, create_test_run, enqueue_jobs from Plans 01/02)
    - apps/api/app/schemas/execution.py (ExecuteTierRequest, TestRunResponse from Plan 01)
    - apps/api/app/main.py (router registration block)
    - .planning/phases/07-execution-engine-workers/07-RESEARCH.md ("Trend/duration/flaky queries"; "POST start endpoint" PATTERNS note)
    - .planning/phases/07-execution-engine-workers/07-PATTERNS.md (exec_history.py + execute.py router entries; "per-flow enqueue for ALL tiers")
  </read_first>
  <behavior>
    - GET pass-rate trend returns per-day sum(passed)/sum(total) over test_runs; durations returns avg/max duration_ms per flow; flaky leaderboard returns flow_ids ordered by flaky count
    - POST /api/executions {tier} resolves the tier (incl. risk-based via rank_risk_flows), creates a test_run row, enqueues ONE job per chosen flow, returns 202 + run_id
    - GET /api/executions returns the run history; GET /api/executions/{run_id} returns the run status + summary; 404 for an unknown run_id
    - all routes are Depends(get_current_user)-gated; an unauthenticated request → 401
  </behavior>
  <action>
    Create `app/services/exec_history.py` with the EXEC-05 queries per RESEARCH (SQLAlchemy 2.0 select/scalars like run_service.list_runs): `pass_rate_trend(db)` (per-day sum(passed)/sum(total) from test_runs), `durations_by_flow(db)` (avg/max duration_ms from test_results), `flaky_leaderboard(db)` (flow_id count where verdict='flaky' desc), `list_runs(db)` (test_runs newest-first), `get_run_status(db, run_id)` (the run row + its test_results summary; None → 404). Extend `app/routers/execute.py` (the existing Phase-3 execute router — extend in place per PATTERNS; reuse its `APIRouter(prefix="/api", dependencies=[Depends(get_current_user)])` gate, mirroring explore.py): `POST /api/executions` taking `ExecuteTierRequest` → `resolve_tier(tier)` (422 on unknown via the ValueError mapping); for tag tiers enqueue one job per approved flow carrying the tag, for full one per approved flow, for risk-based call `rank_risk_flows(db)` (BEFORE the run phase, neo4j up — document D-03b) and enqueue the top-N flows' spec paths; create the test_run row via `create_test_run`; `enqueue_jobs(run_id, jobs)`; return 202 `{run_id}` (mirror the explore 202 shape). `GET /api/executions` → `list_runs` as `TestRunResponse[]`. `GET /api/executions/{run_id}` → `get_run_status` (404 if missing). Register the router in main.py if not already. Create `tests/functional/test_exec_history.py` (marker functional): seed test_runs/test_results rows, assert the trend/duration/flaky queries return the expected aggregates. Create `tests/functional/test_execute_tier.py` (marker functional): POST a `smoke` tier run, assert 202 + run_id, assert a test_run row was created and per-flow jobs were enqueued (assert against the queue or a captured enqueue call), and assert GET /api/executions/{run_id} returns the status (and 404 for a bogus id); assert an unauthenticated POST returns 401.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_exec_history.py tests/functional/test_execute_tier.py -m functional -q</automated>
  </verify>
  <acceptance_criteria>
    - The three history queries return the expected aggregates over seeded rows.
    - POST /api/executions {tier:"smoke"} returns 202 + run_id, creates a test_run row, and enqueues one job per tagged flow.
    - GET /api/executions lists runs; GET /api/executions/{run_id} returns status; bogus id → 404; unauthenticated POST → 401.
    - `grep -n "resolve_tier\|enqueue_jobs\|rank_risk_flows" app/routers/execute.py` shows the tier round-trip wiring.
    - `grep -n "Depends(get_current_user)" app/routers/execute.py` confirms the auth gate.
  </acceptance_criteria>
  <done>Execution history queries and the auth-gated POST /api/executions tier round-trip + GET history/status are implemented and proven functionally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API execution routes | tier + run_id inputs cross the auth gate into tier resolution + queries |
| worker → filesystem | captured artifacts written under workspaces/<run_id>/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Info Disclosure | unauth access to execution history/status | mitigate | router-level Depends(get_current_user) on all execution routes (mirror explore.py); unauth → 401 (asserted in test) |
| T-07-05 | Tampering | tier input → pytest selector | mitigate | resolve_tier allow-list (Plan 02) → 422 on unknown; selector tokens are constants |
| T-07-11 | Tampering | artifact output path | mitigate | --output is run_dir(run_id) (run_id-derived, create=True); flow_id from the trusted job message, not raw client input; serving guard added in Plan 04 |
| T-07-12 | Disclosure/Integrity | storing artifact binaries in Postgres | mitigate | test_artifacts stores String(1024) paths only; functional test asserts no binary column populated (carry-forward rule) |
| T-07-04 | Governance | LLM in worker (SC3) | mitigate | classifier + job extension import no LLM; test_no_llm_in_worker.py re-run green |
</threat_model>

<verification>
- `cd apps/api && uv run pytest tests/unit/test_flaky_classifier.py -q` green.
- `cd apps/api && uv run pytest -m functional -q` (queue profile up, neo4j off) — artifact capture + history + tier round-trip green.
- `cd apps/api && uv run pytest tests/unit/test_no_llm_in_worker.py -q` green.
- `cd apps/api && uv run pytest -m "not live_llm and not graph and not e2e" -q` no regressions.
</verification>

<success_criteria>
- Per-step screenshots + trace always, video on failure, paths in test_artifacts (no binaries in Postgres).
- 2x retry loop + pure flaky-vs-product classifier (passes-on-retry → flaky).
- Execution history trends/durations/flaky-leaderboard queries.
- Auth-gated POST /api/executions tier round-trip (incl. risk-based) + GET history/status.
</success_criteria>

<output>
Create `.planning/phases/07-execution-engine-workers/07-03-SUMMARY.md` when done.
</output>
