---
phase: 07-execution-engine-workers
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/api/app/services/exec_service.py
  - apps/api/app/services/codegen/project.py
  - apps/api/app/templates/conftest.py.j2
  - apps/api/tests/unit/test_exec_tiers.py
  - apps/api/tests/unit/test_risk_ranking.py
  - apps/api/tests/functional/test_codegen_markers.py
autonomous: true
requirements: [EXEC-01]
user_setup: []

must_haves:
  truths:
    - "A tag tier (smoke/sanity/regression) resolves to the correct pytest-bdd marker selector (-m <tag>); full resolves to no filter"
    - "The risk-based tier ranks flows by a frozen-weight combination of graph risk score + recent failure rate and returns the top-N flow spec paths"
    - "Risk-based resolution reads the graph BEFORE the run phase and degrades gracefully (pure structural risk) when neo4j is down or history is empty (cold start)"
    - "The generated project registers the smoke/sanity/regression pytest markers so -m <tag> selects (not 0 selected / no warning)"
  artifacts:
    - path: "apps/api/app/services/exec_service.py"
      provides: "TIER_SELECTOR map + resolve_tier + risk-based ranking (frozen weights)"
      contains: "TIER_SELECTOR"
    - path: "apps/api/app/templates/conftest.py.j2"
      provides: "registered tier markers for pytest-bdd selection"
      contains: "smoke"
  key_links:
    - from: "apps/api/app/services/exec_service.py"
      to: "kg/risk.risk_score + test_results failure history"
      via: "asyncio.wait_for-bounded graph read + history query"
      pattern: "risk_score|wait_for"
    - from: "apps/api/app/services/codegen/project.py"
      to: "the generated pyproject/conftest markers"
      via: "rendered marker registration in the project tree"
      pattern: "smoke|markers"
---

<objective>
Make suites runnable BY TIER: map each tag tier (smoke/sanity/regression/full) to a pytest-bdd marker selector, and compute the risk-based tier dynamically from graph risk + recent failure history (top-N flows), resolved BEFORE the run phase while neo4j is up. Extend codegen so the generated project registers the tier markers (otherwise `-m smoke` selects nothing / warns).

Purpose: EXEC-01 — the user picks a tier and the engine runs exactly the right scenarios. Tag tiers stay in the reviewer's hands (native Gherkin tags, D-01); risk-based is automatic and always reflects the latest signal (D-02).
Output: TIER_SELECTOR + resolve_tier + risk ranking in exec_service.py; registered markers in the generated conftest/pyproject; keyless unit tests for the selector map, the ranking formula, and cold-start; a functional proof that a tag-selected run runs the tagged scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-execution-engine-workers/07-CONTEXT.md
@.planning/phases/07-execution-engine-workers/07-RESEARCH.md
@.planning/phases/07-execution-engine-workers/07-PATTERNS.md
@.planning/phases/07-execution-engine-workers/07-01-SUMMARY.md

<interfaces>
From apps/api/app/services/kg/risk.py:
- `risk_score(...) -> float` (0..100, pure, deterministic, frozen `RiskWeights` dataclass) — reuse for the structural-risk half
From apps/api/app/services/scenarios.py:
- `_flow_risk_index(...)` lines ~66-83 — the asyncio.wait_for(timeout=_RISK_TIMEOUT_S)-bounded graph read that degrades when neo4j is down (copy this graceful-degrade shape)
From apps/api/app/services/exec_service.py (Plan 01):
- `create_test_run(db, tier, selector) -> TestRun`, `enqueue_jobs(run_id, jobs)`
From apps/api/app/services/codegen/project.py:
- `_render_checked_py` (line ~234) ast-parses every rendered .py; the in-memory `files` dict (lines ~233-247) + write loop (lines ~249-255)
From apps/api/app/templates/conftest.py.j2:
- already reads TARGET_BASE_URL (lines ~22-31)
From the EXEC-05 history (Plan 03 owns the queries, but this plan reads test_results directly for failure_rate):
- `test_results(run_id, flow_id, verdict, ...)` — failure_rate = product_failure count / total over last K runs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tier → selector map + risk-based ranking (frozen weights, cold-start safe)</name>
  <read_first>
    - apps/api/app/services/kg/risk.py (the pure frozen-weight RiskWeights shape + risk_score)
    - apps/api/app/services/scenarios.py lines ~66-83 (_flow_risk_index timeout-bounded graceful graph read)
    - apps/api/app/services/exec_service.py (Plan 01 — create_test_run, enqueue_jobs)
    - .planning/phases/07-execution-engine-workers/07-RESEARCH.md (Pattern 5 tier→selector; Pattern 6 risk ranking formula + the [ASSUMED] weights; "Trend/duration/flaky queries" failure-history SQL)
  </read_first>
  <behavior>
    - resolve_tier("smoke") → selector ["-m","smoke"]; "full" → []; an unknown tier → raises/422-able ValueError
    - rank_risk_flows ranks by combined = RISK_WEIGHT*risk_score + FAILURE_WEIGHT*failure_rate*100 and returns the top-N flow ids/spec paths
    - cold start (no test_results history) → failure_weight contributes 0 → ranking is pure structural risk, no crash
    - neo4j down → the graph read times out gracefully and risk-based returns an empty/structural-only ranking with an honest signal, never a hang (mirror _flow_risk_index)
    - the frozen weights live in a RiskRankWeights dataclass tagged [ASSUMED] (RISK_WEIGHT=0.6, FAILURE_WEIGHT=0.4, TOP_N=10) — surfaced for tuning like RiskWeights
  </behavior>
  <action>
    Extend `app/services/exec_service.py`. Add `TIER_SELECTOR = {"smoke": ["-m","smoke"], "sanity": ["-m","sanity"], "regression": ["-m","regression"], "full": []}` and `resolve_tier(tier: str) -> list[str]` that validates tier against an allow-list (the TIER_SELECTOR keys plus "risk-based" — V5 input validation, T-07; unknown → ValueError the router maps to 422). Add a frozen `@dataclass(frozen=True) RiskRankWeights` with `risk_weight=0.6`, `failure_weight=0.4`, `top_n=10` (comment-tag the values `[ASSUMED]` per RESEARCH A1, mirroring kg/risk.RiskWeights). Add `failure_rate(db, flow_ids, *, last_k=10) -> dict[str,float]` reading `test_results` per RESEARCH failure-history SQL (product_failure count / total over the last K test_runs; empty history → 0.0 for all). Add `rank_risk_flows(db, *, weights=RiskRankWeights()) -> list[dict]` that: reads flow risk via `kg/risk.risk_score` over the discovered flows behind an `asyncio.wait_for(..., timeout=...)` graceful-degrade exactly like `scenarios._flow_risk_index` (neo4j down → empty/structural-only, never a hang); combines `risk_weight*risk_score + failure_weight*failure_rate*100`; returns the top-N sorted desc with each flow's id + spec path. Document in the docstring the D-03b sequencing: risk-based MUST resolve here (neo4j up, BEFORE the run phase), materialize the spec list, THEN the run phase proceeds with neo4j off. Create `tests/unit/test_exec_tiers.py` (the selector map + the allow-list 422 path) and `tests/unit/test_risk_ranking.py` (the weighted-sum ranking over fabricated risk+history inputs, top-N truncation, and the cold-start pure-structural path) — both pure, keyless, no graph (inject fabricated risk + history dicts).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_exec_tiers.py tests/unit/test_risk_ranking.py -q</automated>
  </verify>
  <acceptance_criteria>
    - resolve_tier returns the exact selectors above; unknown tier raises ValueError.
    - rank_risk_flows over fabricated inputs returns the expected top-N order by the weighted sum; TOP_N truncates.
    - cold start (empty history dict) → ranking equals pure-structural-risk order; no exception.
    - `grep -n "wait_for\|risk_score" app/services/exec_service.py` shows the bounded graph read reuses the graceful-degrade shape.
  </acceptance_criteria>
  <done>Tier selectors and the dynamic risk-based ranking are implemented with frozen [ASSUMED] weights, cold-start safe, neo4j-down safe, and proven keyless.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register tier markers in the generated project (codegen + conftest extension)</name>
  <read_first>
    - apps/api/app/templates/conftest.py.j2 (the existing fixture; it already reads TARGET_BASE_URL)
    - apps/api/app/services/codegen/project.py lines ~233-255 (_render_checked_py + files dict + write loop)
    - apps/api/pyproject.toml lines ~56-61 (the api's own `[tool.pytest.ini_options] markers` block to mirror)
    - .planning/phases/07-execution-engine-workers/07-RESEARCH.md (Pitfall 3 — unregistered markers warn/select-nothing; Pattern 5)
    - .planning/phases/07-execution-engine-workers/07-PATTERNS.md (conftest.py.j2 + project.py UPGRADE-IN-PLACE entries)
  </read_first>
  <behavior>
    - the generated project tree registers smoke/sanity/regression pytest markers
    - a rendered project ast-parses cleanly (no syntax break introduced) — _render_checked_py stays green
    - a pytest-bdd scenario tagged @smoke is selected by -m smoke against the generated project; an untagged scenario is NOT selected
  </behavior>
  <action>
    Register the tier markers in the generated project so `-m smoke`/`-m sanity`/`-m regression` select (RESEARCH Pitfall 3). Choose ONE home consistent with the existing tree: either extend `app/templates/conftest.py.j2` with `pytest_configure(config)` calling `config.addinivalue_line("markers", "smoke: ...")` for each tier (keep the Jinja output valid Python — it is rendered through `_render_checked_py` which ast-parses it), OR add a rendered `pytest.ini`/`pyproject.toml [tool.pytest.ini_options] markers` entry to the `files` dict in `codegen/project.py` (lines ~233-247) and the write loop (lines ~249-255), mirroring how conftest/fixtures are added (write a .ini/.toml directly, not through _render_checked_py). Mirror the api's own marker block style (pyproject.toml lines ~56-61). Do NOT add a new template file if conftest can hold it. Create `tests/functional/test_codegen_markers.py` (marker functional): render the project tree (reuse the Phase-6 codegen host-driver test pattern, no keys), confirm the rendered project declares the three markers, and run `uv run pytest -m smoke --collect-only` against a planted @smoke-tagged feature to confirm the tagged scenario is collected and an untagged one is not.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_codegen_markers.py -m functional -q</automated>
  </verify>
  <acceptance_criteria>
    - The rendered generated project contains a registration of `smoke`, `sanity`, `regression` markers (grep the rendered output).
    - `_render_checked_py` still ast-parses every .py (the codegen render test stays green).
    - `-m smoke --collect-only` against a planted @smoke feature collects exactly the tagged scenario; no PytestUnknownMarkWarning.
  </acceptance_criteria>
  <done>The generated project registers the tier markers; a tag-selected collection picks exactly the tagged scenarios with no marker warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API tier input | the `tier` string from a request reaches selector resolution |
| API → Neo4j | risk-based reads the graph before the run phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | resolve_tier(tier) used to build pytest argv | mitigate | tier validated against the TIER_SELECTOR + "risk-based" allow-list (V5); unknown → ValueError → 422; the selector tokens are constants, never the raw client string |
| T-07-06 | DoS | risk-based unbounded suite size / neo4j read hang | mitigate | TOP_N cap bounds run size; asyncio.wait_for timeout on the graph read (graceful degrade, no hang) — mirrors scenarios._flow_risk_index |
| T-07-03 | DoS | neo4j up during run phase (memory) | mitigate | risk resolved BEFORE the run phase (D-03b sequencing documented); spec list materialized, then neo4j stopped |
</threat_model>

<verification>
- `cd apps/api && uv run pytest tests/unit/test_exec_tiers.py tests/unit/test_risk_ranking.py -q` green.
- `cd apps/api && uv run pytest tests/functional/test_codegen_markers.py -m functional -q` green.
- `cd apps/api && uv run pytest -m "not live_llm and not graph and not e2e" -q` no regressions.
</verification>

<success_criteria>
- Tag tiers map to the correct pytest-bdd selectors; full = no filter.
- Risk-based ranks flows by frozen-weight risk+failure and returns top-N, cold-start and neo4j-down safe.
- The generated project registers tier markers; tag selection works with no warning.
</success_criteria>

<output>
Create `.planning/phases/07-execution-engine-workers/07-02-SUMMARY.md` when done.
</output>
