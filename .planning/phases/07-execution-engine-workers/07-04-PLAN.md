---
phase: 07-execution-engine-workers
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - shared/events/__init__.py
  - apps/api/app/services/worker/progress.py
  - apps/api/app/services/worker/job.py
  - apps/api/app/services/exec_service.py
  - apps/api/app/routers/execute.py
  - apps/web/package.json
  - apps/web/lib/api/executions.ts
  - apps/web/app/(dashboard)/executions/page.tsx
  - apps/web/app/(dashboard)/executions/[runId]/page.tsx
  - apps/web/components/executions/status-pill.tsx
  - apps/web/components/executions/verdict-badge.tsx
  - apps/web/components/executions/runs-table.tsx
  - apps/web/components/executions/trend-charts.tsx
  - apps/web/components/app-sidebar.tsx
  - apps/api/tests/functional/test_live_exec.py
  - apps/api/tests/functional/test_kill_drain.py
  - apps/web/tests/e2e/executions.spec.ts
autonomous: false
requirements: [EXEC-06]
user_setup: []

must_haves:
  truths:
    - "The worker publishes per-test progress events to Redis channel exec:{run_id}; the SSE endpoint re-emits them to the browser; a reconnect gets a current-state snapshot (no replay)"
    - "Setting the Redis kill flag drains the worker (finishes/aborts the in-flight test, pulls no new jobs) and purges the run's queued jobs — no SIGKILL, no orphaned Chromium"
    - "The live execution view shows per-test progress + absolute counters and a graceful kill that shows a real Stopping… draining state; history shows pass/fail trends + flaky-vs-failed distinctly"
    - "Per-test verdicts and run status render strictly from the server (no fabricated/optimistic status); artifact links are auth-gated, built from run-relative basenames (never raw paths)"
  artifacts:
    - path: "apps/api/app/routers/execute.py"
      provides: "SSE events endpoint + kill endpoint + path-guarded artifact route"
      contains: "EventSourceResponse"
    - path: "apps/web/app/(dashboard)/executions/[runId]/page.tsx"
      provides: "live execution view + terminal run detail (mirrors Phase-4 live view)"
      contains: "EventSource"
    - path: "apps/web/components/app-sidebar.tsx"
      provides: "Executions nav entry"
      contains: "Executions"
  key_links:
    - from: "apps/api/app/services/worker/job.py"
      to: "Redis channel exec:{run_id}"
      via: "publish_test_event per attempt + kill-flag drain check"
      pattern: "publish_test_event|run:.*:kill"
    - from: "apps/api/app/routers/execute.py kill"
      to: "aio-pika queue.purge"
      via: "set kill flag + purge pending jobs"
      pattern: "purge"
    - from: "apps/web/app/(dashboard)/executions/[runId]/page.tsx"
      to: "/api/executions/{runId}/events"
      via: "EventSource over the same-origin proxy (cookie auth)"
      pattern: "EventSource"
---

<objective>
Make a run WATCHABLE and STOPPABLE: the worker publishes per-test progress over the existing Redis→SSE seam; the API re-emits via SSE and owns a graceful kill (Redis flag + drain + queue purge, no SIGKILL); and the Executions UI delivers the launcher + history (with trend charts), the live per-test view with absolute counters and an honest Stopping… draining state, and the terminal run-detail with auth-gated artifact links — all server-authoritative, flaky-vs-failed visually distinct.

Purpose: EXEC-06 (live view + kill switch) + D-07 (graceful drain). The final user-facing slice — after this, a user launches a tier, watches it live, and kills it cleanly.
Output: the per-test shared/events model; worker progress + kill-flag drain; the SSE/kill/artifact routes; recharts (gated); the executions pages + components + sidebar nav; functional proofs for live SSE + kill drain; an e2e against a mocked API+SSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-execution-engine-workers/07-CONTEXT.md
@.planning/phases/07-execution-engine-workers/07-RESEARCH.md
@.planning/phases/07-execution-engine-workers/07-PATTERNS.md
@.planning/phases/07-execution-engine-workers/07-UI-SPEC.md
@.planning/phases/07-execution-engine-workers/07-03-SUMMARY.md
@.planning/phases/04-explorer-agent/04-03-SUMMARY.md

<interfaces>
From apps/api/app/routers/explore.py (the EXACT template):
- SSE: `@router.get(".../events")` → EventSourceResponse over `get_redis().pubsub()`, snapshot-first reconnect, `finally` unsubscribe (lines 90-122)
- artifact path-traversal guard (lines 125-144): reject `/`,`\`,`os.sep`,`..` before FS; resolve inside `run_dir(run_id).resolve()`; containment check
- kill flag (lines 147-156): `get_redis().set(f"...:kill","1")`
From apps/api/app/services/worker/progress.py (Plan 01):
- `publish_test_event(run_id, flow_id, *, status, attempt, duration_s)` → channel exec:{run_id}
From apps/api/app/services/worker/job.py (Plan 03):
- `run_flow_job` retry loop — this plan adds the kill-flag check between attempts + the aborted verdict
From apps/api/app/services/exec_service.py (Plans 01-03):
- `enqueue_jobs`, `create_test_run`, `resolve_tier`; this plan adds `kill_run(run_id)` (set flag + queue.purge)
From apps/web/app/(dashboard)/explore/[runId]/page.tsx (the live-view template to mirror):
- mount GET once to resolve 404/terminal; `new EventSource('/api/explore/{runId}/events')`; absolute-counter contract; feed cap + Jump-to-latest; Kill Dialog; state machine connecting|running|reconnecting|terminal|stream-lost|not-found
From apps/web/lib/api/explore.ts + scenarios.ts:
- the SSE event zod schema; `screenshotUrl(runId,name)` artifact-URL builder; api.get/api.post + schema.parse; startX/stopX mutations
From apps/web/components/explore/{status-pill,counter-tile,terminal-banner}.tsx + components/scenarios/status-badge.tsx:
- reusable components; status-pill DOT/--status-* token map (add `stopping`→--status-quarantine amber)
From apps/web/components/app-sidebar.tsx:
- NAV_ITEMS flat list {icon,label,href}; append {PlayCircle,"Executions","/executions"} after Scenarios; active via pathname.startsWith
From shared/events/__init__.py:
- mirror ExploreProgressEvent → add the per-test ExecutionProgressEvent model (run_id, completed, total, passed, failed, flaky, elapsed_s, status, + per-test delta flow_id/status/attempt/duration_ms)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-test event model + worker kill-flag drain + SSE/kill/artifact routes</name>
  <read_first>
    - apps/api/app/routers/explore.py lines 90-156 (SSE + artifact guard + kill flag — copy verbatim)
    - apps/api/app/services/worker/progress.py (Plan 01 publish) + job.py (Plan 03 retry loop)
    - apps/api/app/services/exec_service.py (enqueue_jobs producer — add kill_run with queue.purge)
    - shared/events/__init__.py + README.md (the ExploreProgressEvent model to mirror)
    - .planning/phases/07-execution-engine-workers/07-RESEARCH.md (Pattern 3 graceful kill + purge; "Per-test progress event"; D-07)
    - .planning/phases/07-execution-engine-workers/07-PATTERNS.md (execute.py router DIRECT-REUSE, progress.py, Graceful cooperative kill)
  </read_first>
  <behavior>
    - publish_test_event emits an ExecutionProgressEvent (absolute run counters + the per-test delta) to channel exec:{run_id}
    - the worker checks run:{run_id}:kill BEFORE each attempt; if set, it publishes an aborted event for remaining tests and pulls no new work (drain) — never SIGKILL
    - kill_run sets the Redis flag AND purges the exec.jobs queue of pending jobs
    - GET /api/executions/{run_id}/events streams events with a current-state snapshot on (re)connect (no replay); POST /api/executions/{run_id}/kill sets the flag + purges; the artifact route rejects ..-/separator names and resolves inside run_dir
  </behavior>
  <action>
    Add `ExecutionProgressEvent` to `shared/events/__init__.py` mirroring `ExploreProgressEvent`: fields run_id, completed, total, passed, failed, flaky (absolute run counters) + the per-test delta (flow_id, test id/name, status passed|flaky|product_failure|aborted|running|queued, attempt, duration_ms) + run status + elapsed_s. Update `app/services/worker/progress.py` to build + publish this model (`model_dump_json()` to channel `exec:{run_id}` via the shared get_redis() — never a second client). Extend `app/services/worker/job.py` retry loop: BEFORE each attempt read `get_redis().get(f"run:{run_id}:kill")`; if set, publish an `aborted` event for this flow and return without pulling new work (D-07 drain, mirrors the explorer loop-top cancel check); the in-flight subprocess is allowed to finish (no SIGKILL). Add `kill_run(run_id)` to `app/services/exec_service.py` per RESEARCH Pattern 3: `await get_redis().set(f"run:{run_id}:kill","1")`, then `connect_robust` → `declare_queue("exec.jobs", durable=True)` → `await queue.purge()` (document the single-worker/one-run-at-a-time assumption A6; per-run queues are a forward note, not Phase-7 scope). Extend `app/routers/execute.py` copying explore.py VERBATIM: `GET /api/executions/{run_id}/events` (EventSourceResponse over `get_redis().pubsub().subscribe("exec:{run_id}")`, a `_snapshot_event` current-state on connect, `finally` unsubscribe); `POST /api/executions/{run_id}/kill` → `kill_run(run_id)`; `GET /api/executions/{run_id}/artifacts/{flow_id}/{name}` reusing the screenshot path-traversal guard VERBATIM (reject `/`,`\`,os.sep,`..`; resolve inside `run_dir(run_id).resolve()`; containment check) returning a FileResponse for screenshot/trace/video/console_log/network_log. All routes stay under the router-level `Depends(get_current_user)` gate. Create `tests/functional/test_live_exec.py` (functional): publish per-test events for a run and assert the SSE endpoint re-emits them + a snapshot on (re)subscribe. Create `tests/functional/test_kill_drain.py` (functional): with jobs queued, set the kill flag via POST kill, assert remaining jobs resolve to `aborted` (not product_failure), assert the queue was purged, and assert NO SIGKILL path exists (grep the worker for os.kill/SIGKILL → none) and no orphaned process is left.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_live_exec.py tests/functional/test_kill_drain.py -m functional -q && uv run pytest tests/unit/test_no_llm_in_worker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EventSourceResponse" app/routers/execute.py` + `grep -n "purge" app/services/exec_service.py` + `grep -n "run:.*:kill" app/services/worker/job.py` all match.
    - `grep -rn "SIGKILL\|os.kill" app/services/worker/ app/services/exec_service.py` finds nothing (cooperative kill only).
    - test_live_exec.py: published per-test events are received over SSE; a reconnect gets a snapshot, not a replay.
    - test_kill_drain.py: after kill, remaining tests are `aborted`, the queue is purged, no orphan.
    - The artifact route rejects `..`/separator names (containment guard reused verbatim).
    - test_no_llm_in_worker.py green.
  </acceptance_criteria>
  <done>The per-test event model, the worker kill-flag drain, and the SSE/kill/path-guarded-artifact routes are implemented and proven functionally (no SIGKILL, snapshot-on-reconnect, queue purge).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Gated install — recharts@3.8.* (the one sanctioned frontend dep) OR native sparkline fallback</name>
  <read_first>
    - apps/web/package.json (the dependencies block — recharts is verified ABSENT)
    - .planning/phases/07-execution-engine-workers/07-UI-SPEC.md ("Charts — the ONE sanctioned new frontend dependency" + the honest native-sparkline fallback)
    - .planning/phases/07-execution-engine-workers/07-PATTERNS.md (trend-charts.tsx NET-NEW gated-dep entry)
  </read_first>
  <what-built>
    Recharts is the chart library named in CLAUDE.md's locked stack SPECIFICALLY for these dashboards (recharts 3.8.x — pass-rate/duration trends) — the frontend analogue of the backend's one sanctioned aio-pika add. It is absent from apps/web/package.json. Per project package policy this single install is gated. Run `cd apps/web && pnpm add recharts@3.8.*` (or the repo's package manager). If the install is risky/unavailable, the UI-SPEC sanctions a zero-dep fallback: a token-styled native SVG/HTML sparkline over --status-* tokens — choose the fallback and skip the install if so. No other new frontend package.
  </what-built>
  <how-to-verify>
    1. Confirm on the registry: https://www.npmjs.com/package/recharts (version 3.8.x).
    2. `cd apps/web && pnpm add recharts@3.8.*` — resolves to 3.8.x.
    3. `git diff apps/web/package.json` shows ONLY recharts (+ its peer-managed transitive in the lockfile) — nothing else.
    4. If electing the fallback: confirm NO change to package.json and that trend-charts.tsx will use a native SVG sparkline.
  </how-to-verify>
  <acceptance_criteria>
    - EITHER recharts 3.8.x is in apps/web/package.json with a clean diff (only recharts), OR the fallback is chosen and package.json is unchanged.
    - No frontend package other than recharts is added.
  </acceptance_criteria>
  <resume-signal>Type "approved (recharts)" or "approved (fallback)" once decided, or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Executions UI — launcher + history + trends, live per-test view + graceful kill, terminal detail, sidebar nav</name>
  <read_first>
    - apps/web/app/(dashboard)/explore/[runId]/page.tsx (the live-view template — mirror verbatim, retarget to per-test)
    - apps/web/app/(dashboard)/scenarios/page.tsx (the list-page + table + segmented-filter template)
    - apps/web/lib/api/explore.ts + lib/api/scenarios.ts (SSE zod schema + artifact-URL builder + api.get/post + mutations)
    - apps/web/components/explore/{status-pill,counter-tile,terminal-banner}.tsx + components/scenarios/status-badge.tsx (reusable components + --status-* token map)
    - apps/web/components/app-sidebar.tsx (NAV_ITEMS flat list + active-route pattern)
    - .planning/phases/07-execution-engine-workers/07-UI-SPEC.md (the FULL contract: every page/state/copy/color/verdict mapping; flaky=amber-never-red; honest draining; artifact basenames)
    - .planning/phases/07-execution-engine-workers/07-PATTERNS.md (every frontend DIRECT-REUSE entry + the styled-native tier picker/progress-bar decisions)
  </read_first>
  <action>
    Build the Executions section to the 07-UI-SPEC verbatim (zero new shadcn add; reuse vendored blocks). `lib/api/executions.ts`: a zod `executionProgressEventSchema` mirroring the backend ExecutionProgressEvent 1:1; `listRuns()`, `getRun(runId)`, `getTrends()` fetchers (api.get + schema.parse); `startRun(tier)` (POST /api/executions) + `killRun(runId)` (POST .../kill) mutations (no optimistic updates — server-authoritative); `artifactUrl(runId, flowId, kind)` builder from run-relative basenames (mirror screenshotUrl; never a raw path). `app/(dashboard)/executions/page.tsx`: the launcher card (heading "Run a suite", a styled-native `<select>` tier picker — Smoke/Sanity/Regression/Full/Risk-based default Smoke, reusing input.tsx token classes; the honest resolved-count helper or "selected when the run starts" for risk-based; accent "Start run" CTA → POST → "Run started" toast → navigate to /executions/{run_id}) + the history `runs-table.tsx` (Tier·Started·Duration·Results·Status; results show "{p} passed · {f} failed · {k} flaky" with flaky AMBER, failed RED, each carrying its WORD; a running row drills into the live view) + the `trend-charts.tsx` Trends region (Recharts pass-rate + duration cards if installed, ELSE the token-styled native sparkline fallback; the runs table is the source of truth, chart supplementary — provide an accessible summary). `app/(dashboard)/executions/[runId]/page.tsx`: mirror the explore live view — mount `GET /api/executions/{runId}` once (resolve 404/terminal), `new EventSource('/api/executions/{runId}/events')`, the counters strip (Passed·Failed·Flaky·Total·Elapsed, ABSOLUTE values from the latest event), the per-test list (role="log" aria-live polite; verdict icon+WORD; "{n} attempts" chip; duration; flaky amber distinct from failed red), the styled-native progress bar (role="progressbar"), the destructive "Kill run" button + focus-trapped confirm Dialog, and the state machine connecting|running|stopping(draining)|reconnecting|terminal(passed/failed/killed)|stream-lost|not-found — INCLUDING the honest amber "Stopping…" draining state (pill role="status", disabled Kill button, the draining banner; NEVER a fake-instant kill, D-07). Once terminal, the SAME route renders the run-detail layout: the Test results table (failures first, then flaky, then passed) with auth-gated artifact links (Screenshot/Trace/Video/Console log/Network log) built via artifactUrl; the honest "Video captured on failure only." caption for passed/flaky tests (no broken link). Create `components/executions/{status-pill,verdict-badge,runs-table,trend-charts}.tsx` (copy/extend the explore/scenarios analogs; add `stopping`→--status-quarantine amber to the pill DOT map). Append `{ icon: PlayCircle, label: "Executions", href: "/executions" }` to NAV_ITEMS in `components/app-sidebar.tsx` after Scenarios (import PlayCircle from lucide-react; active via pathname.startsWith). Create `tests/e2e/executions.spec.ts` mirroring the explore/scenarios e2e: mock /api/executions + a mocked SSE stream; touch every state — launcher start, running with per-test events, the Stopping… draining state on kill, terminal passed/failed/killed, reconnecting, empty (no runs / no trends), error (queue down on start), 404; assert flaky renders amber with the word "Flaky" and failed renders red with the word "Failed" (never color-only).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx eslint app/(dashboard)/executions lib/api/executions.ts components/executions components/app-sidebar.tsx && npx playwright test tests/e2e/executions.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tsc + eslint clean on the touched files.
    - The e2e passes and touches: launcher start, running per-test events, the Stopping… draining state, terminal passed/failed/killed, reconnecting, empty (no runs + no trends), start-error (queue down), 404.
    - Flaky renders AMBER with the word "Flaky"; Failed renders RED with the word "Failed"; status/verdict never color-only (WCAG 1.4.1) — asserted in the e2e.
    - Counters are absolute from the latest SSE event (not deltas); the live view does not poll in parallel with the stream.
    - Artifact links are built from run-relative basenames via artifactUrl (no raw filesystem path in the DOM); the absent-video caption renders for passed/flaky tests.
    - The "Executions" sidebar item appears after Scenarios and is active on /executions.
  </acceptance_criteria>
  <done>The Executions launcher+history+trends, the live per-test view with a graceful Stopping… kill, the terminal run-detail with auth-gated artifact links, and the sidebar nav are built to the 07-UI-SPEC and proven by an e2e against a mocked API+SSE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → SSE/kill routes | EventSource + kill cross the cookie auth gate |
| client → artifact route | flow_id/name reach a filesystem path resolution |
| API → RabbitMQ | kill purges the queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Info Disclosure | SSE/live view leaking run data to an unauth user | mitigate | router-level Depends(get_current_user) on events/kill/artifacts (EventSource uses the httpOnly cookie over the same-origin proxy — exactly like explore.py) |
| T-07-14 | Tampering | artifact path traversal serving evidence | mitigate | reuse explore.py's screenshot guard VERBATIM (reject `..`/separators; resolve inside run_dir(run_id); containment check) — RESEARCH Security Domain mandate |
| T-07-15 | Authz | unauthorized kill of a run | mitigate | kill route under the same auth gate; full RBAC roles are Phase 10 (documented) |
| T-07-16 | DoS/Integrity | SIGKILL orphaning Chromium / corrupt artifacts | mitigate | cooperative Redis-flag drain + queue.purge only; grep gate asserts no os.kill/SIGKILL (Task 1) |
| T-07-17 | Spoofing | fabricated/optimistic status crossing the boundary | mitigate | per-test verdict + run status render strictly from server events/payload (zod-parsed); no optimistic updates; honest draining from a real flag |
</threat_model>

<verification>
- `cd apps/api && uv run pytest tests/functional/test_live_exec.py tests/functional/test_kill_drain.py -m functional -q` green.
- `cd apps/web && npx tsc --noEmit && npx playwright test tests/e2e/executions.spec.ts` green.
- `cd apps/api && uv run pytest tests/unit/test_no_llm_in_worker.py -q` green.
- Manual-Only: the live tier run end-to-end (generate→approve→execute→watch→kill) with provider keys + a real generated suite.
</verification>

<success_criteria>
- Worker publishes per-test progress; SSE re-emits; snapshot on reconnect.
- Graceful kill: flag + drain (aborted, not product_failure) + queue purge; no SIGKILL.
- The Executions UI: launcher + history + trends, live per-test view with honest Stopping… kill, terminal detail with auth-gated artifact links, flaky-vs-failed distinct, all server-authoritative.
- Sidebar nav entry added.
</success_criteria>

<output>
Create `.planning/phases/07-execution-engine-workers/07-04-SUMMARY.md` when done.
</output>
