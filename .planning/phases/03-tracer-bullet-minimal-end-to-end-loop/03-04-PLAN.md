---
phase: 03-tracer-bullet-minimal-end-to-end-loop
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - apps/api/app/services/execution.py
  - apps/api/app/routers/execute.py
  - apps/api/app/schemas/stub.py
  - apps/api/app/routers/stubs.py
  - apps/api/app/main.py
  - apps/api/tests/functional/test_execute.py
  - apps/api/tests/functional/test_surface.py
  - apps/api/tests/functional/test_run_thread.py
autonomous: true
requirements: [PLAT-02]

must_haves:
  truths:
    - "POST /execute runs the run's generated spec via subprocess and lands a result row retrievable via GET /executions"
    - "All 10 PLAT-02 endpoints exist: 5 real + 5 honest 501 stubs with documented OpenAPI contracts"
    - "A single run_id threads explore → generate → execute → result"
    - "The 5 stubs return 501 (never fabricated results) and carry request/response Pydantic contracts in the OpenAPI schema"
  artifacts:
    - path: "apps/api/app/services/execution.py"
      provides: "Async subprocess pytest runner + result capture"
      contains: "create_subprocess_exec"
    - path: "apps/api/app/routers/execute.py"
      provides: "POST /execute (202 + run_id, BackgroundTask)"
      contains: "status_code=202"
    - path: "apps/api/app/routers/stubs.py"
      provides: "5 documented 501 endpoints"
      contains: "501"
  key_links:
    - from: "apps/api/app/services/execution.py"
      to: "uv run pytest <spec_path>"
      via: "asyncio.create_subprocess_exec argv (no shell)"
      pattern: "create_subprocess_exec"
    - from: "apps/api/app/routers/execute.py"
      to: "apps/api/app/services/execution.py"
      via: "BackgroundTasks.add_task(run_execution, ...)"
      pattern: "add_task"
    - from: "apps/api/app/main.py"
      to: "apps/api/app/routers/stubs.py"
      via: "include_router(stubs_router)"
      pattern: "stubs_router"
---

<objective>
Close the loop and complete the surface: the /execute subprocess runner that runs the run's generated spec and writes an executions result row, the 5 honest 501 stub endpoints (heal, create-defect, flows, coverage, dashboard) with documented OpenAPI contracts, and the cross-slice tests proving run_id traceability and the complete 10-endpoint PLAT-02 surface.

Purpose: Satisfies SC3 (execute → result row → GET /executions) and SC4 (all 10 endpoints exist, real or honest stub; shared/events present). Demonstrates the full tracer: explore → generate → execute → result threaded by one run_id.
Output: execution service, execute router, stubs router + contracts, three functional tests (execute, surface, run_thread).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-CONTEXT.md
@.planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-RESEARCH.md
@.planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor uses directly. -->

From infra/scripts/reset_target.py — subprocess discipline to mirror: argv as a LIST, never `shell=True`; `FileNotFoundError` guard for the binary; branch on returncode; the T-01-26 rule — the dynamic value (here spec_path) is registry/run_id-derived, never raw user input.

From apps/api/app/services/run_service.py (Plan 02): `finish_execution(db, execution_id, status, exit_code, output)`, `create_run`/`get_run`, plus an Execution row carrying `spec_path`. From apps/api/app/db/session.py: `SessionLocal`, `get_db`.

From apps/api/app/routers/executions.py (Plan 02): GET /api/executions (list) + GET /api/executions/{run_id} — the poll surface for /execute too.

From apps/api/app/routers/admin_llm.py — small-router frame with `dependencies=[Depends(get_current_user)]` and explicit per-route `status_code=` (analog for stubs.py).

From apps/api/app/routers/targets.py — router auth gate + 404 translation.

Spec location convention (Plan 03): `workspaces/<run_id>/test_login.py`, recorded in `executions.spec_path`. Subprocess: `asyncio.create_subprocess_exec("uv","run","pytest", spec_path, "-q", stdout=PIPE, stderr=STDOUT, cwd="apps/api")`; status = "passed" if returncode==0 else "failed"; capture last ~8000 chars of output (Pitfall 3 — sync-Playwright spec MUST run via subprocess, never pytest.main()).

The 5 stubs (PLAT-02): POST /api/heal (Phase 8), POST /api/create-defect (Phase 9), GET /api/flows (Phase 5), GET /api/coverage (Phase 10), GET /api/dashboard (Phase 10).

From apps/api/tests/conftest.py: `authed_client`, `poll_until_terminal`. From tests/functional/test_targets.py: live-HTTP + 401 auth-gate shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execution service (subprocess pytest runner) + execute router (202 + poll)</name>
  <read_first>
    - infra/scripts/reset_target.py lines 80-108 (subprocess argv + returncode + FileNotFoundError; T-01-26 note lines 22-28 — PATTERNS "execution.py")
    - apps/api/app/services/run_service.py (Plan 02 — finish_execution, create_run, Execution.spec_path)
    - apps/api/app/routers/targets.py lines 16-49 (router frame)
    - .planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-RESEARCH.md Pattern 4 (subprocess runner) + Pitfall 3 (never in-process pytest) + Open Q3 (spec_path convention)
  </read_first>
  <action>
    Create `apps/api/app/services/execution.py` with `async def run_execution(execution_id: str, spec_path: str) -> None` — the BackgroundTask entrypoint: run `proc = await asyncio.create_subprocess_exec("uv","run","pytest", spec_path, "-q", stdout=asyncio.subprocess.PIPE, stderr=asyncio.subprocess.STDOUT, cwd="apps/api")` (argv LIST, no `shell=True`); `out, _ = await proc.communicate()`; `status = "passed" if proc.returncode == 0 else "failed"`; open a FRESH `async with SessionLocal() as db:` (never the request session — Pitfall 2) and call `run_service.finish_execution(db, execution_id, status, exit_code=proc.returncode, output=out.decode(errors="replace")[-8000:])`. Guard `FileNotFoundError` (uv missing) → mark the execution "failed" with an explanatory output. The generated spec uses the SYNC Playwright API and MUST run only via this subprocess — never `pytest.main()` in-process (Pitfall 3). `spec_path` is the run_id-derived `workspaces/<run_id>/test_login.py` recorded in `executions.spec_path`, never raw user input (T-01-26). Create `apps/api/app/routers/execute.py` mirroring targets/explore: `APIRouter(prefix="/api", tags=["execute"], dependencies=[Depends(get_current_user)])`, `@router.post("/execute", status_code=202)` taking a request body with `run_id` + `BackgroundTasks` + `Depends(get_db)`: resolve the spec_path for that run_id (`workspaces/<run_id>/test_login.py`; 404 if the spec was never generated), create an Execution row (status "queued", kind "execute", spec_path), `bg.add_task(run_execution, execution.id, spec_path)`, return 202 + `{"run_id", "status":"queued"}`. The poll surface is the existing GET /api/executions/{run_id} (Plan 02). Include `execute_router` in main.py.
  </action>
  <verify>
    <automated>cd apps/api && uv run python -c "from app.services.execution import run_execution; from app.routers.execute import router; print('import ok')"</automated>
    <automated>grep -n 'create_subprocess_exec' apps/api/app/services/execution.py && grep -L 'pytest.main' apps/api/app/services/execution.py</automated>
  </verify>
  <acceptance_criteria>
    - execution.py uses `asyncio.create_subprocess_exec` with an argv list, no `shell=True`, no `pytest.main()`; opens a fresh `SessionLocal()`; spec_path is run_id-derived.
    - execute router returns 202 + `{run_id, status}`, registers a BackgroundTask, 404s when the spec was never generated, behind `Depends(get_current_user)`.
    - execute_router included in main.py.
  </acceptance_criteria>
  <done>/execute runs the generated SYNC spec in an isolated subprocess and writes a passed/failed executions row with captured output; never runs the spec in-process.</done>
</task>

<task type="auto">
  <name>Task 2: 5 honest 501 stub endpoints with documented OpenAPI contracts</name>
  <read_first>
    - apps/api/app/routers/admin_llm.py lines 19-24 (small-router frame + explicit status_code — PATTERNS "stubs.py")
    - .planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-RESEARCH.md "Honest 501 stub" code example + Anti-pattern (never fabricate results)
    - .planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-CONTEXT.md Claude's Discretion (501 + documented contracts; surface complete and honest)
  </read_first>
  <action>
    Create `apps/api/app/schemas/stub.py` with request/response Pydantic models describing each stub's EVENTUAL shape (so the OpenAPI schema is COMPLETE even though behavior is 501): e.g. `HealRequest`/`HealResponse` (Phase 8 locator-heal shape), `CreateDefectRequest`/`CreateDefectResponse` (Phase 9 Jira-issue shape), `FlowsResponse` (Phase 5 flows), `CoverageResponse` (Phase 10 coverage), `DashboardResponse` (Phase 10 dashboard). Keep them minimal but representative. Create `apps/api/app/routers/stubs.py` mirroring admin_llm.py: `APIRouter(prefix="/api", tags=["stubs"], dependencies=[Depends(get_current_user)])`. Add the five endpoints, each `status_code=501` with a `summary=`, `response_model=` (or documented `responses={501: {...}}`), and a body that `raise HTTPException(status_code=501, detail="<name>: not implemented (Phase N)")` — POST /heal (Phase 8), POST /create-defect (Phase 9), GET /flows (Phase 5), GET /coverage (Phase 10), GET /dashboard (Phase 10). NEVER return a fabricated/plausible payload — 501 only (CONTEXT discretion + RESEARCH anti-pattern). Include `stubs_router` in main.py. After this, all 10 PLAT-02 endpoints exist (5 real from Plans 02-03 + this + 5 stubs here).
  </action>
  <verify>
    <automated>cd apps/api && uv run python -c "from app.routers.stubs import router; print(sorted({r.path for r in router.routes}))"</automated>
    <automated>grep -n 'stubs_router' apps/api/app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - stubs.py exposes /api/heal, /api/create-defect, /api/flows, /api/coverage, /api/dashboard, each behind `Depends(get_current_user)`, each returning 501, each with a request/response Pydantic contract in schemas/stub.py.
    - No stub returns a fabricated result payload (501 only).
    - stubs_router included in main.py; the full 10-endpoint PLAT-02 surface now exists.
  </acceptance_criteria>
  <done>The PLAT-02 surface is COMPLETE and HONEST: 5 real endpoints plus 5 documented 501 stubs that never fake results.</done>
</task>

<task type="auto">
  <name>Task 3: Functional tests — execute result row, full surface, run_id traceability</name>
  <read_first>
    - apps/api/tests/functional/test_targets.py lines 16, 154-170 (live-HTTP + 401 auth gate — PATTERNS "tests")
    - apps/api/tests/conftest.py (authed_client, poll_until_terminal, neo4j_session)
    - .planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-RESEARCH.md Validation Test Map (SC3, SC4) + "Spec execution" assertions
  </read_first>
  <action>
    Create `apps/api/tests/functional/test_execute.py` marked `[pytest.mark.functional, pytest.mark.graph]`: given a run that has a generated spec under workspaces/<run_id>/ (set up minimally — for the deterministic path, a tiny known-passing spec can be planted at the run_id path, OR chain from explore+generate under live_llm), POST /api/execute with `{run_id}`, assert 202, poll GET /api/executions/{run_id} to terminal via `poll_until_terminal`, and assert the executions row has `status in {"passed","failed"}` with non-empty `output` and a recorded `spec_path` (SC3). Create `apps/api/tests/functional/test_surface.py` marked `functional`: for EACH of the 10 PLAT-02 endpoints assert it EXISTS (no 404-not-found at the route level) — the 5 real endpoints return 401 when unauthenticated (auth gate present), and the 5 stubs return 501 when authenticated; assert `from shared.events import ExploreJob, ExecuteJob, RunStatusEvent` imports (schemas present — SC4). Create `apps/api/tests/functional/test_run_thread.py` marked `[pytest.mark.functional, pytest.mark.live_llm]`: drive one run_id through explore → generate-bdd → generate-scripts → execute via authed_client, polling to terminal at each async step, and assert the SAME run_id appears in the executions row and in the Neo4j Page nodes (traceability — the run_id threads the whole slice). Keep names/run_ids unique; never assert global counts.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_surface.py -q</automated>
    <automated>cd apps/api && uv run pytest tests/functional/test_execute.py -q -m graph</automated>
  </verify>
  <acceptance_criteria>
    - test_surface.py proves all 10 endpoints exist: 5 real → 401 unauth, 5 stubs → 501 authed; shared/events importable (SC4).
    - test_execute.py (graph) proves /execute → terminal status → executions row with output + spec_path (SC3).
    - test_run_thread.py (live_llm) proves one run_id threads explore→generate→execute→result.
    - The default gate `uv run pytest -m "not live_llm and not e2e and not graph" -q` stays green (includes test_surface.py).
  </acceptance_criteria>
  <done>SC3 and SC4 have automated proofs; the complete tracer (explore→generate→execute→result) is demonstrated end-to-end threaded by one run_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /execute | authenticated request with run_id |
| service → subprocess (uv run pytest) | a generated spec is executed as a child process |
| service → Postgres | result row written from a fresh session |
| client → stub endpoints | authenticated requests to not-yet-built surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Elevation | subprocess command injection via spec_path | mitigate | `create_subprocess_exec` argv list, no `shell=True`; spec_path is run_id-derived (`workspaces/<run_id>/test_login.py`), never raw user input (T-01-26) |
| T-03-16 | Tampering | running generated spec in the API process | mitigate | subprocess only — never `pytest.main()` in-process (Pitfall 3); the spec runs isolated |
| T-03-17 | Spoofing | unauthenticated /execute or stubs | mitigate | router-level `Depends(get_current_user)` on execute + stubs; surface test asserts 401 on real endpoints |
| T-03-18 | Info Disclosure | secrets in captured subprocess output | mitigate | spec uses SauceDemo public demo creds only; no target ciphertext/provider keys in the spec (PLAT-07, inherited from Plan 03) |
| T-03-19 | Repudiation | faked stub results | mitigate | stubs return 501 only with documented contracts; surface test asserts 501 (never a fabricated payload) |
</threat_model>

<verification>
- Default gate green: `cd apps/api && uv run pytest -m "not live_llm and not e2e and not graph" -q` (includes test_surface.py).
- Under graph_mode: `cd apps/api && uv run pytest tests/functional/test_execute.py -q -m graph` green.
- With keys + graph_mode: `cd apps/api && uv run pytest tests/functional/test_run_thread.py -q` proves the full threaded loop.
</verification>

<success_criteria>
- SC3 satisfied: POST /execute runs the spec and a result row lands in Postgres, retrievable via GET /executions.
- SC4 satisfied: all 10 PLAT-02 endpoints exist (5 real + 5 honest 501 stubs with documented contracts); shared/events schemas present.
- One run_id threads explore → generate → execute → result.
</success_criteria>

<output>
Create `.planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-04-SUMMARY.md` when done
</output>
