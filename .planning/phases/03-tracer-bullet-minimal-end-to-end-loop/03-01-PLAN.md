---
phase: 03-tracer-bullet-minimal-end-to-end-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/pyproject.toml
  - infra/docker-compose.yml
  - .env.example
  - apps/api/app/core/config.py
  - apps/api/app/core/neo4j_driver.py
  - apps/api/app/main.py
  - infra/scripts/graph_mode.py
  - apps/api/tests/conftest.py
autonomous: false
requirements: [PLAT-02]
user_setup: []

must_haves:
  truths:
    - "neo4j:2025 boots under the graph profile with web stopped and stays under the 3GB WSL cap"
    - "The api process opens one lifespan-managed AsyncGraphDatabase driver and closes it on shutdown"
    - "graph_mode helper stops web, brings neo4j healthy, runs work, restores web in one command"
    - "Functional tests can assert Page/NavigatesTo nodes via a host-side Bolt fixture and poll a run to terminal status"
  artifacts:
    - path: "apps/api/app/core/neo4j_driver.py"
      provides: "Lifespan Neo4j driver singleton (init/close/get)"
      contains: "AsyncGraphDatabase"
    - path: "infra/scripts/graph_mode.py"
      provides: "Stop-web-first compose choreography for graph work"
      contains: "def main"
    - path: "infra/docker-compose.yml"
      provides: "Trimmed neo4j service (heap 512m/pagecache 256m/mem_limit 1g)"
      contains: "NEO4J_server_memory_heap_max__size"
    - path: "apps/api/tests/conftest.py"
      provides: "neo4j_session host fixture + poll-until-terminal helper + graph marker"
      contains: "neo4j_session"
  key_links:
    - from: "apps/api/app/main.py"
      to: "apps/api/app/core/neo4j_driver.py"
      via: "init_neo4j()/close_neo4j() in lifespan"
      pattern: "init_neo4j|close_neo4j"
    - from: "infra/scripts/graph_mode.py"
      to: "infra/docker-compose.yml"
      via: "docker compose stop web → --profile graph up -d neo4j → start web"
      pattern: "stop.*web|--profile.*graph"
---

<objective>
Stand up the Neo4j seam and the test harness the rest of Phase 3 depends on: add the four new packages behind a legitimacy gate, trim the dormant neo4j compose service to fit the 3GB WSL cap, add a lifespan-managed async Neo4j driver, build the `graph_mode` web-stop-first choreography helper, and seed the Wave-0 test scaffold (host Bolt fixture + poll-until-terminal helper + `graph` marker).

Purpose: Every downstream slice (explore→graph, generate, execute) needs neo4j reachable, memory-safe, and assertable in tests. This plan delivers that floor without touching the pipeline yet.
Output: Trimmed neo4j compose block + env, `neo4j_driver.py`, `graph_mode.py`, conftest additions, four new deps in pyproject.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-CONTEXT.md
@.planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-RESEARCH.md
@.planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor mirrors. Use directly — no codebase exploration needed. -->

From apps/api/app/core/redis_client.py (the EXACT shape for neo4j_driver.py):
- module-global `_client: redis.Redis | None = None`
- `init_redis() -> redis.Redis` (idempotent, lazy `redis.from_url(settings.redis_url, decode_responses=True)`)
- `async def close_redis() -> None` (idempotent `await _client.aclose()`)
- `get_redis() -> redis.Redis` (lazy-open if `_client is None`)

From apps/api/app/main.py lifespan (lines 46-53) — add neo4j symmetrically:
- `init_redis()` on startup → add `init_neo4j()`
- `await close_redis()` on shutdown → add `await close_neo4j()` (before `await engine.dispose()`)

From apps/api/app/core/config.py — typed-field style; optional fields default None (anthropic_api_key pattern, lines 39-43). Add `neo4j_uri`, `neo4j_user`, `neo4j_password`.

From infra/scripts/reset_target.py — stdlib-only helper: `COMPOSE_FILE = (Path(__file__).resolve().parent.parent / "docker-compose.yml").resolve()`; `_wait_for_health(url, timeout)` with `time.monotonic()` deadline + `urllib.request.urlopen` + `time.sleep`; argv lists, no `shell=True`, `FileNotFoundError` guard, returncode branch; `main(argv)` → `SystemExit` exit codes 0/1/2.

From infra/docker-compose.yml — current dormant neo4j block (lines 120-123): `image: neo4j:2025`, `profiles: [graph]`, `mem_limit: 2g`. saucedemo healthcheck (lines 107-115) is the in-file wget pattern. api env block (lines 47-62) enumerates env explicitly.

From apps/api/tests/conftest.py — `_host_dsn()` host-rewrite pattern (lines 30-35); `client`/`authed_client` fixtures. From apps/api/tests/unit/conftest.py — redis host rewrite `.replace("@redis:", "@localhost:")` (lines 110-111).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate — verify new deps before uv add</name>
  <read_first>
    - apps/api/pyproject.toml (current deps; PATTERNS "package legitimacy" — mirror Phase 1 02-01 gate)
    - .planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-RESEARCH.md "Package Legitimacy Audit" table
  </read_first>
  <what-built>
    Four NEW top-level Python pins are required this phase, all CLAUDE.md-locked and PyPI-verified but tagged `[ASSUMED]` (slopcheck not run in research). Confirm legitimacy BEFORE any `uv add`:
    - neo4j 6.2.* — official Neo4j async Bolt driver — https://pypi.org/project/neo4j/ — github.com/neo4j/neo4j-python-driver
    - gherkin-official 40.* — Cucumber's official Gherkin parser — https://pypi.org/project/gherkin-official/ — github.com/cucumber/gherkin
    - pytest-bdd 8.1.* — BDD plugin for pytest — https://pypi.org/project/pytest-bdd/ — github.com/pytest-dev/pytest-bdd
    - jinja2 3.1.* — Pallets templating engine; the spec-skeleton renderer is now a first-class consumer (Plan 03), so it is pinned explicitly as a top-level dependency regardless of whether langchain pulls it transitively — https://pypi.org/project/Jinja2/ — github.com/pallets/jinja
  </what-built>
  <how-to-verify>
    1. Open each PyPI link above; confirm the package exists at the pinned version, maintainer is the named org (Neo4j / Cucumber / pytest-dev / Pallets), and the source repo matches.
    2. Confirm none is a typosquat (exact names: `neo4j`, `gherkin-official`, `pytest-bdd`, `jinja2`).
    3. Approve to proceed to `uv add`, or name any package to drop/replace.
  </how-to-verify>
  <resume-signal>Type "approved" to allow uv add, or list packages to reject</resume-signal>
  <acceptance_criteria>
    - Human has confirmed all four packages against their PyPI pages.
    - The gate blocks until explicit approval (never auto-advanced).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add deps + trim neo4j compose + Neo4j env/config/driver + lifespan wiring</name>
  <read_first>
    - apps/api/app/core/redis_client.py (EXACT analog for neo4j_driver.py — PATTERNS "neo4j_driver.py")
    - apps/api/app/main.py lines 46-62 (lifespan init/close + router includes — PATTERNS "main.py modify")
    - apps/api/app/core/config.py lines 20-43 (typed-field + optional-field style — PATTERNS)
    - infra/docker-compose.yml lines 47-62, 104-123 (api env block + dormant neo4j + saucedemo healthcheck)
    - .planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-RESEARCH.md Pitfall 1 (env-var underscore-doubling) + Pitfall 6 (URI host) + Runtime State Inventory
  </read_first>
  <action>
    After the Task-1 gate: run `uv add neo4j==6.2.* gherkin-official==40.* pytest-bdd==8.1.* jinja2==3.1.*` from `apps/api/`, confirming all four land in `[project.dependencies]` of apps/api/pyproject.toml. `jinja2==3.1.*` is added explicitly as a top-level pin even though langchain may already resolve it transitively — generation (Plan 03) is now a first-class consumer of Jinja2, so the dependency must be declared, not assumed; verify with `uv pip list | grep -i jinja2` that the resolved version satisfies `3.1.*`. Create `apps/api/app/core/neo4j_driver.py` mirroring redis_client.py verbatim: module-global `_driver: AsyncDriver | None = None`; `init_neo4j() -> AsyncDriver` doing `AsyncGraphDatabase.driver(settings.neo4j_uri, auth=(settings.neo4j_user, settings.neo4j_password))`; `async def close_neo4j() -> None` doing `await _driver.close()`; `get_neo4j() -> AsyncDriver` lazy-opening like get_redis (so unit imports outside the lifespan never get None). Mark the module a tracer-seam-friendly pooled driver in the docstring (one driver = a pool; never one per request). In apps/api/app/core/config.py add typed fields `neo4j_uri: str` (env NEO4J_URI), `neo4j_user: str` (env NEO4J_USER), `neo4j_password: str` (env NEO4J_PASSWORD) with the inline `# env NEO4J_*` comment convention. In apps/api/app/main.py wire `init_neo4j()` next to `init_redis()` and `await close_neo4j()` next to `await close_redis()` (before `await engine.dispose()`); import the driver from `app.core.neo4j_driver`. In infra/docker-compose.yml trim the neo4j block: keep `image: neo4j:2025` and `profiles: [graph]`, set `mem_limit: 1g`, add an `environment:` map with `NEO4J_AUTH: ${NEO4J_AUTH:-neo4j/please-change}`, `NEO4J_server_memory_heap_initial__size: 512m`, `NEO4J_server_memory_heap_max__size: 512m` (DOUBLE underscore on `max__size` per Pitfall 1), `NEO4J_server_memory_pagecache_size: 256m` (single underscore — no literal underscore in `pagecache.size`); add `ports:` `7687:7687` and `7474:7474`; add a wget healthcheck on 7474 mirroring the saucedemo healthcheck shape. Add `NEO4J_URI: bolt://neo4j:7687`, `NEO4J_USER: ${NEO4J_USER:-neo4j}`, `NEO4J_PASSWORD: ${NEO4J_PASSWORD:-please-change}` to the `api.environment` map (Pitfall 6 / 02-01 deviation #2 — the api enumerates env explicitly; omitting these makes Settings() fail). Do NOT add `depends_on: neo4j` to api (Pitfall 6 / A6 — keeps plain `up` working). Add `NEO4J_URI=bolt://localhost:7687`, `NEO4J_USER=neo4j`, `NEO4J_PASSWORD=please-change`, `NEO4J_AUTH=neo4j/please-change` to .env.example. After editing compose/env, rebuild the api image so the new deps and Settings fields take effect.
  </action>
  <verify>
    <automated>cd apps/api && uv pip list | grep -E "neo4j|gherkin-official|pytest-bdd|[Jj]inja2" && uv run python -c "from app.core.neo4j_driver import init_neo4j, close_neo4j, get_neo4j; from app.core.config import settings; print(settings.neo4j_uri)"</automated>
    <automated>grep -n 'jinja2' apps/api/pyproject.toml</automated>
    <automated>grep -n 'NEO4J_server_memory_heap_max__size\|NEO4J_server_memory_pagecache_size\|mem_limit: 1g' infra/docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `neo4j`, `gherkin-official`, `pytest-bdd`, and `jinja2` appear in `uv pip list` and in apps/api/pyproject.toml `[project.dependencies]` (jinja2 declared explicitly as `jinja2==3.1.*`, not relied on transitively).
    - `from app.core.neo4j_driver import init_neo4j, close_neo4j, get_neo4j` imports cleanly and `settings.neo4j_uri` resolves (Settings instantiates with the new fields).
    - infra/docker-compose.yml neo4j block has `mem_limit: 1g`, `NEO4J_server_memory_heap_max__size: 512m` (double underscore), `NEO4J_server_memory_pagecache_size: 256m` (single underscore), and a 7474 wget healthcheck; `api.environment` has `NEO4J_URI/NEO4J_USER/NEO4J_PASSWORD`; api has NO `depends_on: neo4j`.
    - main.py lifespan calls `init_neo4j()` and `await close_neo4j()`; .env.example has the four `NEO4J_*` keys.
  </acceptance_criteria>
  <done>Four new deps installed and gated (jinja2 pinned top-level, not transitive); neo4j compose trimmed with the exact (non-self-invalidating) env-var names; lifespan-managed driver wired; api boots with the new Settings fields.</done>
</task>

<task type="auto">
  <name>Task 3: graph_mode helper + Wave-0 test scaffold (neo4j fixture, poll helper, graph marker)</name>
  <read_first>
    - infra/scripts/reset_target.py (EXACT stdlib-helper analog — PATTERNS "graph_mode.py": docstring exit-code contract, COMPOSE_FILE relative resolve, _wait_for_health, argv lists, main()→SystemExit)
    - apps/api/tests/conftest.py lines 30-35, 55-72 (host-rewrite + client/authed_client — PATTERNS "tests modify")
    - apps/api/tests/unit/conftest.py lines 110-111 (host rewrite for redis — analog for the bolt host rewrite)
    - apps/api/pyproject.toml lines 46-50 (markers list)
    - .planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-RESEARCH.md Pitfall 4 (stop-web-first ordering) + Validation "How to test the hard parts" + Wave 0 Gaps
  </read_first>
  <action>
    Create `infra/scripts/graph_mode.py` mirroring reset_target.py: stdlib-only, `COMPOSE_FILE` resolved relative to `__file__`, argv lists with no `shell=True`, `FileNotFoundError` guard, returncode branching, `main(argv)` → `SystemExit` with exit codes 0/1/2, and a docstring stating the memory-math contract. Its `up` path MUST order operations per Pitfall 4: (1) `docker compose -f <COMPOSE_FILE> stop web`, (2) `docker compose -f <COMPOSE_FILE> --profile graph up -d neo4j` then poll the neo4j Bolt/HTTP endpoint (reuse a `_wait_for_health`-shaped loop hitting `http://localhost:7474`) until healthy or timeout, (3) return success so the caller runs work; provide a `down`/`restore` path that does `docker compose -f <COMPOSE_FILE> start web`. Accept a subcommand argument (e.g. `up`/`down`) so the test harness and humans drive it. NEVER start neo4j before stopping web (Pitfall 4). In apps/api/pyproject.toml add a `graph` marker to the markers list: `"graph: needs the neo4j graph profile active (run under graph_mode; web stopped)"`. In apps/api/tests/conftest.py add: (a) a `neo4j_session` fixture that connects a host-side `AsyncGraphDatabase.driver("bolt://localhost:7687", auth=(neo4j_user, neo4j_password))` (rewrite the in-cluster `neo4j` host to `localhost`, mirroring the redis host rewrite) and yields a session, closing the driver on teardown; (b) a `poll_until_terminal(client, run_id, timeout=60, interval=1.0)` async helper that GETs `/api/executions/{run_id}` until `status in {"passed","failed"}` or raises on timeout (mirror reset_target's `_wait_for_health` deadline loop) — used by downstream functional tests, NEVER assert immediately after a 202. These helpers MUST import lazily / tolerate the endpoints not existing yet (they are consumed by Plans 02-04), so adding them here does not break the current suite.
  </action>
  <verify>
    <automated>cd apps/api && uv run python -c "import ast; ast.parse(open('../../infra/scripts/graph_mode.py').read()); print('graph_mode parses')"</automated>
    <automated>grep -v '^#' apps/api/pyproject.toml | grep -c 'graph:'</automated>
    <automated>cd apps/api && uv run pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - `infra/scripts/graph_mode.py` parses, is stdlib-only, and its `up` path runs `stop web` BEFORE `--profile graph up -d neo4j` (assert ordering by source inspection: the `stop`/`web` argv appears before the `--profile`/`graph` argv).
    - apps/api/pyproject.toml markers list contains a `graph` marker (grep count ≥ 1 on non-comment lines).
    - `cd apps/api && uv run pytest tests/unit -q` stays green (new fixtures/helpers do not break the existing suite).
    - `neo4j_session` and `poll_until_terminal` are defined in tests/conftest.py.
  </acceptance_criteria>
  <done>graph_mode helper enforces stop-web-first ordering; the test harness has a host Bolt fixture, a poll-to-terminal helper, and a registered `graph` marker — the Wave-0 floor for all downstream functional tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host shell → docker compose | graph_mode builds compose argv; container orchestration |
| api process → neo4j Bolt | new auth'd Bolt connection |
| compose env → Settings | NEO4J_* injected; secret material (NEO4J_AUTH) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | graph_mode.py compose argv | mitigate | argv as lists, no `shell=True`, all tokens are literal compose constants (mirror reset_target T-01-26) — no user input reaches argv |
| T-03-02 | Spoofing | Neo4j Bolt auth | mitigate | `NEO4J_AUTH`/`NEO4J_USER`/`NEO4J_PASSWORD` from env only; default `please-change` flagged in .env.example; never hard-coded plaintext in compose |
| T-03-03 | Info Disclosure | NEO4J_PASSWORD in logs | mitigate | password lives in Settings + env only; structlog redaction already covers `password`/`secret`/`token` keys; never logged by the driver init |
| T-03-04 | Denial of Service | host OOM (3GB cap) | mitigate | mem_limit 1g + trimmed heap/pagecache + graph_mode stops web (1.5g) BEFORE neo4j starts (Pitfall 4) |
| T-03-SC | Tampering | neo4j/gherkin-official/pytest-bdd/jinja2 installs | mitigate | Task-1 blocking-human legitimacy gate before `uv add`; all four CLAUDE.md-locked + PyPI-verified |
</threat_model>

<verification>
- `cd apps/api && uv run pytest tests/unit -q` green (no regression from new fixtures).
- Under graph_mode (`python infra/scripts/graph_mode.py up`): neo4j reaches healthy, web is stopped, `docker stats` stays under the 3GB cap (Manual-Only memory check in 03-VALIDATION).
- api container restarts cleanly with the new Settings fields and lifespan driver.
</verification>

<success_criteria>
- Four new packages installed behind a blocking legitimacy gate (jinja2 pinned top-level).
- neo4j compose service trimmed to heap 512m / pagecache 256m / mem_limit 1g with the EXACT env-var names (no silent OOM).
- Lifespan-managed AsyncGraphDatabase driver opens at startup, closes at shutdown.
- graph_mode enforces stop-web-before-start-neo4j; test harness has the neo4j fixture + poll helper + graph marker.
</success_criteria>

<output>
Create `.planning/phases/03-tracer-bullet-minimal-end-to-end-loop/03-01-SUMMARY.md` when done
</output>
