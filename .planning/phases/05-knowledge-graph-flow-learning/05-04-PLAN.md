---
phase: 05-knowledge-graph-flow-learning
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - apps/api/tests/fixtures/ground_truth/saucedemo.json
  - apps/api/app/services/kg/coverage.py
  - apps/api/app/routers/kg.py
  - apps/api/app/schemas/kg.py
  - apps/api/tests/unit/test_coverage.py
  - apps/api/tests/functional/test_coverage_live.py
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "A committed hand-authored JSON ground-truth fixture enumerates SauceDemo pages + key flows"
    - "compute_coverage(ground_truth, discovered) returns matched ÷ total as a deterministic percentage (fingerprint primary, normalized-url fallback)"
    - "GET /coverage surfaces the real metric, honestly reporting 'not yet measured' when no discovered graph exists"
    - "The coverage metric is unit-tested on a fixture KG to a KNOWN expected percentage with no provider key"
    - "The live ≥80% gate is documented as Manual-Only (needs a real exploration + keys)"
  artifacts:
    - path: "apps/api/tests/fixtures/ground_truth/saucedemo.json"
      provides: "Hand-labeled SauceDemo ground-truth pages + flows (QUAL-01 reference)"
      contains: "pages"
    - path: "apps/api/app/services/kg/coverage.py"
      provides: "Pure matched ÷ total coverage metric + normalize_url"
      contains: "def compute_coverage"
    - path: "apps/api/tests/unit/test_coverage.py"
      provides: "Deterministic coverage-metric proof on a fixture GT+KG → known %"
  key_links:
    - from: "apps/api/app/routers/kg.py"
      to: "apps/api/app/services/kg/coverage.py"
      via: "GET /coverage calls compute_coverage over the fixture + the discovered graph"
      pattern: "compute_coverage"
    - from: "apps/api/app/services/kg/coverage.py"
      to: "apps/api/tests/fixtures/ground_truth/saucedemo.json"
      via: "reads the committed ground-truth fixture"
      pattern: "ground_truth"
---

<objective>
Ship the QUAL-01 trust gate: a committed hand-authored JSON ground-truth fixture of SauceDemo pages + key flows, a PURE deterministic coverage metric (matched ÷ total, fingerprint primary / normalized-URL fallback), wire it into the real `GET /coverage`, unit-test the metric to a KNOWN percentage on a fixture KG (no keys), and document the live ≥80% gate as Manual-Only. Coverage is NEVER fabricated — `measured=false` honestly when no discovered graph exists.

Purpose: QUAL-01 — exploration coverage measured against a hand-labeled SauceDemo ground-truth graph (>80% target). The metric logic ships + is deterministically tested now; the live ≥80% proof on a real discovery is gated on provider keys (Manual-Only, same posture as Phase 4).
Output: `tests/fixtures/ground_truth/saucedemo.json`, `kg/coverage.py`, the real `GET /coverage`, the deterministic coverage unit test, the Manual-Only live gate test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-knowledge-graph-flow-learning/05-CONTEXT.md
@.planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md
@.planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md
@.planning/phases/05-knowledge-graph-flow-learning/05-VALIDATION.md
@.planning/phases/05-knowledge-graph-flow-learning/05-03-PLAN.md

<interfaces>
<!-- Coverage metric (RESEARCH lines 454-468): -->
<!-- def compute_coverage(ground_truth, discovered) -> dict: match a GT page if its fingerprint -->
<!--   in discovered fps OR normalize_url(url) in discovered urls (fp primary, url fallback). -->
<!--   return {screens_total, screens_covered, coverage_percent, matched}. -->

<!-- Ground-truth JSON shape (RESEARCH lines 470-489): {app, pages:[{name,url,fingerprint?}], flows:[{name,pages:[...]}]} -->
<!-- In-cluster host is http://saucedemo:80; public is https://www.saucedemo.com — normalize to PATH-ONLY -->
<!--   so host differences don't break matching (RESEARCH Pitfall 4 / Open Q2). -->

<!-- Coverage response model from plan 03 (schemas/kg.py CoverageResponse): includes `measured: bool` -->
<!-- The graph-marked test infra: conftest neo4j_session + the `graph` marker; live test adds `live_llm`. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 + metric): ground-truth fixture + kg/coverage.py + deterministic unit test</name>
  <files>apps/api/tests/fixtures/ground_truth/saucedemo.json, apps/api/app/services/kg/coverage.py, apps/api/tests/unit/test_coverage.py</files>
  <behavior>
    - compute_coverage(GT, discovered) matches a GT page when its fingerprint is in the discovered fingerprints OR normalize_url(its url) is in the discovered normalized urls (fp primary, url fallback).
    - On a fixture GT (7 pages) + a fixture discovered graph that contains a KNOWN subset, the returned coverage_percent equals the hand-computed expected value (e.g. 6/7 → 85.7).
    - An empty discovered graph yields coverage_percent 0.0 and the caller can treat it as "not yet measured" (screens_covered == 0 / no pages).
    - normalize_url strips scheme+host to a path-only form so the in-cluster host and the public host match.
    - flow coverage = matched GT flows ÷ total GT flows by the same page-set matching.
  </behavior>
  <read_first>
    - .planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md (compute_coverage Code Example lines 454-468; ground-truth JSON shape lines 470-489; Pitfall 4 matching rule lines 363-367; Open Q2 normalization lines 524-527; A5)
    - apps/api/app/services/explorer/budget.py (pure-function purity discipline — no I/O in the metric)
    - apps/api/tests/fixtures/kg/pages.json (the plan-01 fixture KG snapshot — reuse/extend for the discovered side of the coverage test)
    - .planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md (coverage.py lines 20; ground-truth no-analog note lines 248)
  </read_first>
  <action>
    Create `tests/fixtures/ground_truth/saucedemo.json` hand-authored per RESEARCH lines 470-489: an `app` field, a `pages` array of the 7 canonical SauceDemo pages (Login, Inventory, Item Detail, Cart, Checkout: Info, Checkout: Overview, Checkout: Complete) each with `name`+`url`, and a `flows` array (Login; Add to Cart & Checkout) with their page-name sequences. Commit it (diffable, no live deps).

    Create `app/services/kg/coverage.py` as a PURE module: `normalize_url(url)` strips scheme+host to a path-only canonical form (so `http://saucedemo:80/cart.html` and `https://www.saucedemo.com/cart.html` both normalize to `/cart.html`); `compute_coverage(ground_truth: dict, discovered: dict) -> dict` implements matched ÷ total — a GT page matches when its `fingerprint` (if present) is in the discovered fingerprints OR its `normalize_url(url)` is in the discovered normalized urls (fp primary, url fallback, Pitfall 4). Return `screens_total`, `screens_covered`, `coverage_percent` (rounded 1dp, 0.0 when total is 0), `flows_total`, `flows_covered`, and a `matched` name list. Add a `load_ground_truth(path)` stdlib-`json` loader. No I/O beyond reading the fixture file; no neo4j, no keys.

    Create `tests/unit/test_coverage.py` (default gate, no keys): load the committed ground truth + a fixture discovered graph (a known subset of pages by url/fingerprint) and assert `coverage_percent` equals the hand-computed expected value; assert the fp-primary path matches when a fingerprint is present; assert the url-fallback path matches across hosts (in-cluster vs public) via `normalize_url`; assert an empty discovered graph yields 0.0; assert flow coverage counts correctly.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_coverage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/ground_truth/saucedemo.json` parses as JSON with 7 `pages` and ≥2 `flows`.
    - `kg/coverage.py` defines `normalize_url`, `compute_coverage`, `load_ground_truth`; `compute_coverage` is pure (no neo4j/llm imports) and clamps/rounds coverage_percent.
    - Matching is fingerprint-primary, normalized-URL fallback; `normalize_url` is path-only so cross-host urls match.
    - `test_coverage.py` proves a KNOWN expected % on a fixture GT+KG, the cross-host url-fallback match, the empty-graph 0.0 case, and flow coverage — all green with no keys/stack.
  </acceptance_criteria>
  <done>The committed ground truth + the pure deterministic coverage metric exist and pass the known-% unit test without keys.</done>
</task>

<task type="auto">
  <name>Task 2: Wire real GET /coverage + the Manual-Only live ≥80% gate test</name>
  <files>apps/api/app/routers/kg.py, apps/api/app/schemas/kg.py, apps/api/tests/functional/test_coverage_live.py</files>
  <read_first>
    - apps/api/app/routers/kg.py (the GET /coverage handler from plan 03 returning the honest measured=false placeholder — replace its body with the real metric)
    - apps/api/app/schemas/kg.py (CoverageResponse incl. `measured` from plan 03)
    - apps/api/app/services/kg/coverage.py + kg/reader.py (compute_coverage + list_pages/graph_summary to gather the discovered side)
    - apps/api/tests/functional/test_explore_discovery.py (the live_llm+graph test posture — neo4j_session, in-cluster host, skip without keys)
    - .planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md (D-08 live gate Manual-Only lines 18, 571; coverage honest "not yet measured" lines 121 of UI-SPEC)
    - .planning/phases/05-knowledge-graph-flow-learning/05-VALIDATION.md (Manual-Only Verifications table lines 70-77)
  </read_first>
  <action>
    Replace the `GET /coverage` handler body in `routers/kg.py` with the real metric: load the committed ground truth via `coverage.load_ground_truth`, gather the discovered pages (fingerprints + urls) from `kg.reader`, call `compute_coverage`, and return the `CoverageResponse`. When the discovered graph is empty (no pages), return `measured=false` with `coverage_percent` omitted/null-equivalent and zero counts — the UI renders "Not yet measured" (NEVER a fabricated 0%-as-measured). When pages exist, `measured=true` with the computed percentage. Keep the handler read-only and behind the existing router auth gate.

    Create `tests/functional/test_coverage_live.py` marked `[functional, graph, live_llm]` and skipped without a provider key: the Manual-Only ≥80% gate — run (or assume a freshly explored) live SauceDemo graph, call `compute_coverage` against the committed ground truth, and assert `coverage_percent >= 80.0`. Document in the test docstring that this is the QUAL-01 trust gate, requires keys + a real exploration, and that the deterministic `test_coverage.py` proves the metric logic without keys. Add/confirm the Manual-Only entry is reflected so `/gsd:verify-work` surfaces it.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest -m graph tests/functional/test_kg_endpoints.py -x 2>&1 | tail -10  # /coverage now real; live ≥80% test is live_llm (skipped without keys)</automated>
  </verify>
  <acceptance_criteria>
    - `GET /coverage` returns the real `compute_coverage` result when a discovered graph exists, and `measured=false` (no fabricated percentage) when it is empty.
    - `test_coverage_live.py` carries `[functional, graph, live_llm]`, is skipped without keys, and asserts `coverage_percent >= 80.0` against the committed ground truth.
    - The endpoint test (`test_kg_endpoints.py`) for /coverage passes under `-m graph` against a seeded graph (measured=true with a real percentage).
    - The live ≥80% gate is documented as Manual-Only (test docstring + reflected for verify-work).
    - The single-write-path grep test stays green (coverage adds no write-Cypher).
  </acceptance_criteria>
  <done>GET /coverage surfaces the real metric honestly; the live ≥80% gate is a documented Manual-Only test; coverage is never fabricated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ground-truth fixture → coverage metric | a tampered fixture could inflate/deflate reported coverage |
| discovered graph → coverage report | an empty/partial graph must not report a fabricated percentage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering / Integrity | ground-truth fixture | mitigate | Ground truth is a committed, diffable, version-controlled JSON file (D-07); any change is reviewable in git — no runtime/network-sourced ground truth |
| T-05-14 | Integrity (fabricated coverage) | GET /coverage | mitigate | `measured=false` honest shape when no discovered graph (D-08); never a fabricated 0%/percentage; metric is pure + deterministically unit-tested to a known value |
| T-05-15 | Repudiation | QUAL-01 ≥80% claim | mitigate | The live ≥80% gate is an explicit Manual-Only test requiring keys + a real exploration; the metric logic is proven deterministically without keys so the claim is auditable |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (JSON via stdlib; D-07 prefers JSON to avoid a YAML dep) — slopcheck N/A |
</threat_model>

<verification>
- Default gate: `uv run pytest tests/unit/test_coverage.py -x -q` green (known % proven without keys).
- Under graph_mode: `uv run pytest -m graph tests/functional/test_kg_endpoints.py -x` green (real /coverage on a seeded graph); `docker compose stop neo4j` after (Pitfall 5).
- Manual-Only (keys + real exploration): `uv run pytest -m "graph and live_llm" tests/functional/test_coverage_live.py` asserts ≥80% — the QUAL-01 trust gate.
- `uv run pytest tests/unit/test_single_write_path.py -x` still green.
</verification>

<success_criteria>
- QUAL-01: a hand-labeled SauceDemo ground-truth fixture + a deterministic coverage metric (unit-tested to a known %); GET /coverage surfaces it honestly; the live ≥80% gate is documented Manual-Only; coverage is never fabricated.
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-graph-flow-learning/05-04-SUMMARY.md` when done.
</output>
