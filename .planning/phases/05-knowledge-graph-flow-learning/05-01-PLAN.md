---
phase: 05-knowledge-graph-flow-learning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/app/services/kg/__init__.py
  - apps/api/app/services/kg/schema.py
  - apps/api/app/services/kg/writer.py
  - apps/api/app/services/explorer/nodes.py
  - apps/api/app/main.py
  - apps/api/tests/fixtures/kg/pages.json
  - apps/api/tests/unit/test_single_write_path.py
  - apps/api/tests/functional/test_kg_idempotency.py
  - apps/api/tests/functional/test_kg_schema.py
autonomous: true
requirements: [KG-01, KG-03, KG-05]
must_haves:
  truths:
    - "Re-running the writer over the same fixture node set produces ~0 duplicate nodes (counts unchanged)"
    - "Every node carries first_seen (set once) and last_verified (bumped each run)"
    - "first_seen is immutable across re-observation; last_verified advances"
    - "A uniqueness constraint on Page.fingerprint exists after startup"
    - "No Cypher write statement exists anywhere under apps/api/app/ outside kg/writer.py and kg/schema.py"
    - "The explorer persist node calls kg_writer.* and contains zero MERGE/CREATE/SET/DELETE Cypher"
  artifacts:
    - path: "apps/api/app/services/kg/writer.py"
      provides: "The single Neo4j write path: upsert_*/link_* with idempotent fingerprint-MERGE + freshness + read-back guard"
      contains: "async def upsert_page"
    - path: "apps/api/app/services/kg/schema.py"
      provides: "Label/edge constants + uniqueness constraint Cypher + ensure_constraints()"
      contains: "REQUIRE"
    - path: "apps/api/tests/unit/test_single_write_path.py"
      provides: "Grep enforcement: zero write-Cypher outside the writer/schema"
    - path: "apps/api/tests/functional/test_kg_idempotency.py"
      provides: "Deterministic re-run proof (counts unchanged, first_seen immutable, last_verified bumped)"
  key_links:
    - from: "apps/api/app/services/explorer/nodes.py"
      to: "apps/api/app/services/kg/writer.py"
      via: "persist node delegates to writer.upsert_*/link_*"
      pattern: "kg.*writer|from app.services.kg"
    - from: "apps/api/app/main.py"
      to: "apps/api/app/services/kg/schema.py"
      via: "lifespan calls ensure_constraints (graceful when neo4j down)"
      pattern: "ensure_constraints"
---

<objective>
Build `app/services/kg/` as the SINGLE Neo4j write path (KG-05). Lift the explorer's inline persist Cypher verbatim into `kg/writer.py`, change the MERGE key from `key` to the Phase-4 structural `fingerprint`, add `first_seen`/`last_verified` freshness (the ON CREATE / ON MATCH split), back each MERGE with a uniqueness constraint created idempotently at startup, and refactor `explorer/nodes.py` into a thin delegate that holds zero Cypher. A grep test enforces the single write path; a deterministic re-run harness proves ~0 duplicates.

Purpose: Turn Phase-4's working-but-uncanonical inline writes into a proper, idempotent, fresh knowledge-graph write layer — the foundation every later slice reads from. This is a REFACTOR first (KG-05/KG-03), a build second.
Output: `kg/__init__.py`, `kg/schema.py`, `kg/writer.py`; refactored `explorer/nodes.py`; lifespan constraint setup in `main.py`; the grep + idempotency + schema tests; a fixture KG dataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-knowledge-graph-flow-learning/05-CONTEXT.md
@.planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md
@.planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md

<interfaces>
<!-- The exact write discipline to PRESERVE inside the writer (from explorer/nodes.py:461-469): -->
<!-- async def _write(tx) -> int: result = await tx.run(cypher, **params); record = await result.single(); return int(record["n"]) if record else 0 -->
<!-- async with get_neo4j().session() as session: written = await session.execute_write(_write) -->
<!-- if written < 1: raise RuntimeError(...)   # SC1: a 0-count write FAILS -->

<!-- The lifespan driver seam (apps/api/app/core/neo4j_driver.py): -->
<!-- from app.core.neo4j_driver import get_neo4j  — reuse the singleton; NEVER a second driver -->
<!-- init_neo4j() is LAZY (no connect at boot). ensure_constraints MUST tolerate an unreachable neo4j at startup. -->

<!-- llm_gateway.complete signature (NOT used in this plan, used in plan 02): -->
<!-- (db, messages, *, operation_type, run_id=None, model=None, temperature=0, max_tokens, ...) -->

<!-- The Cypher being lifted lives at explorer/nodes.py:379-557: -->
<!--   _build_persist_cypher (379-402): Page/Page/Element + NavigatesTo + HAS_ELEMENT -->
<!--   _write_workflow_step (502-525): Workflow-[STEP {order}]->Page -->
<!--   _write_form_validation (528-557): Page-[HAS_FORM]->Form{validation_rules} -->
<!--   persist_to_neo4j (405-499): the node that orchestrates the above -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Test scaffold + fixture KG dataset + grep enforcement</name>
  <files>apps/api/tests/fixtures/kg/pages.json, apps/api/tests/unit/test_single_write_path.py, apps/api/tests/functional/test_kg_idempotency.py, apps/api/tests/functional/test_kg_schema.py</files>
  <read_first>
    - apps/api/tests/conftest.py (the `neo4j_session` fixture lines 50-78; `_host_bolt_uri` in-cluster→localhost rewrite; graph marker discipline)
    - apps/api/tests/unit/conftest.py (mocked-gateway scaffold pattern; load_dotenv override=False)
    - apps/api/pyproject.toml lines 56-60 (markers: functional/e2e/live_llm/graph; asyncio_mode=auto; pythonpath)
    - .planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md (Idempotency proof harness Code Example lines 432-452; Wave 0 Gaps lines 580-594)
    - .planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md (Shared Patterns: single write path lines 202-204; graph-marked tests lines 230-232)
  </read_first>
  <action>
    Create `tests/fixtures/kg/pages.json`: a small hand-built fixture node set (3-5 page records, each with fingerprint, url, title) plus a couple of element/edge records, so writer/MERGE/freshness/idempotency are testable WITHOUT a live browser or LLM. Shape mirrors the RESEARCH ground-truth shape but is a KG snapshot (distinct file from the slice-4 ground truth).

    Create `tests/unit/test_single_write_path.py` (default gate, no neo4j): the KG-05 grep enforcement. Walk `apps/api/app/` for write-Cypher tokens (`MERGE`, `CREATE (`, `SET `, `DETACH DELETE`, `REMOVE `) using a filtered scan that strips comment lines (`^\s*#`) before counting (grep gate hygiene). Assert ANY hit outside `app/services/kg/writer.py` and `app/services/kg/schema.py` fails the test. This test goes RED until Task 2 removes the explorer's inline Cypher — that is intended (it is the enforcement).

    Create `tests/functional/test_kg_idempotency.py` (`pytestmark = [pytest.mark.functional, pytest.mark.graph]`, NOT live_llm): the deterministic re-run proof from RESEARCH lines 432-452 — upsert the fixture node set at now1, count + capture first_seen map; upsert the SAME set at now2; assert counts unchanged, first_seen map unchanged, all last_verified == now2. Also assert the uniqueness constraint blocks a duplicate-fingerprint create. Use the `neo4j_session` fixture; clear the graph (`MATCH (n) DETACH DELETE n`) in setup so the proof runs on a fresh graph (one-time key→fingerprint migration posture per RESEARCH Runtime State Inventory).

    Create `tests/functional/test_kg_schema.py` (graph): assert the writer creates the canonical labels/edges — after upserting fixture pages + an element + a button + a business entity + a form, query that `:Page`/`:Button`/`:Form`/`:Element`/`:BusinessEntity` nodes and `NavigatesTo`/`HAS_ELEMENT`/`HAS_BUTTON`/`HAS_FORM`/`Submits`/`Creates` edges exist.

    These tests import `app.services.kg.writer` and `app.services.kg.schema` which Tasks 1-2 create; mark them so they run under graph_mode only.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_single_write_path.py -x 2>&1 | tail -5  # expected RED until Task 2 (enforcement target)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/kg/pages.json` exists and parses as JSON with a `pages` array of ≥3 records each carrying `fingerprint`, `url`, `title`.
    - `tests/unit/test_single_write_path.py` exists, strips `^\s*#` comment lines before token counting, and asserts zero write-Cypher outside `kg/writer.py`+`kg/schema.py`.
    - `tests/functional/test_kg_idempotency.py` carries `[functional, graph]` markers (NOT live_llm), clears the graph in setup, and asserts: counts unchanged, first_seen map identical, all last_verified == now2.
    - `tests/functional/test_kg_schema.py` carries the `graph` marker and asserts the canonical labels + edges exist.
  </acceptance_criteria>
  <done>Wave-0 fixtures + four test files exist; the grep test runs (RED until Task 2); the graph tests collect under `-m graph`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: kg/schema.py (constraints + constants) and kg/writer.py (the single write path)</name>
  <files>apps/api/app/services/kg/__init__.py, apps/api/app/services/kg/schema.py, apps/api/app/services/kg/writer.py</files>
  <behavior>
    - upsert_page MERGEs on `fingerprint`; ON CREATE sets first_seen+last_verified; a second upsert with a new `now` leaves first_seen unchanged and advances last_verified; node count stays 1.
    - A write that returns count 0 raises RuntimeError (read-back guard, SC1).
    - ensure_constraints(driver) runs all `CREATE CONSTRAINT ... IF NOT EXISTS` idempotently and does NOT raise when neo4j is unreachable (graceful-boot contract).
    - link_navigates_to / link_submits / link_creates / link_updates / link_deletes / link_has_element / link_has_button / link_has_form create the canonical edges, each read-back guarded.
  </behavior>
  <read_first>
    - apps/api/app/services/explorer/nodes.py lines 379-557 (the inline Cypher to lift: _build_persist_cypher, _write_workflow_step, _write_form_validation, and their _write(tx) bodies)
    - apps/api/app/core/neo4j_driver.py (get_neo4j singleton; LAZY init; liveness_check_timeout)
    - apps/api/app/core/checkpointer.py (the setup()-at-startup analog; note it connects EAGERLY — the KG constraint setup must NOT, to preserve graceful boot)
    - apps/api/app/services/explorer/risk.py (DENY_VERBS frozenset — the module-level-constant-as-source-of-truth pattern for schema.py)
    - .planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md (Pattern 1 lines 185-225 freshness MERGE; schema lines 383-415; Canonical schema; BusinessEntity verb→entity map lines 411-415; Pitfall 1/2/6)
    - .planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md (writer.py assignment lines 45-67; schema.py assignment lines 71-84)
  </read_first>
  <action>
    Create `kg/schema.py` as the one source of truth for labels, edge types, and constraints. Define module-level string constants for each canonical label (`Page`, `Button`, `Form`, `Workflow`, `Element`, `BusinessEntity`) and edge type (`NavigatesTo`, `Submits`, `Creates`, `Updates`, `Deletes`, `HAS_ELEMENT`, `HAS_FORM`, `HAS_BUTTON`, `STEP`). Define `CREATE CONSTRAINT ... IF NOT EXISTS FOR (p:Page) REQUIRE p.fingerprint IS UNIQUE` and analogous unique constraints for Button.key, Form.key, Element.key, BusinessEntity.name. Define a deterministic `VERB_ENTITY_MAP` (label substrings → BusinessEntity name/kind) per RESEARCH lines 411-415: `add-to-cart`→Cart(Updates), `checkout`/`finish`→Order(Creates), inventory/product labels→Product(catalog_item). Write `async def ensure_constraints(driver)` that runs every constraint via a managed write session, wrapped so a Neo4j connection error logs-and-returns (does NOT raise) — preserving the graceful-boot-without-neo4j contract (constraints then get created on first reachable write or next boot).

    Create `kg/writer.py` as the ONLY write path. Lift the three Cypher builders from `explorer/nodes.py` verbatim, then apply the only semantic edits: (a) MERGE key `{key:$...}` → `{fingerprint:$fingerprint}` for Page (Button/Form/Element/BusinessEntity keep their unique key/name), (b) add the freshness split — `ON CREATE SET first_seen=$now` and `ON MATCH SET last_verified=$now` plus a trailing `SET n.last_verified=coalesce(n.last_verified,$now)` so a freshly-created node also carries last_verified in the same statement; NEVER touch first_seen on ON MATCH (Pitfall 2). Provide module-level `async def upsert_page/upsert_button/upsert_form/upsert_workflow/upsert_element/upsert_business_entity` and `async def link_navigates_to/link_submits/link_creates/link_updates/link_deletes/link_has_element/link_has_button/link_has_form`. Every function reuses the EXACT read-back pattern (`_write(tx)` returning `int(record["n"])`, `get_neo4j().session()`, `execute_write`, raise on `<1`). Parameterized Cypher only — labels/edge-types come from the schema.py constants, never interpolated from page-derived text. Reuse the lifespan `get_neo4j()` singleton; never construct a second driver. Create `kg/__init__.py` exporting the writer + ensure_constraints (mirror `explorer/__init__.py`).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest -m graph tests/functional/test_kg_idempotency.py tests/functional/test_kg_schema.py -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `kg/schema.py` defines a `REQUIRE p.fingerprint IS UNIQUE` constraint, label/edge constants, `VERB_ENTITY_MAP`, and `ensure_constraints(driver)` that does NOT raise when neo4j is unreachable.
    - `kg/writer.py` exposes `upsert_page` (MERGE on `fingerprint`) plus the other 5 upserts and 8 link functions, each with the read-back guard that raises on a 0-count write.
    - `first_seen` appears ONLY in `ON CREATE SET`; `last_verified` is bumped on ON MATCH with a `coalesce` on create.
    - `test_kg_idempotency.py` passes under `-m graph`: counts unchanged on re-run, first_seen immutable, last_verified advanced, duplicate-fingerprint create blocked by the constraint.
    - `test_kg_schema.py` passes under `-m graph`: canonical labels + edges present.
    - No `AsyncGraphDatabase.driver(` call appears in `kg/writer.py` (reuses `get_neo4j()`).
  </acceptance_criteria>
  <done>The single write path exists with idempotent fingerprint-MERGE + freshness; constraints set up gracefully; idempotency + schema graph tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor explorer/nodes.py to delegate + wire ensure_constraints into lifespan</name>
  <files>apps/api/app/services/explorer/nodes.py, apps/api/app/main.py</files>
  <read_first>
    - apps/api/app/services/explorer/nodes.py (the FULL file — persist_to_neo4j 405-499 orchestrates the writes; _build_persist_cypher/_write_workflow_step/_write_form_validation 379-557 are REMOVED; the pure helpers parse_workflow_flag/extract_validation_rules STAY)
    - apps/api/app/main.py lines 13, 54-67, 81 (the init_neo4j import; the lifespan body where init_checkpointer() runs; the include_router block)
    - .planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md (nodes.py refactor lines 22, 45-67; main.py lines 194-196; the "lift verbatim, remove not wrap" mandate at top)
    - .planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md (Deprecated lines 501-503: _build_persist_cypher etc. MOVE; D-02 refactor-not-wrap)
  </read_first>
  <action>
    Refactor `explorer/nodes.py` per D-02 (refactor, NOT wrap): DELETE `_build_persist_cypher`, `_write_workflow_step`, `_write_form_validation`, and the `_write(tx)`/`execute_write` bodies inside `persist_to_neo4j`. Rewrite `persist_to_neo4j` so it computes the same params it computes today (a_key now is the structural fingerprint `state["current_fingerprint"]`, b_key, element key/chain/history via the existing `merge_locator_history`) and then DELEGATES: call `kg_writer.upsert_page(...)`, `kg_writer.upsert_element(...)`, `kg_writer.link_navigates_to(...)`, `kg_writer.link_has_element(...)`, and (when `workflow_flag`/`validation_submit_result` present) `kg_writer.upsert_workflow` + `link_step` and `kg_writer.upsert_form` + `link_has_form`. When a chosen action is submit-like and maps via `schema.VERB_ENTITY_MAP`, also call `kg_writer.upsert_business_entity` + the matching `link_creates/link_updates`. Pass a single `now` timestamp (ISO-8601 UTC) into every upsert so freshness is consistent within a step. The node must hold ZERO `MERGE/CREATE/SET/DELETE` Cypher after this — keep only the pure helpers (`parse_workflow_flag`, `extract_validation_rules`, `parse_index`) and the orchestration. Remove the now-unused `json`/Cypher-builder imports if they are no longer referenced.

    Wire `kg.schema.ensure_constraints` into the lifespan in `main.py`: import it alongside `init_neo4j`, and after `init_neo4j()` (which is lazy) add `await ensure_constraints(get_neo4j())` inside the lifespan startup — modeled on the `init_checkpointer()` line — relying on ensure_constraints' own graceful no-raise behavior so the api still boots when neo4j is down.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_single_write_path.py -x && uv run pytest -m "not live_llm and not e2e and not graph" -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_single_write_path.py` is GREEN (no write-Cypher outside kg/writer.py+kg/schema.py).
    - `explorer/nodes.py` contains no `MERGE`/`CREATE (`/`SET `/`DETACH DELETE` token and no `_build_persist_cypher`/`_write_workflow_step`/`_write_form_validation` definitions.
    - `persist_to_neo4j` calls `kg_writer.upsert_*` / `link_*` (grep `from app.services.kg` or `kg_writer.` present).
    - `main.py` lifespan calls `ensure_constraints` after `init_neo4j()`.
    - The default (non-graph, non-live) suite still passes (no regression in the explorer pure-logic tests).
  </acceptance_criteria>
  <done>The explorer is a thin delegate; the single-write-path grep test is green; constraints wired into lifespan without breaking graceful boot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| explorer BackgroundTask → kg_writer | page-derived text (titles, labels, locator values) crosses into Cypher parameters |
| api startup → Neo4j | constraint DDL at boot when neo4j may be down |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | kg/writer.py Cypher | mitigate | Parameterized Cypher ONLY; labels/edge-types are kg/schema.py code constants, never interpolated from page text (Cypher-injection mitigation, V5; carries T-04-14) |
| T-05-02 | Tampering / EoP | single write path (KG-05) | mitigate | Grep test fails on any write-Cypher outside kg/writer.py+kg/schema.py; explorer inline Cypher removed not dormant (Pitfall 6) |
| T-05-03 | Denial of Service | ensure_constraints at boot | accept | ensure_constraints catches connection errors and returns (graceful boot); api boots when neo4j down — no startup DoS |
| T-05-04 | Integrity | first_seen/last_verified freshness | mitigate | first_seen ONLY ON CREATE; constraint-backed MERGE guarantees ~0 duplicates; read-back guard fails a 0-count write (no silent no-op) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan (RESEARCH: zero new deps); slopcheck N/A |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_single_write_path.py -x` green after Task 2.
- Under graph_mode (`python infra/scripts/graph_mode.py up`): `uv run pytest -m graph tests/functional/test_kg_idempotency.py tests/functional/test_kg_schema.py -x` green; then `docker compose stop neo4j` before the full default stack (Pitfall 5).
- Default gate `uv run pytest -m "not live_llm and not e2e and not graph" -q` green (no explorer regression).
</verification>

<success_criteria>
- KG-05: a single writer service is the only Neo4j write path (grep-enforced); explorer refactored to delegate.
- KG-03: re-exploring an unchanged fixture set yields ~0 duplicates; first_seen/last_verified freshness present and correct; uniqueness constraint exists.
- KG-01 (write half): canonical Page/Button/Form/Workflow/Element/BusinessEntity nodes + NavigatesTo/Submits/Creates/Updates/Deletes/HAS_* edges written by the writer.
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-graph-flow-learning/05-01-SUMMARY.md` when done.
</output>
