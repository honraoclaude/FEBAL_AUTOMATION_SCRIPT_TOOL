---
phase: 05-knowledge-graph-flow-learning
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/api/app/services/kg/reader.py
  - apps/api/app/services/kg/risk.py
  - apps/api/app/services/kg/flows.py
  - apps/api/tests/unit/test_risk.py
  - apps/api/tests/unit/test_flow_mining.py
  - apps/api/tests/unit/test_flow_categorize.py
  - apps/api/tests/functional/test_element_repo.py
autonomous: true
requirements: [KG-04, KG-05]
must_haves:
  truths:
    - "Bounded path-mining over a fixture graph returns the expected journeys, deduped by node-set, honoring MAX_PATH_LENGTH and MAX_FLOWS"
    - "risk_score(signals) is a pure, deterministic, clamped 0-100 weighted sum with tunable frozen weights"
    - "Flow categorization names flows via the gateway (operation_type=flow.categorize) and falls back to deterministic names with no provider key"
    - "The Element Repository returns each element's locator chain and history queryable per element"
  artifacts:
    - path: "apps/api/app/services/kg/risk.py"
      provides: "Pure RiskWeights dataclass + risk_score(signals) -> int clamped 0-100"
      contains: "def risk_score"
    - path: "apps/api/app/services/kg/flows.py"
      provides: "Bounded deterministic path-mining + signal extraction + gateway categorization with deterministic fallback"
      contains: "MAX_PATH_LENGTH"
    - path: "apps/api/app/services/kg/reader.py"
      provides: "Read-only Cypher: element repository, pages, flows-source queries"
      contains: "execute_read"
  key_links:
    - from: "apps/api/app/services/kg/flows.py"
      to: "apps/api/app/services/llm_gateway.py"
      via: "categorize via llm_gateway.complete(operation_type='flow.categorize', run_id)"
      pattern: "flow.categorize"
    - from: "apps/api/app/services/kg/flows.py"
      to: "apps/api/app/services/kg/risk.py"
      via: "risk_score(signals) per mined flow"
      pattern: "risk_score"
---

<objective>
Build the read + derive layer over the Slice-1 graph: `kg/reader.py` (read-only Cypher incl. the Element Repository), `kg/risk.py` (a PURE deterministic 0-100 risk formula), and `kg/flows.py` (bounded deterministic path-mining + signal extraction + LLM categorization through the gateway with a deterministic no-key fallback). All deterministic logic is unit-tested WITHOUT provider keys.

Purpose: Derive risk-scored business flows (KG-04) from the graph and make element fingerprints + locator history queryable per element (KG-05 half) — the data the Slice-3 read API and browse UI render.
Output: `kg/reader.py`, `kg/risk.py`, `kg/flows.py`; unit tests for risk/mining/categorize-fallback (no keys); element-repository graph test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-knowledge-graph-flow-learning/05-CONTEXT.md
@.planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md
@.planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md
@.planning/phases/05-knowledge-graph-flow-learning/05-01-PLAN.md

<interfaces>
<!-- The gateway call pattern to copy (explorer/nodes.py:235-252): -->
<!-- async with SessionLocal() as db: result = await llm_gateway.complete(db, messages, -->
<!--     operation_type="flow.categorize", run_id=run_id, temperature=0, max_tokens=128) -->
<!-- except (llm_gateway.BudgetExceeded, llm_gateway.KillSwitchActive): <deterministic fallback> -->

<!-- Untrusted-observation fencing (explorer/nodes.py:49-55, 224-230): -->
<!-- system prompt declares the STEPS block as data-only; wrap as <<<UNTRUSTED_STEPS>>>...<<<END_UNTRUSTED_STEPS>>> -->

<!-- Element Repository read (RESEARCH lines 417-430) — MATCH (p:Page)-[:HAS_ELEMENT]->(e:Element) -->
<!-- RETURN e.key, e.role, e.label, e.chain_json, e.history_json, p.fingerprint, p.url, e.first_seen, e.last_verified -->

<!-- Pure-function analog (explorer/budget.py): @dataclass(frozen=True) + no-I/O table-testable funcs -->

<!-- read-back-free read pattern: get_neo4j().session() -> session.execute_read(_read) with LIMIT on every query -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kg/risk.py — pure deterministic risk formula</name>
  <files>apps/api/app/services/kg/risk.py, apps/api/tests/unit/test_risk.py</files>
  <behavior>
    - risk_score({has_destructive, state_change_edges, auth_gated_steps, form_count, path_length}) returns a clamped int 0-100.
    - A flow with a destructive action + several state-change edges scores High (>=67); a single login navigation scores Low (<34); thresholds 67/34 separate the three tiers.
    - Weights live in a frozen RiskWeights dataclass and are swappable via a parameter (default DEFAULT_WEIGHTS); the function is pure (no I/O, no LLM).
    - risk_tier(score) returns "high"/"medium"/"low" by the 67/34 thresholds.
  </behavior>
  <read_first>
    - apps/api/app/services/explorer/budget.py (the @dataclass(frozen=True) ExploreBudget + pure functions — the exact shape to mirror)
    - apps/api/app/services/explorer/risk.py (the "PURE CODE, NEVER LLM JUDGMENT" discipline, D-04)
    - .planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md (Pattern 3 lines 252-280 RiskWeights + risk_score + tiers; A1 weights are tunable starting points)
    - .planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md (risk.py assignment lines 88-93)
  </read_first>
  <action>
    Create `kg/risk.py` with a `@dataclass(frozen=True) RiskWeights` carrying the proposed tunable weights (destructive_action, per_state_change, auth_gated_step, per_form, depth per RESEARCH Pattern 3), a `DEFAULT_WEIGHTS` instance, and a pure `risk_score(signals: dict, w: RiskWeights = DEFAULT_WEIGHTS) -> int` computing the clamped weighted sum (binary destructive term + per-edge/step/form/length terms), clamped to 0-100. Add `risk_tier(score: int) -> str` returning "high" (>=67) / "medium" (34-66) / "low" (<34) — the UI badge tiers. NEVER call the LLM here (D-04). Keep it pure and import-light (stdlib dataclasses only).

    Create `tests/unit/test_risk.py` (default gate, no keys): a table test of representative signal dicts → expected scores and tiers, including the clamp at both ends (a maximal signal set clamps to 100; an empty set scores 0/low), and a test that passing custom weights changes the score (swappability).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_risk.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `kg/risk.py` defines `RiskWeights` (frozen), `DEFAULT_WEIGHTS`, `risk_score`, `risk_tier`.
    - `risk_score` is pure (no imports of neo4j/llm_gateway/SessionLocal) and clamps to 0-100.
    - `test_risk.py` covers: tier boundaries (33/34, 66/67), clamp at 0 and 100, and custom-weights swappability — all green with no keys/stack.
  </acceptance_criteria>
  <done>Pure deterministic risk formula + tiers exist and pass table tests with no provider key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: kg/flows.py — bounded path-mining + signal extraction + gateway categorization</name>
  <files>apps/api/app/services/kg/flows.py, apps/api/tests/unit/test_flow_mining.py, apps/api/tests/unit/test_flow_categorize.py</files>
  <behavior>
    - mine_flows over a fixture in-memory graph returns simple paths up to MAX_PATH_LENGTH, deduped by frozenset of node fingerprints, capped at MAX_FLOWS with a `bounded` flag set when the cap is hit, seeded from entry pages (no inbound NavigatesTo).
    - extract_signals(path) returns the dict risk_score consumes (has_destructive, state_change_edges, auth_gated_steps, form_count, path_length).
    - categorize_flow(steps_summary, run_id) names a flow via llm_gateway.complete(operation_type="flow.categorize"); with no key (BudgetExceeded/KillSwitchActive) it returns a deterministic fallback name "Flow: {start} → {end}" so flows + risk still render without keys.
    - The categorize prompt fences the steps as untrusted data; the LLM never receives instructions from page text.
  </behavior>
  <read_first>
    - apps/api/app/services/explorer/nodes.py lines 49-55, 212-259 (the decide gateway call + untrusted fence + BudgetExceeded/KillSwitchActive catch to copy)
    - apps/api/app/services/explorer/budget.py (pure traversal/ledger logic — the bounded-mining analog)
    - apps/api/app/db/session.py (SessionLocal — fresh session per gateway call)
    - apps/api/tests/unit/conftest.py (fake_gateway fixture; fake_chat_model — records operation_type/run_id, scripts complete() returns)
    - .planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md (Pattern 2 lines 227-250 bounding rules + MINE_PATHS; Pattern 4 lines 282-305 categorize + fallback; Pitfall 3 lines 357-361; A4 parameterized path-range caveat)
    - .planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md (flows.py assignment lines 96-114)
  </read_first>
  <action>
    Create `kg/flows.py`. Define module-level bounds `MAX_PATH_LENGTH` (start at 8) and `MAX_FLOWS` (start at 200). Implement `mine_flows(graph)` as a deterministic bounded simple-path enumeration: seed from entry pages (no inbound NavigatesTo or the login landing), extend along NavigatesTo/Submits edges up to MAX_PATH_LENGTH, reject any path with a repeated node (simple paths only — kills cycles), dedup emitted paths by the frozenset of their node fingerprints, cap at MAX_FLOWS and set a `bounded=True` flag on the result when the cap is hit. Accept an in-memory graph structure (adjacency over the fixture KG) so the deterministic test needs no neo4j; also provide a `mine_flows_from_neo4j(driver)` thin wrapper that runs the bounded read query and feeds the same pure miner — per A4, if neo4j 6.2 rejects a parameterized `*1..$maxlen` range, inline the validated integer code-constant into the query string (never user input). Implement `extract_signals(path)` returning the dict `risk_score` consumes (use `explorer.risk.is_destructive` / a destructive label check for has_destructive; count state-change edges; auth-gated steps; form count; path length).

    Implement `async def categorize_flow(steps_summary, run_id)` copying the explorer decide pattern: a data-only system prompt declaring `<<<UNTRUSTED_STEPS>>>...<<<END_UNTRUSTED_STEPS>>>`, a fresh `SessionLocal()` around `llm_gateway.complete(..., operation_type="flow.categorize", run_id=run_id, temperature=0, max_tokens=128)`, a deterministic parse of name/category from the response, and a `try/except (BudgetExceeded, KillSwitchActive)` that returns the deterministic fallback `"Flow: {start} → {end}"`. Compose `build_flows(graph_or_driver, run_id)` that mines → extracts signals → scores risk (via `kg.risk.risk_score`) → categorizes, returning flow records (id, name, category, risk_score, risk_tier, step_count, bounded, signals).

    Create `tests/unit/test_flow_mining.py` (no keys): mine over the fixture KG; assert expected journey count, dedup-by-node-set (two orderings collapse), MAX_PATH_LENGTH stops extension, MAX_FLOWS cap sets `bounded`. Create `tests/unit/test_flow_categorize.py` (no keys) using `fake_gateway`: assert the call uses `operation_type="flow.categorize"` and the run_id; and assert the no-key path (gateway raises BudgetExceeded) returns the deterministic fallback name.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_flow_mining.py tests/unit/test_flow_categorize.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `kg/flows.py` defines `MAX_PATH_LENGTH`, `MAX_FLOWS`, `mine_flows`, `extract_signals`, `categorize_flow`, `build_flows`.
    - Mining is bounded (simple paths only, dedup by node-set, MAX_FLOWS cap → `bounded` flag) and seeds from entry pages.
    - `categorize_flow` routes through `llm_gateway.complete` with `operation_type="flow.categorize"` and the run_id; on BudgetExceeded/KillSwitchActive it returns the deterministic `"Flow: {start} → {end}"` name.
    - `test_flow_mining.py` and `test_flow_categorize.py` pass with no provider key/stack; the categorize test asserts the operation_type and the fallback path.
  </acceptance_criteria>
  <done>Bounded deterministic mining + signal extraction + gateway categorization (with no-key fallback) exist; unit tests green with no keys.</done>
</task>

<task type="auto">
  <name>Task 3: kg/reader.py — read-only Cypher + Element Repository</name>
  <files>apps/api/app/services/kg/reader.py, apps/api/tests/functional/test_element_repo.py</files>
  <read_first>
    - apps/api/app/services/explorer/nodes.py (tx.run + result patterns; reader uses execute_read not execute_write)
    - apps/api/app/core/neo4j_driver.py (get_neo4j singleton)
    - apps/api/app/services/explorer/locators.py (the chain_json/history_json seam written on :Element)
    - .planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md (Element Repo query lines 417-430; Security: LIMIT on every read query, parameterized, no client Cypher lines 609-616)
    - .planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md (reader.py assignment lines 118-122)
  </read_first>
  <action>
    Create `kg/reader.py` with read-only query functions using `get_neo4j().session()` + `session.execute_read(_read)` (the read counterpart of the writer's execute_write — no read-back guard needed, but a `LIMIT` on EVERY query as a DoS guard). Implement: `list_pages()` (Page nodes with fingerprint/url/title/first_seen/last_verified + an element count), `page_detail(fingerprint)` (a page + its elements, forms, outbound NavigatesTo edges with the `via` label), `element_repository()` (the RESEARCH `ELEMENT_REPO` query verbatim — per-element chain_json/history_json + host page), `element_detail(key)`, and `graph_summary()` (counts by label for the /graph index). Deserialize `chain_json`/`history_json` into structured lists in the return values so the router/UI receive the locator chain + history directly. Parameterized Cypher only; labels/edge types come from `kg.schema` constants; never accept client-supplied Cypher.

    Create `tests/functional/test_element_repo.py` (`[functional, graph]`): seed a page + element (with a known chain + history) via the writer, then assert `element_repository()` returns that element's prioritized locator chain and its history, keyed per element.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest -m graph tests/functional/test_element_repo.py -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `kg/reader.py` exposes `list_pages`, `page_detail`, `element_repository`, `element_detail`, `graph_summary`, each using `execute_read` with a `LIMIT` and parameterized Cypher.
    - `element_repository()` returns each element's deserialized locator chain + history per element (KG-05 half).
    - `test_element_repo.py` passes under `-m graph`: the seeded element's chain + history are returned per element.
    - No write-Cypher token (`MERGE`/`CREATE (`/`SET `/`DELETE`) appears in `reader.py` (keeps the single-write-path grep green).
  </acceptance_criteria>
  <done>Read-only Cypher + Element Repository exist; element chain/history queryable per element; element-repo graph test green; single-write-path grep still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flow steps (page text) → categorize LLM | untrusted page-derived text reaches the LLM prompt |
| read query → Neo4j | read-only traversal could be unbounded on a large graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering / EoP | kg/flows.py categorize prompt | mitigate | Untrusted-observation fencing (`<<<UNTRUSTED_STEPS>>>`), data-only system prompt — same pattern as explorer decide; page text is data, never instructions |
| T-05-06 | DoS / financial | flow categorization spend | mitigate | Routed through the budgeted gateway (PLAT-06); temperature=0, max_tokens=128; deterministic fallback when no key — never an unmetered direct init_chat_model call |
| T-05-07 | DoS | unbounded path-mining / read query | mitigate | MAX_PATH_LENGTH, simple-paths-only, dedup-by-node-set, MAX_FLOWS cap (Pitfall 3); LIMIT on every read query (V5) |
| T-05-08 | Tampering | mining/read Cypher | mitigate | Parameterized Cypher; the path-range bound is a validated code constant, never user input (A4) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan; slopcheck N/A |
</threat_model>

<verification>
- Default gate (no keys/stack): `uv run pytest tests/unit/test_risk.py tests/unit/test_flow_mining.py tests/unit/test_flow_categorize.py -q` green.
- Under graph_mode: `uv run pytest -m graph tests/functional/test_element_repo.py -x` green; `docker compose stop neo4j` after (Pitfall 5).
- `uv run pytest tests/unit/test_single_write_path.py -x` still green (reader adds no write-Cypher).
</verification>

<success_criteria>
- KG-04: bounded deterministic path-mining derives journeys; a pure deterministic risk score is assigned per flow; the LLM only NAMES flows (deterministic fallback works without keys).
- KG-05 (element half): element fingerprints + locator history queryable per element via the Element Repository.
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-graph-flow-learning/05-02-SUMMARY.md` when done.
</output>
