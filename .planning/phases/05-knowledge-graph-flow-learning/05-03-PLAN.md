---
phase: 05-knowledge-graph-flow-learning
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - apps/api/app/schemas/kg.py
  - apps/api/app/routers/kg.py
  - apps/api/app/routers/stubs.py
  - apps/api/app/main.py
  - apps/api/tests/functional/test_kg_endpoints.py
  - apps/web/lib/api/kg.ts
  - apps/web/app/(dashboard)/graph/page.tsx
  - apps/web/app/(dashboard)/graph/flows/page.tsx
  - apps/web/app/(dashboard)/graph/elements/page.tsx
  - apps/web/components/graph/pages-table.tsx
  - apps/web/components/graph/flows-table.tsx
  - apps/web/components/graph/elements-table.tsx
  - apps/web/components/graph/risk-badge.tsx
  - apps/web/components/app-sidebar.tsx
  - apps/web/tests/e2e/kg-browse.spec.ts
autonomous: true
requirements: [KG-02]
must_haves:
  truths:
    - "GET /flows returns derived flows with risk scores; GET /coverage returns a coverage figure; GET /graph + /pages + /elements return read-only graph data"
    - "Every KG read endpoint is auth-gated (401 unauthenticated); the Phase-3 /flows + /coverage 501 stubs are removed"
    - "A user can browse Pages, Flows (with risk badges), and the Element Repository in the UI with drill-in links"
    - "A 'Knowledge graph' sidebar nav item routes to /graph"
  artifacts:
    - path: "apps/api/app/routers/kg.py"
      provides: "Real read-only auth-gated GET /flows /coverage /graph /pages /elements + drill-in endpoints"
      exports: ["router"]
    - path: "apps/web/app/(dashboard)/graph/page.tsx"
      provides: "Pages browse view + coverage header stat"
    - path: "apps/web/components/graph/risk-badge.tsx"
      provides: "Risk-tier badge (dot + score + word) mapping to --status-* tokens"
  key_links:
    - from: "apps/web/app/(dashboard)/graph/flows/page.tsx"
      to: "apps/api/app/routers/kg.py"
      via: "react-query fetch GET /api/flows through the same-origin rewrite"
      pattern: "/api/flows"
    - from: "apps/api/app/routers/kg.py"
      to: "apps/api/app/services/kg/flows.py"
      via: "build_flows / reader queries"
      pattern: "build_flows|reader\\."
    - from: "apps/web/components/app-sidebar.tsx"
      to: "/graph"
      via: "NAV_ITEMS append"
      pattern: "/graph"
---

<objective>
Make the Phase-3 `GET /flows` + `/coverage` 501 stubs REAL and add `GET /graph`/`/pages`/`/elements` (+ drill-in) read-only endpoints behind the existing `Depends(get_current_user)` gate, with Pydantic response schemas. Build the tabular browse UI under `app/(dashboard)/graph/` EXACTLY to 05-UI-SPEC — Pages / Flows (risk badges) / Element Repository views with drill-in links, honest coverage + empty states — reusing only already-vendored shadcn (zero new deps, no graph-viz lib). Append one sidebar nav item.

Purpose: KG-02 / D-05 / D-06 — let the user query and visually browse the knowledge graph.
Output: `schemas/kg.py`, `routers/kg.py`, refactored `stubs.py`, lifespan router wiring, endpoint tests; `lib/api/kg.ts` + the three browse pages + tables + risk badge + sidebar entry + the web e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-knowledge-graph-flow-learning/05-CONTEXT.md
@.planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md
@.planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md
@.planning/phases/05-knowledge-graph-flow-learning/05-UI-SPEC.md
@.planning/phases/05-knowledge-graph-flow-learning/05-02-PLAN.md
@apps/web/AGENTS.md

<interfaces>
<!-- Read-router shape to copy (routers/executions.py + the stubs router): -->
<!-- router = APIRouter(prefix="/api", tags=["kg"], dependencies=[Depends(get_current_user)]) -->
<!-- @router.get("/flows", response_model=FlowsResponse) -->

<!-- The seed response shapes to EXPAND (schemas/stub.py:59-83): -->
<!--   FlowSummary{flow_id,name,step_count}; FlowsResponse{flows}; -->
<!--   CoverageResponse{screens_total,screens_covered,flows_total,flows_covered,coverage_percent} -->

<!-- Web client + zod-at-boundary pattern (lib/api/explore.ts, lib/api/targets.ts): -->
<!--   import { z } from "zod"; import { api } from "./client"; -->
<!--   export async function listFlows(){ return z.array(flowSchema).parse(await api.get("/api/flows")); } -->

<!-- Table component to copy verbatim (components/targets/targets-table.tsx): -->
<!--   shadcn Table/TableHeader/TableBody/TableRow/TableHead/TableCell; 12px font-normal headers; -->
<!--   font-mono text-sm for urls/fingerprints/locators; LoadingRows() skeleton; EmptyState(); -->
<!--   StatusBadge/SandboxBadge: Badge variant="outline" + size-1.5 rounded-full bg-[var(--status-*)] -->

<!-- Sidebar append (components/app-sidebar.tsx:28-34): NAV_ITEMS is a flat {icon,label,href} list -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0 + API): schemas/kg.py + routers/kg.py + remove the /flows /coverage stubs + endpoint tests</name>
  <files>apps/api/app/schemas/kg.py, apps/api/app/routers/kg.py, apps/api/app/routers/stubs.py, apps/api/app/main.py, apps/api/tests/functional/test_kg_endpoints.py</files>
  <read_first>
    - apps/api/app/routers/stubs.py (the flows() + coverage() 501 handlers at lines 66-87 to REMOVE; leave heal/create-defect/dashboard untouched)
    - apps/api/app/schemas/stub.py (FlowSummary/FlowsResponse/CoverageResponse seed shapes lines 56-83 to expand)
    - apps/api/app/routers/executions.py (the read-router + router-level Depends(get_current_user) gate to copy)
    - apps/api/app/main.py lines 27, 81 (stubs_router import + include_router block — add kg_router)
    - apps/api/app/services/kg/flows.py + kg/reader.py (build_flows, list_pages, element_repository, graph_summary from plan 02)
    - .planning/phases/05-knowledge-graph-flow-learning/05-RESEARCH.md (Open Q3 lines 529-532 compute-on-demand; coverage shape lines 454-468; Security V4/V5 lines 600-616)
    - .planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md (kg.py router lines 126-141; stubs.py refactor lines 145-147; schemas/kg.py lines 151-155)
  </read_first>
  <action>
    Create `app/schemas/kg.py`: expand the stub shapes into the real response models — `FlowSchema`(flow_id, name, category|None, risk_score:int, risk_tier:str, step_count:int, bounded:bool, signals:dict), `FlowsResponse`(flows:list), `CoverageResponse`(screens_total, screens_covered, flows_total, flows_covered, coverage_percent:float, measured:bool), `PageSchema`(fingerprint, url, title, first_seen, last_verified, element_count), `PagesResponse`, `ElementSchema`(key, role, label, page_fingerprint, page_url, locator_chain:list, locator_history:list, first_seen, last_verified), `ElementsResponse`, `GraphSummaryResponse`(label counts), and page/flow/element detail responses. Field names MUST match the zod schemas in `lib/api/kg.ts` (Task 2).

    Create `app/routers/kg.py`: `router = APIRouter(prefix="/api", tags=["kg"], dependencies=[Depends(get_current_user)])` (router-level auth gate, V4). Implement read-only handlers computed ON-DEMAND (Open Q3): `GET /flows` (call `build_flows` over the live graph via reader; categorize uses the deterministic fallback when no key), `GET /coverage` (returns the slice-4 coverage figure; until slice-4 lands return `measured=false` honest shape — slice 4 wires the real metric in), `GET /graph` (graph_summary), `GET /pages` + `GET /pages/{fingerprint}` (list + detail), `GET /elements` + `GET /elements/{key}` (repository + detail), `GET /flows/{flow_id}` (flow detail with steps + risk breakdown). Default-sort flows by risk descending. Every handler is read-only (no writes — keeps the single-write-path grep green).

    Refactor `app/routers/stubs.py`: delete the `flows()` and `coverage()` 501 handlers (lines 66-87) and their now-unused imports; LEAVE `heal`/`create-defect`/`dashboard` as honest 501s. Wire `kg_router` into `main.py` lifespan/include block after `stubs_router`.

    Create `tests/functional/test_kg_endpoints.py`: assert each KG endpoint returns 401 when unauthenticated (V4) and, under graph_mode with an authed client + a seeded graph, returns the documented shape (flows carry risk_score+risk_tier; coverage carries the honest `measured` flag; elements carry locator chain+history). Mark `[functional, graph]` for the seeded-graph assertions; the 401 checks can run without a seeded graph.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/unit/test_single_write_path.py -x && uv run pytest -m graph tests/functional/test_kg_endpoints.py -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `schemas/kg.py` defines the flow/coverage/page/element/graph response models with `risk_score`+`risk_tier` on flows and `measured` on coverage.
    - `routers/kg.py` exposes GET /flows /coverage /graph /pages /pages/{fingerprint} /elements /elements/{key} /flows/{flow_id}, all behind a router-level `Depends(get_current_user)`, all read-only.
    - `stubs.py` no longer defines `flows()` or `coverage()`; `heal`/`create-defect`/`dashboard` remain 501.
    - `main.py` includes `kg_router`.
    - `test_kg_endpoints.py` asserts 401 unauth on every KG endpoint and the documented shapes under `-m graph`.
    - The single-write-path grep test stays green (router/schemas add no write-Cypher).
  </acceptance_criteria>
  <done>The /flows + /coverage stubs are real read-only auth-gated endpoints (plus /graph /pages /elements + detail); endpoint tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Web browse UI — lib/api/kg.ts, the three views, tables, risk badge, sidebar entry, e2e</name>
  <files>apps/web/lib/api/kg.ts, apps/web/app/(dashboard)/graph/page.tsx, apps/web/app/(dashboard)/graph/flows/page.tsx, apps/web/app/(dashboard)/graph/elements/page.tsx, apps/web/components/graph/pages-table.tsx, apps/web/components/graph/flows-table.tsx, apps/web/components/graph/elements-table.tsx, apps/web/components/graph/risk-badge.tsx, apps/web/components/app-sidebar.tsx, apps/web/tests/e2e/kg-browse.spec.ts</files>
  <read_first>
    - apps/web/AGENTS.md AND node_modules/next/dist/docs/ (this is NOT the Next.js you know — read the relevant guide before writing routes/pages)
    - apps/web/components/targets/targets-table.tsx (the canonical table: states, 12px headers, font-mono, StatusBadge/SandboxBadge dot pattern, EmptyState/LoadingRows)
    - apps/web/app/(dashboard)/targets/page.tsx (useQuery page shell; header block; drop mutations — read-only)
    - apps/web/lib/api/explore.ts + apps/web/lib/api/targets.ts (zod-at-boundary + api.get from ./client; same-origin /api rewrite + httpOnly cookie)
    - apps/web/components/app-sidebar.tsx (NAV_ITEMS flat list; active via pathname.startsWith)
    - .planning/phases/05-knowledge-graph-flow-learning/05-UI-SPEC.md (the FULL contract: page set, columns, risk-tier→--status-* mapping lines 95-106, copy table lines 114-142, states inventory lines 146-196, accessibility lines 188-196)
    - .planning/phases/05-knowledge-graph-flow-learning/05-PATTERNS.md (web assignments lines 159-196)
  </read_first>
  <action>
    Create `lib/api/kg.ts`: zod schemas mirroring the Pydantic models (flow, page, element, coverage, graph-summary, detail) + fetchers (`listFlows`, `getCoverage`, `listPages`, `pageDetail`, `listElements`, `elementDetail`, `flowDetail`) calling `api.get("/api/...")` through `./client` (same-origin rewrite, httpOnly cookie — no token handling). Field names match `schemas/kg.py`.

    Create `components/graph/risk-badge.tsx`: a `Badge variant="outline"` with a `size-1.5 rounded-full bg-[var(--status-*)]` dot + the mono numeric score + the tier WORD (never color alone — WCAG 1.4.1), mapping high→`--status-fail`, medium→`--status-quarantine`, low→`--status-pass`, unscored→`--status-neutral` with label "—" (per UI-SPEC risk mapping). A tooltip carries the breakdown summary.

    Create the three table components (`pages-table.tsx`, `flows-table.tsx`, `elements-table.tsx`) by copying the targets-table structure: shadcn Table; 12px `font-normal` headers; `font-mono text-sm` for url/fingerprint/locator; `LoadingRows()` skeleton; `EmptyState()` per the UI-SPEC empty copy (no-graph / no-flows / no-elements variants). Columns + copy EXACTLY per UI-SPEC Screen & State Inventory (Pages: Page·URL·Fingerprint·Elements·First seen·Last verified; Flows: Flow·Category·Risk·Steps·First seen, default sort risk-desc; Elements: Element·Role·Page·Locator(top priority)·Last verified). Relationships render as accent drill-in `<Link>`s (page→elements, navigates-to, flow→steps, element→detail). Stale rows muted with a Clock icon + tooltip. React default escaping only — no dangerouslySetInnerHTML.

    Create the three route pages under `app/(dashboard)/graph/` (read the Next guide first): `page.tsx` (Pages view + the coverage header stat card — render "Not yet measured" muted when `measured=false`, never "0%"), `flows/page.tsx`, `elements/page.tsx`. Each uses `useQuery` against the fetchers, renders the view-switcher segments ("Pages"·"Flows"·"Element repository" as accent-underlined route links), and surfaces error / graph-profile-down states inline with a Retry button per UI-SPEC copy. Drill-in detail panels (page/flow/element detail) per UI-SPEC — routed detail pages or sheets at executor discretion, content/sections per the spec (Flow detail MUST include the "Risk breakdown" auditable signals section).

    Append one entry to `NAV_ITEMS` in `components/app-sidebar.tsx`: `{ icon: Workflow, label: "Knowledge graph", href: "/graph" }` (import `Workflow` from lucide-react), positioned after "Explorations"; active via the existing `pathname.startsWith`.

    Create `apps/web/tests/e2e/kg-browse.spec.ts` (@playwright/test, mocked API — no backend/keys): mock `/api/flows`, `/api/coverage`, `/api/pages`, `/api/elements`; assert Pages/Flows(risk badge)/Element Repository tables render, the coverage stat shows the mocked % (and "Not yet measured" when `measured=false`), a drill-in link navigates, and the empty + error states render.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx eslint app/\(dashboard\)/graph components/graph lib/api/kg.ts components/app-sidebar.tsx && npx playwright test tests/e2e/kg-browse.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `lib/api/kg.ts` exports zod schemas + fetchers; every fetch hits `/api/*` via `./client` (no token handling).
    - `risk-badge.tsx` renders dot + mono score + tier word (never color alone) mapped to `--status-*`; unscored renders "—" not a fabricated number.
    - The three views render all UI-SPEC states (loading/empty-no-graph/populated/error/graph-down); Flows default-sorts risk descending; coverage shows "Not yet measured" when `measured=false`.
    - Drill-in relationships are real `<Link>`s; Flow detail includes the Risk breakdown signals section.
    - `app-sidebar.tsx` has exactly one new `{ icon, label: "Knowledge graph", href: "/graph" }` entry.
    - `npx tsc --noEmit` clean, eslint clean on touched files, `kg-browse.spec.ts` passes against the mocked API.
    - No new frontend dependency added (no `npm install`, no new shadcn add) — verify `package.json` unchanged.
  </acceptance_criteria>
  <done>The KG browse UI renders Pages/Flows(risk)/Element Repository with drill-in + honest coverage, exactly to UI-SPEC, zero new deps; e2e green; sidebar entry added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → KG read endpoints | unauthenticated client could reach graph data |
| Neo4j-derived text → React render | page-derived labels/urls rendered in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure / EoP | KG read endpoints | mitigate | Router-level `Depends(get_current_user)` (V4); test_kg_endpoints asserts 401 unauth on every endpoint (RBAC roles arrive Phase 10) |
| T-05-10 | Tampering (XSS) | browse UI rendering page-derived text | mitigate | React default escaping only; no dangerouslySetInnerHTML (T-01-24) |
| T-05-11 | Integrity | coverage display | mitigate | Coverage renders "Not yet measured" when `measured=false` — never a fabricated 0%/percentage (D-08 honesty) |
| T-05-12 | DoS | read-endpoint queries | mitigate | LIMIT on reader queries (from plan 02); no auto-polling on the UI (manual Retry) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new deps (D-05); package.json unchanged — slopcheck N/A |
</threat_model>

<verification>
- `uv run pytest -m graph tests/functional/test_kg_endpoints.py -x` green under graph_mode; 401-unauth checks green; `docker compose stop neo4j` after (Pitfall 5).
- `cd apps/web && npx tsc --noEmit && npx eslint <touched> && npx playwright test tests/e2e/kg-browse.spec.ts` green.
- `uv run pytest tests/unit/test_single_write_path.py -x` still green.
</verification>

<success_criteria>
- KG-02 / D-06: GET /flows + /coverage are real (stubs removed) + /graph /pages /elements read-only auth-gated endpoints.
- KG-02 / D-05: a user can browse Pages, Flows (risk badges), and the Element Repository with drill-in links, exactly to 05-UI-SPEC, with zero new deps and no graph-viz lib.
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-graph-flow-learning/05-03-SUMMARY.md` when done.
</output>
