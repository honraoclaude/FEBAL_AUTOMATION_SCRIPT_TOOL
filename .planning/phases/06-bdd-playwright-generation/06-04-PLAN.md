---
phase: 06-bdd-playwright-generation
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - apps/api/app/services/stability.py
  - apps/api/app/core/config.py
  - infra/targets/saucedemo/Dockerfile
  - infra/docker-compose.yml
  - .env.example
  - apps/api/tests/functional/test_stability.py
  - apps/api/tests/functional/test_seeded_bug.py
autonomous: true
requirements: [GEN-05]
must_haves:
  truths:
    - "A generated spec is accepted only if it passes N consecutive runs (N env-configurable, default 3); a flaky/non-green run rejects it"
    - "A dedicated seeded-bug SauceDemo build (one injected defect, distinct service/port, profile-gated) exists and is built"
    - "An accepted spec run against the seeded-bug build FAILS — proving real-breakage detection"
    - "The whole harness is proven deterministically with a planted template-rendered spec (passes N times vs SauceDemo, fails vs the bug build) with NO provider keys"
    - "The harness sequences for memory: codegen reads the Element Repository under graph_mode, then neo4j is stopped before the run phase"
  artifacts:
    - path: "apps/api/app/services/stability.py"
      provides: "N-run stability + seeded-bug acceptance harness (reuses the Phase-3 subprocess runner)"
      exports: ["run_stability", "run_seeded_bug"]
    - path: "infra/targets/saucedemo/Dockerfile"
      provides: "SEED_BUG build-arg toggling one injected DOM defect"
      contains: "SEED_BUG"
    - path: "infra/docker-compose.yml"
      provides: "saucedemo-bug service (profile-gated, distinct port)"
      contains: "saucedemo-bug"
  key_links:
    - from: "app/services/stability.py"
      to: "asyncio.create_subprocess_exec (uv run pytest, argv list, no shell)"
      via: "reuse the Phase-3 execution runner shape N times + once vs the bug build"
      pattern: "create_subprocess_exec"
    - from: "infra/docker-compose.yml saucedemo-bug"
      to: "infra/targets/saucedemo/Dockerfile SEED_BUG arg"
      via: "build args: {SEED_BUG: '1'} with a distinct host port + bugbuild profile"
      pattern: "SEED_BUG"
---

<objective>
Slice 4 of Phase 6: the stability + breakage-detection trust gate. Build the N-consecutive-run stability harness (reusing the Phase-3 subprocess runner), the dedicated seeded-bug SauceDemo build (a SEED_BUG build-arg on the existing Dockerfile that injects ONE deterministic DOM defect, plus a profile-gated saucedemo-bug compose service on a distinct port), and the breakage-detection acceptance: an accepted spec must pass N times vs standard SauceDemo AND fail vs the bug build. Prove the whole harness deterministically with a planted template-rendered spec (no keys), and sequence the harness for the 3GB memory cap.

Purpose: GEN-05 (stability half / D-07/D-08) — generated tests pass an N-run stability check before acceptance, and a seeded-bug build makes them fail (they detect real breakage). The mechanics are provable WITHOUT keys via the planted spec.

Output: services/stability.py, the STABILITY_RUNS + SEEDED_BUG_BASE_URL settings + compose env, the upgraded saucedemo Dockerfile + the saucedemo-bug service, and the planted-spec functional stability + seeded-bug tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bdd-playwright-generation/06-CONTEXT.md
@.planning/phases/06-bdd-playwright-generation/06-RESEARCH.md
@.planning/phases/06-bdd-playwright-generation/06-PATTERNS.md
@.planning/phases/06-bdd-playwright-generation/06-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. From Phase 3 (the runner) + Slice 3 (codegen) + the infra. -->

From app/services/execution.py (Phase 3 — reuse the subprocess shape VERBATIM): async run_execution(run_id, spec_path) -> None runs `asyncio.create_subprocess_exec("uv","run","pytest",spec_path,"-q", stdout=PIPE, stderr=STDOUT, cwd=_run_cwd())` (argv LIST, no shell — T-03-15), status="passed" if returncode==0 else "failed", _OUTPUT_TAIL_CHARS cap, _run_cwd() resolver. NEVER in-process pytest (Pitfall 3).
From app/services/codegen/project.py (Slice 3): async generate_project(db, run_id, *, driver=None) -> project_root path (the tree the harness runs).
From app/core/workspaces.py: run_dir(run_id) (where the spec lives).
From app/templates/test_login.py.j2 (Phase 3, RETAINED): the planted-spec template — render with FIXED slots (in-cluster http://saucedemo:80, standard_user/secret_sauce, the observed selectors INCLUDING the post-login `.inventory_list` assertion) for the deterministic proof. Author the planted spec to assert `.inventory_list` so it passes vs std SauceDemo and fails vs the bug build that renames it.
From app/core/config.py: the Settings singleton; add STABILITY_RUNS (default 3) and SEEDED_BUG_BASE_URL fields (Phase-2 explicit-enumeration: compose enumerates env; add to .env.example too).

From infra/targets/saucedemo/Dockerfile: the existing pinned-SHA multi-stage build serving /usr/share/nginx/html via nginx:alpine; ARG SAUCEDEMO_SHA. Add `ARG SEED_BUG=0` and a final layer that, when SEED_BUG=1, applies ONE deterministic sed mutation to the served files.
From infra/docker-compose.yml: the saucedemo service (build ./targets/saucedemo, mem_limit 128m, wget healthcheck on 127.0.0.1:80, ports 8080:80); the neo4j service under profiles:[graph]; the graph_mode helper.

Seeded defect (RESEARCH Open-Q1, decide id+spec together): rename the post-login success element `.inventory_list` so the accepted spec's success assertion fails. The planted spec asserts `.inventory_list` — passes vs std, fails vs bug.
</interfaces>

<source_patterns>
@apps/api/app/services/execution.py
@apps/api/app/templates/test_login.py.j2
@apps/api/tests/functional/test_execute.py
@infra/targets/saucedemo/Dockerfile
@infra/docker-compose.yml
</source_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: seeded-bug SauceDemo build (SEED_BUG build-arg) + saucedemo-bug compose service + settings/env</name>
  <files>infra/targets/saucedemo/Dockerfile, infra/docker-compose.yml, .env.example, apps/api/app/core/config.py</files>
  <read_first>
    - infra/targets/saucedemo/Dockerfile (the existing pinned-SHA multi-stage nginx build; the COPY --from=build to /usr/share/nginx/html)
    - infra/docker-compose.yml:127-173 (the saucedemo service block + the neo4j profiles:[graph] gating shape to mirror for profiles:[bugbuild])
    - apps/api/app/core/config.py (the Settings field style + how compose enumerates env explicitly — Phase-2 pattern)
    - .env.example (the env contract to extend)
    - RESEARCH §Novel Mechanism 4 (the SEED_BUG build-arg, the saucedemo-bug service distinct port + bugbuild profile, SEEDED_BUG_BASE_URL; Open-Q1 the exact mutation)
  </read_first>
  <action>
    Upgrade infra/targets/saucedemo/Dockerfile: add `ARG SEED_BUG=0` after the SAUCEDEMO_SHA arg; in the final nginx stage, after the COPY, add a conditional layer that when SEED_BUG=1 applies ONE deterministic sed substitution to the served bundle renaming the post-login success element class `.inventory_list` (e.g. to `.inventory_list_BROKEN`) — a single reproducible defect on the same nginx serve, keeping the pinned-SHA build unchanged for the default (SEED_BUG=0) build. Document the mutation in a comment (paired with the planted spec in Task 2).
    Add a saucedemo-bug service to infra/docker-compose.yml mirroring the saucedemo block: `build: { context: ./targets/saucedemo, args: { SEED_BUG: "1" } }`, mem_limit 128m, the SAME wget healthcheck shape (127.0.0.1:80), a DISTINCT host port (8081:80), and `profiles: [bugbuild]` so it is OFF by default (mirrors neo4j's profiles:[graph]). Add STABILITY_RUNS (default 3) + SEEDED_BUG_BASE_URL (e.g. http://saucedemo-bug:80 in-cluster) to the api service environment block.
    Add `stability_runs: int = 3` and `seeded_bug_base_url: str` (or Optional) Settings fields in config.py; add STABILITY_RUNS + SEEDED_BUG_BASE_URL to .env.example (Phase-2 explicit-enumeration; never literal secrets).
    Carry invariants: the seeded-bug build is COMPOSE INFRA (a build-arg), NOT a package — zero new packages; the default saucedemo build is unchanged; bugbuild is profile-gated OFF by default (OOM safety).
  </action>
  <verify>
    <automated>cd infra && docker compose --profile bugbuild build saucedemo-bug && docker compose config | grep -A2 "saucedemo-bug" ; cd ../apps/api && uv run python -c "from app.core.config import settings; print('STABILITY_RUNS', settings.stability_runs)"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose --profile bugbuild build saucedemo-bug` succeeds; the image applies the .inventory_list rename only when SEED_BUG=1 (the default saucedemo build is unchanged).
    - `docker compose config` shows saucedemo-bug with args SEED_BUG="1", a distinct port (8081:80), mem_limit 128m, and profiles:[bugbuild] (off by default).
    - settings.stability_runs defaults to 3; SEEDED_BUG_BASE_URL is wired into the api env and .env.example.
    - The saucedemo (default) service definition is unchanged.
  </acceptance_criteria>
  <done>The SEED_BUG build-arg injects one deterministic DOM defect; the profile-gated saucedemo-bug service builds on a distinct port; STABILITY_RUNS + SEEDED_BUG_BASE_URL are settings/env-wired — zero new packages.</done>
</task>

<task type="auto">
  <name>Task 2: N-run stability + seeded-bug harness + planted-spec deterministic proof</name>
  <files>apps/api/app/services/stability.py, apps/api/tests/functional/test_stability.py, apps/api/tests/functional/test_seeded_bug.py</files>
  <read_first>
    - apps/api/app/services/execution.py:49-92 (the subprocess runner shape to reuse VERBATIM: create_subprocess_exec argv list, no shell, _run_cwd, output cap)
    - apps/api/tests/functional/test_execute.py (the Phase-3 deterministic proof: render test_login.py.j2 with FIXED slots, run via subprocess; the graph-marked functional pattern)
    - apps/api/app/templates/test_login.py.j2 (the planted-spec template — assert .inventory_list so it passes vs std and fails vs the bug build)
    - apps/api/app/core/config.py (Task 1: settings.stability_runs, settings.seeded_bug_base_url)
    - apps/api/app/services/codegen/project.py (Slice 3: generate_project — the spec the live harness runs)
    - RESEARCH §Novel Mechanism 4 (N-run accept iff all green; seeded-bug run must FAIL; the planted-spec proof; the OOM sequencing — codegen under graph_mode then STOP neo4j before the run phase)
  </read_first>
  <action>
    Create services/stability.py with `async def run_stability(spec_path, *, runs=None) -> dict`: call the execution.run_execution subprocess shape (asyncio.create_subprocess_exec "uv","run","pytest",spec_path,"-q", argv list, no shell, output-capped, _run_cwd) N times (runs or settings.stability_runs, default 3); accept iff ALL N return exit 0; any non-green (incl. flake) → reject. Record per-run status for the trace. Each run is a fresh subprocess + fresh browser context (pytest-playwright). Add `async def run_seeded_bug(spec_path) -> dict`: run the SAME spec once with SEEDED_BUG_BASE_URL overriding the base URL (pass it via the spec's base-url env var the conftest reads), assert the run FAILS (returncode != 0). Expose `async def accept_spec(spec_path) -> dict` that returns accepted=True iff run_stability all-green AND run_seeded_bug failed (the breakage-detection acceptance). NEVER in-process pytest (Pitfall 3); argv list, no shell, spec_path run_id-derived (T-03-15).
    Document the OOM sequencing in the module docstring and enforce it in the test: (1) codegen (Slice 3) reads the Element Repository under graph_mode (neo4j up, web stopped) and WRITES the spec; (2) STOP neo4j, then run run_stability vs saucedemo and run_seeded_bug vs saucedemo-bug (the spec is already written; running it needs no graph). The planted-spec proof needs no neo4j at all.
    Create tests/functional/test_stability.py (marked graph, planted): render the REAL test_login.py.j2 with FIXED slots (in-cluster http://saucedemo:80, standard_user/secret_sauce, asserting .inventory_list) — a planted spec, no gateway, no keys — then run_stability and assert accepted (N green vs saucedemo). Also assert a deliberately-failing planted spec is rejected (not all green).
    Create tests/functional/test_seeded_bug.py (marked graph/bugbuild, planted): build/up saucedemo-bug (profile bugbuild), run the SAME planted spec via run_seeded_bug against SEEDED_BUG_BASE_URL, assert it FAILS (the renamed .inventory_list breaks the success assertion); and assert accept_spec returns accepted=True only when N-green-vs-std AND red-vs-bug both hold. No keys — the planted spec proves the whole mechanic.
    Carry invariants: reuse the Phase-3 subprocess runner verbatim (argv list, no shell, never in-process); spec_path run_id-derived; OOM sequencing (codegen under graph_mode, neo4j stopped before the run phase); the planted-spec deterministic proof needs no keys; live generate→stabilize is Manual-Only.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_stability.py -m graph -q ; uv run pytest tests/functional/test_seeded_bug.py -m graph -q</automated>
  </verify>
  <acceptance_criteria>
    - test_stability.py (planted, graph): an all-green planted spec is accepted over N runs; a failing planted spec is rejected. Subprocess discipline (grep): create_subprocess_exec argv list, no shell, no pytest.main.
    - test_seeded_bug.py (planted, graph/bugbuild): the SAME planted spec FAILS against saucedemo-bug; accept_spec returns accepted=True only when N-green-vs-std AND red-vs-bug.
    - run_stability accepts iff ALL N runs exit 0 (N from settings.stability_runs, overridable).
    - The harness never runs the spec in-process; the OOM sequencing is documented and the test stops neo4j before the run phase.
    - No provider key used anywhere in these tests (planted spec, no gateway).
  </acceptance_criteria>
  <done>The N-run stability + seeded-bug acceptance harness reuses the Phase-3 subprocess runner, accepts only N-green-then-red, and is proven deterministically with a planted spec (passes vs SauceDemo, fails vs the bug build) with no keys and the OOM sequencing enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_id-derived spec_path → subprocess | the spec path is run_id-derived, never client input |
| SEEDED_BUG_BASE_URL → spec env | the base-url override for the bug run is operator/settings-controlled |
| generated spec → subprocess execution | sync-Playwright spec runs only in an isolated subprocess |
| seeded-bug build → nginx serve | one deterministic DOM mutation, profile-gated off by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Tampering | command injection via spec_path in the runner | mitigate | run_id-derived spec_path, argv list, no shell=True (Phase-3 T-03-15 reused verbatim) |
| T-06-19 | Tampering | in-process pytest deadlock/leak | mitigate | sync-Playwright spec runs ONLY via asyncio.create_subprocess_exec — never in-process (Pitfall 3 / T-03-16) |
| T-06-20 | DoS | OOM: neo4j + both targets + Chromium > 3GB | mitigate | sequence — codegen reads the Element Repository under graph_mode, STOP neo4j before the run phase; saucedemo + saucedemo-bug + Chromium fit without neo4j; bugbuild profile off by default (Pitfall 4) |
| T-06-21 | Tampering | seeded-bug build drifting the default target | mitigate | SEED_BUG defaults 0 (default saucedemo build unchanged); the mutation applies only under args SEED_BUG=1 on a distinct service/port |
| T-06-22 | Info Disclosure | target creds in the planted/generated spec | mitigate | SauceDemo public demo creds only; no decrypted/ciphertext creds (PLAT-07) |
| T-06-23 | Tampering | output bloat from a noisy spec | mitigate | output capped (_OUTPUT_TAIL_CHARS) per the Phase-3 runner |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages; the seeded-bug build is a Dockerfile build-arg (infra), not a package; any new package requires a blocking-human checkpoint |
</threat_model>

<verification>
- `cd infra && docker compose --profile bugbuild build saucedemo-bug` → succeeds.
- `cd apps/api && uv run pytest tests/functional/test_stability.py tests/functional/test_seeded_bug.py -m graph -q` (under graph_mode for codegen, then neo4j stopped for the run phase; bugbuild profile up) → green.
- Subprocess grep: `stability.py` contains create_subprocess_exec with an argv list and NO pytest.main/shell=True.
- `cd apps/api && uv run pytest -m "not live_llm and not e2e and not graph" -q` → green (no regressions).
- `docker stats` during the seeded-bug harness stays under the 3GB cap (Manual-Only memory check per 06-VALIDATION).
</verification>

<success_criteria>
- GEN-05 (stability half): generated tests pass an N-consecutive-run stability check before acceptance (N env-configurable, default 3), and a seeded-bug build of the target makes them fail (real-breakage detection) — proven deterministically with a planted spec, no keys.
- The harness sequences for the 3GB memory cap (codegen under graph_mode, neo4j stopped before the run phase).
- The seeded-bug build is compose infra (a build-arg), profile-gated off by default; zero new packages.
</success_criteria>

<output>
Create `.planning/phases/06-bdd-playwright-generation/06-04-SUMMARY.md` when done.
</output>
