---
phase: 06-bdd-playwright-generation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/api/app/schemas/scenario.py
  - apps/api/app/routers/scenarios.py
  - apps/api/app/routers/generate.py
  - apps/api/app/main.py
  - apps/api/tests/functional/test_scenarios_router.py
  - apps/web/lib/api/scenarios.ts
  - apps/web/app/(dashboard)/scenarios/page.tsx
  - apps/web/app/(dashboard)/scenarios/[id]/page.tsx
  - apps/web/components/scenarios/gate-indicators.tsx
  - apps/web/components/scenarios/gherkin-editor.tsx
  - apps/web/components/app-sidebar.tsx
  - apps/web/tests/e2e/scenarios.spec.ts
autonomous: true
requirements: [GEN-02]
must_haves:
  truths:
    - "The reviewer sees generated scenarios as a queue of drafts with status + source-flow risk, filterable by draft/approved/rejected/all"
    - "Editing a scenario's Gherkin and saving RE-RUNS both gates server-side; a failing edit returns 422, keeps the text, stays draft, leaves Approve disabled"
    - "Approving re-runs both gates and sets status=approved only when they pass; codegen reads approved only"
    - "Rejecting sets status=rejected"
    - "Every review endpoint is auth-gated (401 unauthenticated)"
    - "Per-Then gate indicators render strictly from the server's then_refs resolution — green only when the server confirmed it"
  artifacts:
    - path: "apps/api/app/routers/scenarios.py"
      provides: "auth-gated review router (list/get/edit/approve/reject) re-running both gates on edit/approve"
      exports: ["router"]
    - path: "apps/api/app/schemas/scenario.py"
      provides: "Pydantic request/response models mirroring the scenarios row + then_refs"
      contains: "class ScenarioDetail"
    - path: "apps/web/app/(dashboard)/scenarios/page.tsx"
      provides: "review queue list (status badge, source-flow risk, filter)"
    - path: "apps/web/app/(dashboard)/scenarios/[id]/page.tsx"
      provides: "scenario detail/review (editable Gherkin + honest per-Then indicators + approve/reject/save)"
  key_links:
    - from: "app/routers/scenarios.py POST /scenarios/{id}/edit"
      to: "gates.gherkin_lint.validate_gherkin + gates.assertion_gate.assert_non_vacuous"
      via: "re-run both gates on save; 422 + no save on failure"
      pattern: "assert_non_vacuous"
    - from: "apps/web/app/(dashboard)/scenarios/[id]/page.tsx"
      to: "/api/scenarios/{id}/edit|approve|reject"
      via: "api.post wrapper with query invalidation (no optimistic updates)"
      pattern: "api.post"
---

<objective>
Slice 2 of Phase 6: the approve/edit review queue. Build the auth-gated review router (list drafts / get one / edit-save with both gates re-run / approve with both gates re-run / reject), the Pydantic + zod contract, and the review-queue UI built EXACTLY to 06-UI-SPEC — a list view with status + source-flow risk + filter, and a detail/review view with an editable Gherkin area, honest per-Then no-vacuous indicators, and the edit→re-validate→approve flow. Only approved scenarios feed codegen.

Purpose: GEN-02 (D-01/D-02) — the review step between generation (Slice 1) and codegen (Slice 3). Edit-in-place must re-run the gates so an edited scenario cannot bypass quality (D-04).

Output: routers/scenarios.py + schemas/scenario.py (wired in main.py), the generate-scenarios entrypoint on the upgraded routers/generate.py, lib/api/scenarios.ts, the two scenarios pages + the gate-indicator and gherkin-editor compositions, one sidebar nav item, the functional router test, and the mocked-API web e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bdd-playwright-generation/06-CONTEXT.md
@.planning/phases/06-bdd-playwright-generation/06-RESEARCH.md
@.planning/phases/06-bdd-playwright-generation/06-PATTERNS.md
@.planning/phases/06-bdd-playwright-generation/06-UI-SPEC.md
@.planning/phases/06-bdd-playwright-generation/06-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. From Slice 1 (06-01) + the Phase-5 review-UI analogs. -->

From app/services/scenario_service.py (Slice 1): create_scenario, get(db, id), list_scenarios(db, *, status=None), set_status(db, id, status) [validated], update_gherkin(db, id, gherkin_text, then_refs) [edited=True, stays draft], list_approved(db, run_id).
From app/services/gates/gherkin_lint.py: validate_gherkin(text) -> None (raises GenerationError on malformed).
From app/services/gates/assertion_gate.py: async resolve_then_refs(then_refs, *, driver=None) -> list[str] (unresolved Then texts); async assert_non_vacuous(then_refs, *, driver=None) -> None (raises GenerationError). The router derives per-Then {then_text, resolved, kg_ref|reason} from resolve_then_refs so the UI shows honest green/red.
From app/services/generation.py (Slice 1): async generate_scenarios(db, run_id) -> persists draft rows; GenerationError.

From app/routers/kg.py:49-54 + routers/executions.py:20-24 (auth gate): APIRouter(prefix="/api", tags=[...], dependencies=[Depends(get_current_user)]).
From app/routers/generate.py:29-45: the _require_run 404 guard + GenerationError -> HTTPException(422) translation to mirror.
From app/schemas/kg.py: Pydantic v2 BaseModel + Field, `from __future__ import annotations`; the schemas/kg.py ↔ lib/api/kg.ts field-name alignment discipline.

From apps/web/app/(dashboard)/graph/flows/page.tsx: "use client", useQuery({queryKey, queryFn, retry:false}), loading/empty/error states, table.
From apps/web/app/(dashboard)/graph/flows/[id]/page.tsx: detail-fetch shape.
From apps/web/lib/api/kg.ts: zod-at-boundary schemas + api.get/api.post fetchers over the same-origin /api client; encodeURIComponent on path params.
From apps/web/lib/api/client.ts:86-91: api.post wrapper (mutations) — react-query invalidation on success, NO optimistic updates.
From apps/web/components/graph/risk-badge.tsx: the risk badge (dot + mono score + tier WORD) to REUSE for the source-flow risk column.
From apps/web/components/app-sidebar.tsx:28-37: NAV_ITEMS flat list {icon,label,href}; active via pathname.startsWith(item.href).
</interfaces>

<source_patterns>
@apps/api/app/routers/kg.py
@apps/api/app/routers/generate.py
@apps/api/app/schemas/kg.py
@apps/web/app/(dashboard)/graph/flows/page.tsx
@apps/web/app/(dashboard)/graph/flows/[id]/page.tsx
@apps/web/lib/api/kg.ts
@apps/web/components/graph/risk-badge.tsx
@apps/web/components/app-sidebar.tsx
@apps/web/tests/e2e/kg-browse.spec.ts
</source_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: review router + schemas + generate-scenarios entrypoint (auth-gated, gates re-run on edit/approve)</name>
  <files>apps/api/app/schemas/scenario.py, apps/api/app/routers/scenarios.py, apps/api/app/routers/generate.py, apps/api/app/main.py, apps/api/tests/functional/test_scenarios_router.py</files>
  <read_first>
    - apps/api/app/routers/kg.py:1-60 (auth-gated read router shape; list + get-by-id handlers)
    - apps/api/app/routers/generate.py (the _require_run 404 guard + GenerationError->422 translation + existing generate-bdd/generate-scripts entrypoints to extend)
    - apps/api/app/schemas/kg.py (Pydantic v2 response-model style + the field-name alignment note)
    - apps/api/app/services/scenario_service.py + gates/* + generation.generate_scenarios (Slice 1 — the service surface this router wires)
    - RESEARCH §Review Queue Model (the endpoint list, edit/approve re-run rule, codegen-reads-approved-only)
  </read_first>
  <action>
    Create schemas/scenario.py (Pydantic v2, `from __future__ import annotations`): ThenRefResult{then_text, resolved: bool, kg_ref: str | None, reason: str | None}; ScenarioSummary{id, run_id, flow_id, feature_name, status, edited, stale, flow_risk_score: int | None, flow_risk_tier: str | None, updated_at}; ScenarioDetail{...summary fields..., gherkin_text, then_results: list[ThenRefResult]}; EditRequest{gherkin_text, then_refs}; GenerateScenariosRequest{run_id}; GenerateScriptsRequest{run_id} (Slice-3 consumer). Keep field names aligned with lib/api/scenarios.ts zod.
    Create routers/scenarios.py: APIRouter(prefix="/api", tags=["scenarios"], dependencies=[Depends(get_current_user)]). Handlers: GET /scenarios?status=draft (list; default drafts; attach each row's source-flow risk via kg/flows build_flows lookup by flow_id, honest None when unscored); GET /scenarios/{id} (one scenario + compute per-Then results: run resolve_then_refs and map each then_ref to ThenRefResult resolved/reason — honest, server-authoritative); POST /scenarios/{id}/edit (body EditRequest → re-run BOTH gates: validate_gherkin THEN assert_non_vacuous; on GenerationError → HTTPException(422) and NO save; on success → scenario_service.update_gherkin sets edited=True, stays draft, returns the fresh ScenarioDetail); POST /scenarios/{id}/approve (re-run BOTH gates defense-in-depth → on pass set_status approved, else 422); POST /scenarios/{id}/reject (set_status rejected). Translate GenerationError → 422 exactly like generate.py. Use the request db session; the gate's driver defaults to the lifespan singleton.
    Upgrade routers/generate.py: ADD POST /generate-scenarios (body GenerateScenariosRequest → generation.generate_scenarios; _require_run 404 guard; GenerationError→422) alongside the existing entrypoints; reserve the generate-scripts codegen wiring for Slice 3 (this slice adds the scenarios entrypoint + request schemas only). Wire scenarios_router into main.py (before stubs_router, like kg_router).
    Create tests/functional/test_scenarios_router.py: 401-unauth on every scenarios endpoint (no graph); the status lifecycle (seed a draft row via scenario_service in a fixture; edit with valid gherkin+resolvable refs → 200 + edited=true + still draft; edit with malformed gherkin → 422 + unchanged; edit introducing a vacuous Then → 422 + unchanged; approve → status=approved; reject another → rejected); and a list_approved assertion that codegen sees only approved. Use the Slice-1 fake-driver injection for the non-graph lifecycle resolution, plus a small graph-marked check for live resolution.
    Carry invariants: auth-gated router; edit AND approve both re-run lint + no-vacuous (gate integrity, D-04); only approved feed codegen; read-only Cypher in the gate; request session per convention.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/functional/test_scenarios_router.py -m "not graph" -q ; uv run pytest tests/unit/test_single_write_path.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All scenarios endpoints return 401 unauthenticated (parametrized, no graph).
    - Edit with valid gherkin + resolvable refs → 200, edited=true, status stays draft; edit with malformed gherkin OR a vacuous Then → 422 and the row is unchanged (assert gherkin_text + status before/after).
    - Approve re-runs both gates and sets status=approved only on pass; reject sets status=rejected.
    - GET /scenarios/{id} returns per-Then then_results with resolved reflecting the gate (no fabricated green).
    - scenarios_router appears in /openapi.json; list_approved returns only approved rows.
    - single-write-path gate green (router adds no write-Cypher).
  </acceptance_criteria>
  <done>The auth-gated review router lists/gets/edits/approves/rejects with both gates re-run on edit and approve, returns honest per-Then results, exposes generate-scenarios, and only-approved-feed-codegen is enforced; functional tests green.</done>
</task>

<task type="auto">
  <name>Task 2: review-queue UI (list + detail/edit) + zod client + sidebar nav + e2e</name>
  <files>apps/web/lib/api/scenarios.ts, apps/web/app/(dashboard)/scenarios/page.tsx, apps/web/app/(dashboard)/scenarios/[id]/page.tsx, apps/web/components/scenarios/gate-indicators.tsx, apps/web/components/scenarios/gherkin-editor.tsx, apps/web/components/app-sidebar.tsx, apps/web/tests/e2e/scenarios.spec.ts</files>
  <read_first>
    - .planning/phases/06-bdd-playwright-generation/06-UI-SPEC.md (the FULL contract: list columns + filter, detail layout, editable Gherkin area as a styled-native textarea, per-Then indicators, edit→re-validate→approve flow, every empty/loading/error state, copywriting, the sidebar item)
    - apps/web/app/(dashboard)/graph/flows/page.tsx (list pattern: "use client", useQuery retry:false, loading/empty/error, table)
    - apps/web/app/(dashboard)/graph/flows/[id]/page.tsx (detail-fetch shape)
    - apps/web/lib/api/kg.ts (zod-at-boundary + api.get/api.post + encodeURIComponent)
    - apps/web/lib/api/client.ts:86-91 (api.post wrapper; mutation + query invalidation, no optimistic updates)
    - apps/web/components/graph/risk-badge.tsx (reuse for the source-flow risk column)
    - apps/web/components/app-sidebar.tsx:21-37 (NAV_ITEMS; the file already anticipates a "Scenarios" item)
    - apps/web/tests/e2e/kg-browse.spec.ts (mocked-API e2e pattern, no backend/keys)
  </read_first>
  <action>
    Create lib/api/scenarios.ts: zod schemas mirroring schemas/scenario.py (ScenarioSummary, ScenarioDetail with then_results, the mutation request shapes); fetchers listScenarios(status), scenarioDetail(id), editScenario(id, body), approveScenario(id), rejectScenario(id) via api.get/api.post over the same-origin /api client. Validate at the boundary (Pydantic is authority).
    Create app/(dashboard)/scenarios/page.tsx (list, EXACTLY to 06-UI-SPEC §1): "use client", useQuery({queryKey:["scenarios", status], queryFn, retry:false}). Table columns Scenario (accent drill-in link → /scenarios/[id]) · Source flow (name/category Body + mono flow-id caption) · Risk (REUSE risk-badge) · Status (status badge Draft amber / Approved green / Rejected muted via the existing --status-* tokens; "Edited" muted caption when edited=true) · Updated (mono timestamp). Default sort risk-desc then updated-desc. Filter segments "Drafts · Approved · Rejected · All" mapped to ?status= (deep-linkable; default Drafts) built as accent-underlined segments over tokens (NOT a new tabs block). All states: loading skeleton rows, the two empty states (no scenarios at all → "Go to knowledge graph"; filter-no-match → "View drafts"), populated, rejected-rows muted, inline error+Retry. NO new shadcn add, NO new dependency.
    Create components/scenarios/gherkin-editor.tsx: a styled-native <textarea> (NOT a vendored shadcn block — the textarea block is deliberately not added) reusing the input.tsx token classes (bg/border/focus-ring), monospace, fixed min-h, resizable, with an associated <label> "Gherkin". "Save edits" enabled only when text differs; "Cancel" reverts to saved.
    Create components/scenarios/gate-indicators.tsx: render per-Then results STRICTLY from server then_results — green CheckCircle2 "Resolved" + mono kg_ref when resolved, red XCircle "Vacuous" + mono reason when not, muted "Pending re-check" on an unsaved edit; the summary line; offending Thens highlighted (left red border + muted-red tint). Never fabricate green. Each carries the WORD + icon (WCAG 1.4.1).
    Create app/(dashboard)/scenarios/[id]/page.tsx (detail/review, EXACTLY to 06-UI-SPEC §2): breadcrumb, title + status badge + Edited caption + timestamps; the Gherkin editor section; the "Assertion checks" gate-indicators section; the "Source flow" section (risk badge + accent link "View in knowledge graph" → /graph/flows/[id]); the action bar — "Approve scenario" (accent, DISABLED unless gherkin parses AND every Then resolves AND no unsaved edit; aria-describedby helper caption), "Reject scenario" (destructive → confirm dialog), Save/Cancel with the editor. Mutations via api.post with list+detail query invalidation on success (NO optimistic updates — the gate result is server-authoritative). On edit-save 422: render the failure INLINE (parser error above editor / offending Thens flagged red) — NEVER a toast; keep the reviewer's text. Success toasts (sonner) for saved/approved/rejected only. Handle the graph-down-during-recheck inline state ("graph_mode up" + Retry checks; Approve stays disabled).
    Modify components/app-sidebar.tsx: append { icon: ListChecks, label: "Scenarios", href: "/scenarios" } after "Knowledge graph" (active via the existing pathname.startsWith).
    Create tests/e2e/scenarios.spec.ts: mocked-API e2e (no backend/keys) touching list render + status/risk badges + filter, drill-in, the editable textarea, a save that re-validates (mock a 422 → inline failure, text preserved, Approve disabled; mock a 200 → fresh honest indicators, Approve enabled), approve, reject-with-confirm, and the empty/error states.
    Carry invariants: zero new shadcn add; zero new frontend dependency (package.json/lock unchanged); honest gate state only from the server; errors inline, success toasts only; the design system reused verbatim from 01/05-UI-SPEC.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit ; npx eslint "app/(dashboard)/scenarios" components/scenarios lib/api/scenarios.ts components/app-sidebar.tsx tests/e2e/scenarios.spec.ts ; npx playwright test tests/e2e/scenarios.spec.ts ; git diff --exit-code package.json package-lock.json</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit clean; eslint clean on the touched paths.
    - npx playwright test tests/e2e/scenarios.spec.ts green against the mocked API (list, filter, drill-in, edit→422-inline-failure-text-preserved, edit→200-fresh-honest-indicators, approve, reject-confirm, empty/error states).
    - The Gherkin editor is a styled-native <textarea> (grep: no `shadcn add textarea`; no components/ui/textarea.tsx created).
    - Per-Then indicators read only from server then_results (no client-computed green).
    - git diff --exit-code package.json package-lock.json passes (zero new frontend deps).
    - One "Scenarios" nav item appended after "Knowledge graph".
  </acceptance_criteria>
  <done>The review-queue list + detail/review UI is built exactly to 06-UI-SPEC with a styled-native Gherkin editor, honest server-authoritative per-Then indicators, the edit→re-validate→approve flow, all empty/loading/error states, one sidebar item, and a green mocked e2e — zero new shadcn, zero new deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → review API | unauthenticated/edited requests cross here (edit body carries gherkin + then_refs) |
| edit body → gates → Neo4j | edited then_refs (edge_type/element_key) resolve against the graph |
| server then_results → UI | gate resolution result rendered as honest green/red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Spoofing | review + generate-scenarios endpoints | mitigate | router-level Depends(get_current_user); 401-unauth tests on every endpoint (V2/V4) |
| T-06-08 | Tampering | edited scenario bypassing gates | mitigate | edit-save AND approve both re-run validate_gherkin + assert_non_vacuous; 422 + no save on failure; status stays draft (D-02/D-04 gate integrity) |
| T-06-09 | Tampering | edited then_refs edge_type → Cypher | mitigate | resolution reuses assertion_gate (edge_type allow-list + parameterized read-only Cypher) — inherited from Slice 1 |
| T-06-10 | Spoofing | fabricated "Resolved" gate state in the UI | mitigate | per-Then indicators render strictly from the server then_results; no client-computed green; Approve disabled until the server confirms (D-03 honesty) |
| T-06-11 | Elevation of Privilege | codegen consuming non-approved scenarios | mitigate | list_approved filters status=="approved" in the query; codegen (Slice 3) reads only that |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages + ZERO new shadcn add this slice; package.json/lock unchanged (git diff gate) |
</threat_model>

<verification>
- `cd apps/api && uv run pytest tests/functional/test_scenarios_router.py -m "not graph" -q` → green; the graph-marked resolution check green under graph_mode.
- `cd apps/api && uv run pytest -m "not live_llm and not e2e and not graph" -q` → green (no regressions).
- `cd apps/web && npx tsc --noEmit && npx playwright test tests/e2e/scenarios.spec.ts` → green.
- `git diff --exit-code apps/web/package.json apps/web/package-lock.json` → clean.
- `cd apps/api && uv run pytest tests/unit/test_single_write_path.py -q` → green.
</verification>

<success_criteria>
- GEN-02: generated scenarios land in an approve/edit review queue; editing re-runs both gates server-side (422 + no save on failure); approve sets status=approved only on pass; reject works; only approved scenarios feed codegen.
- The UI is built exactly to 06-UI-SPEC with honest server-authoritative per-Then indicators, a styled-native Gherkin editor, and zero new shadcn/deps.
</success_criteria>

<output>
Create `.planning/phases/06-bdd-playwright-generation/06-02-SUMMARY.md` when done.
</output>
