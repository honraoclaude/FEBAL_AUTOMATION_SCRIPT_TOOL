# kg/

Knowledge-graph schema (`schema.cypher`) and the KG Writer — the ONLY Neo4j write path.

**Populated in Phases 3/5** (tracer bullet writes first, full KG engine in Phase 5). Empty by design until then — no speculative code (PITFALLS Pitfall 8).
