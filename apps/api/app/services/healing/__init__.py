"""Deterministic, keyless self-healing locator engine (Phase 8, HEAL-01..04 / QUAL-02).

The PURE core (confidence / geometry / candidates) is the keyless, stdlib-only scorer every
other Phase-8 plan consumes — table-testable on fixture dicts and byte-vendorable into the
in-spec `_healing.py` layer (plan 02). NO LLM, NO browser, NO I/O in the scorer (D-02).
"""
