"""Defect-intelligence services (Phase 9) — the PURE, keyless decision plane (D-01).

The class/confidence DECISION is deterministic, reproducible, and free: the classifier, the
fingerprint, and the infra-health signal import NOTHING from the LLM/gateway/LangChain/LangGraph/
explorer/DB plane (the test_no_llm_in_classifier grep gate enforces it). The LLM is used ONLY for
Jira description PROSE in a later plan — never for the classification.
"""
