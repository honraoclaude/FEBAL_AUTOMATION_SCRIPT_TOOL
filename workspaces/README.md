# workspaces/

Generated per-app test repositories (features/, pages/, specs/) — data PRODUCED by the platform, not platform source.

**Contents are gitignored** (see repo-root `.gitignore`). Generation engines write here from Phase 6 onward; each app workspace may become its own git repo for healing audit trails.
