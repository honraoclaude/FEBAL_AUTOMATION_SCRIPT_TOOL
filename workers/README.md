# workers/

Playwright executor runner image (Node/TS) consuming execution jobs from RabbitMQ.

**Populated in Phase 7** (Execution Engine). Empty by design until then — no speculative code (PITFALLS Pitfall 8).
