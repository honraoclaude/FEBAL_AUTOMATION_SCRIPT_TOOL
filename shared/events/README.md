# shared/events/

Cross-service queue message schemas (Pydantic / JSON Schema) — the inter-service API, versioned like one.

**Populated in Phase 3** (tracer bullet defines the first event contracts). Empty by design until then — no speculative code (PITFALLS Pitfall 8).
