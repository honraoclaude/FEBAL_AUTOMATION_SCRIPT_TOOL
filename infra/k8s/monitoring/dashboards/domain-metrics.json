{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "id": 1,
      "type": "stat",
      "title": "Self-Healing Success Rate",
      "description": "Platform self-healing success rate (0..1). Absent when there have been no heal attempts.",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "fieldConfig": {
        "defaults": { "unit": "percentunit", "min": 0, "max": 1 },
        "overrides": []
      },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [
        {
          "refId": "A",
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "qa_platform_heal_success_rate",
          "legendFormat": "heal success rate"
        }
      ]
    },
    {
      "id": 2,
      "type": "stat",
      "title": "Classification Precision",
      "description": "Defect classification precision = applied / (applied + rejected) over reviewed defects (0..1). Absent when zero defects have been reviewed (D-05 honest absence).",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "fieldConfig": {
        "defaults": { "unit": "percentunit", "min": 0, "max": 1 },
        "overrides": []
      },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [
        {
          "refId": "A",
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "qa_platform_classification_precision",
          "legendFormat": "classification precision"
        }
      ]
    },
    {
      "id": 3,
      "type": "gauge",
      "title": "Lifecycle Coverage Percent",
      "description": "Lifecycle coverage percent (0..100). Absent when Neo4j is down (graph profile inactive) — correct degrade, never a fake 0.",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "fieldConfig": {
        "defaults": { "unit": "percent", "min": 0, "max": 100 },
        "overrides": []
      },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [
        {
          "refId": "A",
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "qa_platform_coverage_percent",
          "legendFormat": "coverage %"
        }
      ]
    },
    {
      "id": 4,
      "type": "stat",
      "title": "Total LLM Spend (USD)",
      "description": "Total LLM spend in USD across all gateway calls (a real $0.0 when there are no usage rows, never absent).",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "fieldConfig": {
        "defaults": { "unit": "currencyUSD", "min": 0 },
        "overrides": []
      },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [
        {
          "refId": "A",
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "qa_platform_llm_cost_usd_total",
          "legendFormat": "llm cost usd"
        }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["qa-platform", "domain"],
  "templating": {
    "list": [
      {
        "name": "DS_PROMETHEUS",
        "type": "datasource",
        "query": "prometheus",
        "current": { "text": "Prometheus", "value": "Prometheus" },
        "hide": 0
      }
    ]
  },
  "time": { "from": "now-6h", "to": "now" },
  "timezone": "",
  "title": "QA Platform — Domain Metrics",
  "uid": "qa-domain-metrics",
  "version": 1
}
