# agents/

LangGraph agentic plane (supervisor, explorer, flow learning, generation, healing, triage, jira, llm gateway).

**Populated from Phase 2 onward** (LLM gateway first, then Explorer in Phase 4). Empty by design until then — no speculative code (PITFALLS Pitfall 8).
